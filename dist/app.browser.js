/* Generated file. Run "npm run build:browser" after editing src/. */
(function () {
  "use strict";

// brasse2-data.js
const BRASSE2_MODELS = [
  {
    "id": "0101ACR132",
    "brand": "GoodHome",
    "model": "Linto",
    "diameterCm": 132,
    "ceilingDistanceCm": 30,
    "testHeightCm": 220,
    "ratioDh": 0.22727272727272727,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 199,
    "powerMaxW": 55,
    "lwaMinDbA": 24,
    "lwaMaxDbA": 37.6,
    "vDirAssisMax": 0.629351439182915,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 2.86,
    "ceDirDeboutMax": 3.53,
    "ventMax": 0.732385886316784,
    "ceEntrMax": 3.09,
    "vAvgMax": 0.7327176273463275,
    "ceAvgMax": 3.09
  },
  {
    "id": "0201ACR132",
    "brand": "Hunter",
    "model": "Industrie II",
    "diameterCm": 132,
    "ceilingDistanceCm": 34,
    "testHeightCm": 216,
    "ratioDh": 0.25757575757575757,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 158,
    "powerMaxW": 65,
    "lwaMinDbA": 22,
    "lwaMaxDbA": 42.1,
    "vDirAssisMax": 1.22480933584501,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.8,
    "ceDirDeboutMax": 5.02,
    "ventMax": 0.742186824568255,
    "ceEntrMax": 3.11,
    "vAvgMax": 0.795012351477876,
    "ceAvgMax": 3.21
  },
  {
    "id": "0202ACR112",
    "brand": "Hunter",
    "model": "Low Profile III",
    "diameterCm": 112,
    "ceilingDistanceCm": 21,
    "testHeightCm": 229,
    "ratioDh": 0.1875,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 241,
    "powerMaxW": 62,
    "lwaMinDbA": 34.2,
    "lwaMaxDbA": 52.6,
    "vDirAssisMax": 0.796970147314295,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.21,
    "ceDirDeboutMax": 4.63,
    "ventMax": 0.558691833841077,
    "ceEntrMax": 2.68,
    "vAvgMax": 0.615355254700855,
    "ceAvgMax": 2.825
  },
  {
    "id": "0203ACR132",
    "brand": "Hunter",
    "model": "Low Profile III",
    "diameterCm": 132,
    "ceilingDistanceCm": 21,
    "testHeightCm": 229,
    "ratioDh": 0.1590909090909091,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 185,
    "powerMaxW": 66,
    "lwaMinDbA": 27.2,
    "lwaMaxDbA": 49.9,
    "vDirAssisMax": 0.304301237656251,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 1.64,
    "ceDirDeboutMax": 2.66,
    "ventMax": 0.589056562604535,
    "ceEntrMax": 2.76,
    "vAvgMax": 0.5901127932946351,
    "ceAvgMax": 2.765
  },
  {
    "id": "0204ACF132",
    "brand": "Hunter",
    "model": "Industrie II",
    "diameterCm": 132,
    "ceilingDistanceCm": 19,
    "testHeightCm": 231,
    "ratioDh": 0.14393939393939395,
    "fixation": "Monobloc",
    "motor": "AC",
    "rpmMax": 161,
    "powerMaxW": 69,
    "lwaMinDbA": 27.2,
    "lwaMaxDbA": 45.7,
    "vDirAssisMax": 0.568007566671305,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 2.7,
    "ceDirDeboutMax": 3.43,
    "ventMax": 0.413628242618239,
    "ceEntrMax": 2.18,
    "vAvgMax": 0.428007567668341,
    "ceAvgMax": 2.23
  },
  {
    "id": "0301ACR132",
    "brand": "Create",
    "model": "Wind Wood",
    "diameterCm": 132,
    "ceilingDistanceCm": 35,
    "testHeightCm": 215,
    "ratioDh": 0.26515151515151514,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 168,
    "powerMaxW": 68,
    "lwaMinDbA": 31.2,
    "lwaMaxDbA": 39.5,
    "vDirAssisMax": 1.16909064973686,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.74,
    "ceDirDeboutMax": 5.02,
    "ventMax": 0.770514266659398,
    "ceEntrMax": 3.17,
    "vAvgMax": 0.827922227922552,
    "ceAvgMax": 3.2750000000000004
  },
  {
    "id": "0302DCR152",
    "brand": "Create",
    "model": "Wind Large",
    "diameterCm": 152,
    "ceilingDistanceCm": 47,
    "testHeightCm": 203,
    "ratioDh": 0.3092105263157895,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 168,
    "powerMaxW": 37,
    "lwaMinDbA": 21.2,
    "lwaMaxDbA": 42.2,
    "vDirAssisMax": 1.53758032051201,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.09,
    "ceDirDeboutMax": 5.38,
    "ventMax": 1.04260037495599,
    "ceEntrMax": 3.59,
    "vAvgMax": 1.13702414248837,
    "ceAvgMax": 3.7
  },
  {
    "id": "0303DCR132",
    "brand": "Create",
    "model": "Wind Stylance",
    "diameterCm": 132,
    "ceilingDistanceCm": 46.5,
    "testHeightCm": 203.5,
    "ratioDh": 0.3522727272727273,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 199,
    "powerMaxW": 34,
    "lwaMinDbA": 23.4,
    "lwaMaxDbA": 45.1,
    "vDirAssisMax": 1.39306164536346,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.96,
    "ceDirDeboutMax": 5.37,
    "ventMax": 0.898203672943928,
    "ceEntrMax": 3.38,
    "vAvgMax": 0.9658962758333824,
    "ceAvgMax": 3.48
  },
  {
    "id": "0304DCF132",
    "brand": "Create",
    "model": "Wind Calm",
    "diameterCm": 132,
    "ceilingDistanceCm": 24,
    "testHeightCm": 226,
    "ratioDh": 0.18181818181818182,
    "fixation": "Monobloc",
    "motor": "DC",
    "rpmMax": 138,
    "powerMaxW": 13,
    "lwaMinDbA": 22,
    "lwaMaxDbA": 33.4,
    "vDirAssisMax": 0.895136412337948,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.38,
    "ceDirDeboutMax": 4.85,
    "ventMax": 0.599022746884592,
    "ceEntrMax": 2.79,
    "vAvgMax": 0.642026934367723,
    "ceAvgMax": 2.885
  },
  {
    "id": "0305DCR132",
    "brand": "Create",
    "model": "Wind Stylance",
    "diameterCm": 132,
    "ceilingDistanceCm": 37,
    "testHeightCm": 213,
    "ratioDh": 0.2803030303030303,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 200,
    "powerMaxW": 28,
    "lwaMinDbA": 23.5,
    "lwaMaxDbA": 43.4,
    "vDirAssisMax": 1.46882777903557,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.03,
    "ceDirDeboutMax": 5.29,
    "ventMax": 0.910107303159295,
    "ceEntrMax": 3.4,
    "vAvgMax": 0.9811737992920264,
    "ceAvgMax": 3.505
  },
  {
    "id": "0401DCR162",
    "brand": "Turbobrise",
    "model": "ERP160",
    "diameterCm": 162,
    "ceilingDistanceCm": 34,
    "testHeightCm": 216,
    "ratioDh": 0.20987654320987653,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 163,
    "powerMaxW": 37,
    "lwaMinDbA": 25.8,
    "lwaMaxDbA": 50.9,
    "vDirAssisMax": 1.55168098641193,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.1,
    "ceDirDeboutMax": 5.43,
    "ventMax": 1.00992488146978,
    "ceEntrMax": 3.55,
    "vAvgMax": 1.097245419537515,
    "ceAvgMax": 3.6550000000000002
  },
  {
    "id": "0501DCR132",
    "brand": "Big Ass Fans",
    "model": "Haiku L",
    "diameterCm": 132,
    "ceilingDistanceCm": 29.5,
    "testHeightCm": 220.5,
    "ratioDh": 0.22348484848484848,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 204,
    "powerMaxW": 22,
    "lwaMinDbA": 21.4,
    "lwaMaxDbA": 47.4,
    "vDirAssisMax": 1.62143777710214,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.15,
    "ceDirDeboutMax": 5.45,
    "ventMax": 0.93638130628756,
    "ceEntrMax": 3.44,
    "vAvgMax": 1.0110608649035329,
    "ceAvgMax": 3.545
  },
  {
    "id": "0701DCR086",
    "brand": "Inspire",
    "model": "Palmela S",
    "diameterCm": 86,
    "ceilingDistanceCm": 29,
    "testHeightCm": 221,
    "ratioDh": 0.3372093023255814,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 410,
    "powerMaxW": 16,
    "lwaMinDbA": 20,
    "lwaMaxDbA": 50.2,
    "vDirAssisMax": 1.73765286336134,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.23,
    "ceDirDeboutMax": 5.63,
    "ventMax": 0.798313581063095,
    "ceEntrMax": 3.22,
    "vAvgMax": 0.849292631578102,
    "ceAvgMax": 3.3049999999999997
  },
  {
    "id": "0702DCR091",
    "brand": "Inspire",
    "model": "Siroco Plus",
    "diameterCm": 91,
    "ceilingDistanceCm": 30,
    "testHeightCm": 220,
    "ratioDh": 0.32967032967032966,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 214,
    "powerMaxW": 33,
    "lwaMinDbA": 23.4,
    "lwaMaxDbA": 44.7,
    "vDirAssisMax": 0.984576986492205,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.51,
    "ceDirDeboutMax": 5.01,
    "ventMax": 0.742481601634956,
    "ceEntrMax": 3.11,
    "vAvgMax": 0.778449825462751,
    "ceAvgMax": 3.175
  },
  {
    "id": "0703DCR107",
    "brand": "Inspire",
    "model": "Beach M",
    "diameterCm": 107,
    "ceilingDistanceCm": 40,
    "testHeightCm": 210,
    "ratioDh": 0.37383177570093457,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 212,
    "powerMaxW": 43,
    "lwaMinDbA": 27.3,
    "lwaMaxDbA": 44.5,
    "vDirAssisMax": 1.37120262189318,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.94,
    "ceDirDeboutMax": 5.48,
    "ventMax": 0.843788473801684,
    "ceEntrMax": 3.3,
    "vAvgMax": 0.90306590792198,
    "ceAvgMax": 3.395
  },
  {
    "id": "0704DCR071",
    "brand": "Inspire",
    "model": "Beach S",
    "diameterCm": 71,
    "ceilingDistanceCm": 26,
    "testHeightCm": 224,
    "ratioDh": 0.36619718309859156,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 288,
    "powerMaxW": 24,
    "lwaMinDbA": 20.5,
    "lwaMaxDbA": 44.2,
    "vDirAssisMax": 1.11756156116727,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.68,
    "ceDirDeboutMax": 5.12,
    "ventMax": 0.716912147696791,
    "ceEntrMax": 3.06,
    "vAvgMax": 0.7698236427611396,
    "ceAvgMax": 3.165
  },
  {
    "id": "0705DCR086",
    "brand": "Inspire",
    "model": "Palmela S",
    "diameterCm": 86,
    "ceilingDistanceCm": 45.5,
    "testHeightCm": 204.5,
    "ratioDh": 0.5290697674418605,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 401,
    "powerMaxW": 15,
    "lwaMinDbA": 20.5,
    "lwaMaxDbA": 51.5,
    "vDirAssisMax": 1.77931876631836,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.26,
    "ceDirDeboutMax": 5.64,
    "ventMax": 0.793920427218899,
    "ceEntrMax": 3.21,
    "vAvgMax": 0.84705516749893,
    "ceAvgMax": 3.295
  },
  {
    "id": "0801DCR132",
    "brand": "brasseurs-air-re2020",
    "model": "IZYFAN",
    "diameterCm": 132,
    "ceilingDistanceCm": 31,
    "testHeightCm": 219,
    "ratioDh": 0.23484848484848486,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 224,
    "powerMaxW": 46,
    "lwaMinDbA": 26.8,
    "lwaMaxDbA": 53.8,
    "vDirAssisMax": 2.13533985845795,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.47,
    "ceDirDeboutMax": 5.82,
    "ventMax": 1.23214704968546,
    "ceEntrMax": 3.81,
    "vAvgMax": 1.32979038159797,
    "ceAvgMax": 3.9050000000000002
  },
  {
    "id": "0802DCF132",
    "brand": "brasseurs-air-re2020",
    "model": "Samarat",
    "diameterCm": 132,
    "ceilingDistanceCm": 15,
    "testHeightCm": 235,
    "ratioDh": 0.11363636363636363,
    "fixation": "Monobloc",
    "motor": "DC",
    "rpmMax": 203,
    "powerMaxW": 42,
    "lwaMinDbA": 25.3,
    "lwaMaxDbA": 50.1,
    "vDirAssisMax": 0.645966972580187,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 2.9,
    "ceDirDeboutMax": 3.63,
    "ventMax": 0.516662079063332,
    "ceEntrMax": 2.55,
    "vAvgMax": 0.521595611225302,
    "ceAvgMax": 2.575
  },
  {
    "id": "0803DCF132",
    "brand": "brasseurs-air-re2020",
    "model": "Samarat",
    "diameterCm": 132,
    "ceilingDistanceCm": 43,
    "testHeightCm": 207,
    "ratioDh": 0.32575757575757575,
    "fixation": "Monobloc",
    "motor": "DC",
    "rpmMax": 200,
    "powerMaxW": 44,
    "lwaMinDbA": 28.7,
    "lwaMaxDbA": 50,
    "vDirAssisMax": 1.90019283230333,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.34,
    "ceDirDeboutMax": 5.68,
    "ventMax": 1.12977147260952,
    "ceEntrMax": 3.7,
    "vAvgMax": 1.214781134816405,
    "ceAvgMax": 3.79
  },
  {
    "id": "0901DCR135",
    "brand": "S&P",
    "model": "HTD135DCOAK",
    "diameterCm": 135,
    "ceilingDistanceCm": 33,
    "testHeightCm": 217,
    "ratioDh": 0.24444444444444444,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 199,
    "powerMaxW": 28,
    "lwaMinDbA": 23.5,
    "lwaMaxDbA": 47,
    "vDirAssisMax": 1.19501555322719,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.77,
    "ceDirDeboutMax": 5.22,
    "ventMax": 0.824249870055398,
    "ceEntrMax": 3.26,
    "vAvgMax": 0.893759577701849,
    "ceAvgMax": 3.38
  },
  {
    "id": "0902ACR100",
    "brand": "S&P",
    "model": "HTB90RC",
    "diameterCm": 100,
    "ceilingDistanceCm": 36,
    "testHeightCm": 214,
    "ratioDh": 0.36,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 250,
    "powerMaxW": 61,
    "lwaMinDbA": 32.1,
    "lwaMaxDbA": 50.7,
    "vDirAssisMax": 1.66824984571817,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.18,
    "ceDirDeboutMax": 5.54,
    "ventMax": 0.881619546141039,
    "ceEntrMax": 3.36,
    "vAvgMax": 0.9444649093428354,
    "ceAvgMax": 3.455
  },
  {
    "id": "0903ACR140",
    "brand": "S&P",
    "model": "HTB140RC",
    "diameterCm": 140,
    "ceilingDistanceCm": 36,
    "testHeightCm": 214,
    "ratioDh": 0.2571428571428571,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 288.06779661016947,
    "powerMaxW": 53.55172413793103,
    "lwaMinDbA": 50.4,
    "lwaMaxDbA": 59.4,
    "vDirAssisMax": 1.45380616946281,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.02,
    "ceDirDeboutMax": 5.24,
    "ventMax": 0.875544883375605,
    "ceEntrMax": 3.35,
    "vAvgMax": 0.952533398413226,
    "ceAvgMax": 3.465
  },
  {
    "id": "1001ACR105",
    "brand": "Westinghouse",
    "model": "Princess euro",
    "diameterCm": 105,
    "ceilingDistanceCm": 27.5,
    "testHeightCm": 222.5,
    "ratioDh": 0.2619047619047619,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 222,
    "powerMaxW": 45,
    "lwaMinDbA": 28.8,
    "lwaMaxDbA": 46.4,
    "vDirAssisMax": 0.940357000786128,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.45,
    "ceDirDeboutMax": 4.89,
    "ventMax": 0.5878532672195,
    "ceEntrMax": 2.76,
    "vAvgMax": 0.627387752839985,
    "ceAvgMax": 2.85
  },
  {
    "id": "1002ACR142",
    "brand": "Westinghouse",
    "model": "Industrial",
    "diameterCm": 142,
    "ceilingDistanceCm": 44.5,
    "testHeightCm": 205.5,
    "ratioDh": 0.31338028169014087,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 262,
    "powerMaxW": 61,
    "lwaMinDbA": 28.3,
    "lwaMaxDbA": 58.7,
    "vDirAssisMax": 1.53299788613606,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.08,
    "ceDirDeboutMax": 5.33,
    "ventMax": 0.951115595736619,
    "ceEntrMax": 3.46,
    "vAvgMax": 1.0299300895140449,
    "ceAvgMax": 3.57
  },
  {
    "id": "1003ACR076",
    "brand": "Westinghouse",
    "model": "Turbo Swirl",
    "diameterCm": 76,
    "ceilingDistanceCm": 23.5,
    "testHeightCm": 226.5,
    "ratioDh": 0.3092105263157895,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 270,
    "powerMaxW": 50,
    "lwaMinDbA": 39.1,
    "lwaMaxDbA": 49.6,
    "vDirAssisMax": 1.12999224820634,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.7,
    "ceDirDeboutMax": 5.13,
    "ventMax": 0.707666367047059,
    "ceEntrMax": 3.04,
    "vAvgMax": 0.7573820996478164,
    "ceAvgMax": 3.135
  },
  {
    "id": "1004ACR132",
    "brand": "Westinghouse",
    "model": "Bendan",
    "diameterCm": 132,
    "ceilingDistanceCm": 24,
    "testHeightCm": 226,
    "ratioDh": 0.18181818181818182,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 182,
    "powerMaxW": 60,
    "lwaMinDbA": 23.1,
    "lwaMaxDbA": 49.3,
    "vDirAssisMax": 0.97832490949645,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.5,
    "ceDirDeboutMax": 4.8,
    "ventMax": 0.611275519002781,
    "ceEntrMax": 2.82,
    "vAvgMax": 0.666745501620845,
    "ceAvgMax": 2.96
  },
  {
    "id": "1101DCR128",
    "brand": "Faro",
    "model": "Just fan",
    "diameterCm": 128,
    "ceilingDistanceCm": 28,
    "testHeightCm": 222,
    "ratioDh": 0.21875,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 184,
    "powerMaxW": 29,
    "lwaMinDbA": 20.6,
    "lwaMaxDbA": 38.6,
    "vDirAssisMax": 1.04408995104338,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.59,
    "ceDirDeboutMax": 5.11,
    "ventMax": 0.682783326315273,
    "ceEntrMax": 2.99,
    "vAvgMax": 0.7330226397962956,
    "ceAvgMax": 3.09
  },
  {
    "id": "1101DCR128B",
    "brand": "Faro",
    "model": "Just fan",
    "diameterCm": 128,
    "ceilingDistanceCm": 38,
    "testHeightCm": 212,
    "ratioDh": 0.296875,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 178,
    "powerMaxW": 30,
    "lwaMinDbA": 20.9,
    "lwaMaxDbA": 43.3,
    "vDirAssisMax": 1.41366398598831,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.98,
    "ceDirDeboutMax": 5.18,
    "ventMax": 0.790180455968443,
    "ceEntrMax": 3.2,
    "vAvgMax": 0.8504025192816456,
    "ceAvgMax": 3.3049999999999997
  },
  {
    "id": "1102DCR132",
    "brand": "Faro",
    "model": "Nu",
    "diameterCm": 132,
    "ceilingDistanceCm": 31,
    "testHeightCm": 219,
    "ratioDh": 0.23484848484848486,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 220,
    "powerMaxW": 31,
    "lwaMinDbA": 26.9,
    "lwaMaxDbA": 44.1,
    "vDirAssisMax": 1.05418248100613,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.6,
    "ceDirDeboutMax": 4.72,
    "ventMax": 0.661284318074259,
    "ceEntrMax": 2.94,
    "vAvgMax": 0.7234872384905895,
    "ceAvgMax": 3.08
  },
  {
    "id": "1103DCR142",
    "brand": "Faro",
    "model": "Malvinas",
    "diameterCm": 142,
    "ceilingDistanceCm": 63,
    "testHeightCm": 187,
    "ratioDh": 0.44366197183098594,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 236,
    "powerMaxW": 75,
    "lwaMinDbA": 37.2,
    "lwaMaxDbA": 55.6,
    "vDirAssisMax": 1.16370049418924,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.73,
    "ceDirDeboutMax": 4.93,
    "ventMax": 0.851168188046172,
    "ceEntrMax": 3.31,
    "vAvgMax": 0.9143360539205341,
    "ceAvgMax": 3.41
  },
  {
    "id": "1201DCF132",
    "brand": "Fanelite",
    "model": "ARATUA",
    "diameterCm": 132,
    "ceilingDistanceCm": 21,
    "testHeightCm": 229,
    "ratioDh": 0.1590909090909091,
    "fixation": "Monobloc",
    "motor": "DC",
    "rpmMax": 167,
    "powerMaxW": 34,
    "lwaMinDbA": 25.6,
    "lwaMaxDbA": 43.9,
    "vDirAssisMax": 0.735333253888308,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.1,
    "ceDirDeboutMax": 4.39,
    "ventMax": 0.6049369995581,
    "ceEntrMax": 2.8,
    "vAvgMax": 0.688000125559125,
    "ceAvgMax": 3.005
  },
  {
    "id": "1401ACR132",
    "brand": "Casafan",
    "model": "Titanum",
    "diameterCm": 132,
    "ceilingDistanceCm": 26,
    "testHeightCm": 224,
    "ratioDh": 0.19696969696969696,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 266,
    "powerMaxW": 66,
    "lwaMinDbA": 25.2,
    "lwaMaxDbA": 55.1,
    "vDirAssisMax": 1.36357562395679,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.94,
    "ceDirDeboutMax": 5.35,
    "ventMax": 0.802227601736477,
    "ceEntrMax": 3.22,
    "vAvgMax": 0.8649589471965784,
    "ceAvgMax": 3.335
  },
  {
    "id": "1402DCF142",
    "brand": "CasaFan",
    "model": "Eco Pallas",
    "diameterCm": 142,
    "ceilingDistanceCm": 20,
    "testHeightCm": 230,
    "ratioDh": 0.14084507042253522,
    "fixation": "Monobloc",
    "motor": "DC",
    "rpmMax": 138,
    "powerMaxW": 26,
    "lwaMinDbA": 25.9,
    "lwaMaxDbA": 50.6,
    "vDirAssisMax": 0.745082975932793,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.12,
    "ceDirDeboutMax": 4.63,
    "ventMax": 0.535031626213815,
    "ceEntrMax": 2.61,
    "vAvgMax": 0.5678437346395699,
    "ceAvgMax": 2.71
  },
  {
    "id": "1403DCR152",
    "brand": "Casafan",
    "model": "Eco genuino",
    "diameterCm": 152,
    "ceilingDistanceCm": 35,
    "testHeightCm": 215,
    "ratioDh": 0.23026315789473684,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 155,
    "powerMaxW": 17,
    "lwaMinDbA": 25.1,
    "lwaMaxDbA": 32.3,
    "vDirAssisMax": 1.21029757473001,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.79,
    "ceDirDeboutMax": 4.89,
    "ventMax": 0.770742800881619,
    "ceEntrMax": 3.17,
    "vAvgMax": 0.845932017109402,
    "ceAvgMax": 3.295
  },
  {
    "id": "1404DCF140",
    "brand": "Casafan",
    "model": "Eco regento",
    "diameterCm": 140,
    "ceilingDistanceCm": 27,
    "testHeightCm": 223,
    "ratioDh": 0.19285714285714287,
    "fixation": "Monobloc",
    "motor": "DC",
    "rpmMax": 138,
    "powerMaxW": 26,
    "lwaMinDbA": 20.9,
    "lwaMaxDbA": 36.2,
    "vDirAssisMax": 0.995435947641635,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.53,
    "ceDirDeboutMax": 4.96,
    "ventMax": 0.665667760537087,
    "ceEntrMax": 2.95,
    "vAvgMax": 0.7270298816790746,
    "ceAvgMax": 3.075
  },
  {
    "id": "1405ACF132",
    "brand": "Casafan",
    "model": "Classic Flat III",
    "diameterCm": 132,
    "ceilingDistanceCm": 19,
    "testHeightCm": 231,
    "ratioDh": 0.14393939393939395,
    "fixation": "Monobloc",
    "motor": "AC",
    "rpmMax": 48,
    "powerMaxW": 25,
    "lwaMinDbA": 22.7,
    "lwaMaxDbA": 42.6,
    "vDirAssisMax": 0.214597470437516,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 0.96,
    "ceDirDeboutMax": 1.54,
    "ventMax": 0.171881129575172,
    "ceEntrMax": 0.5,
    "vAvgMax": 0.5946097525654754,
    "ceAvgMax": 2.775
  },
  {
    "id": "1501DCR100",
    "brand": "Leds C4",
    "model": "Xaloc",
    "diameterCm": 100,
    "ceilingDistanceCm": 27,
    "testHeightCm": 223,
    "ratioDh": 0.27,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 149,
    "powerMaxW": 6,
    "lwaMinDbA": 27.2,
    "lwaMaxDbA": 45.8,
    "vDirAssisMax": 0.499896026724955,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 2.5,
    "ceDirDeboutMax": 3.8,
    "ventMax": 0.323132777228623,
    "ceEntrMax": 1.75,
    "vAvgMax": 0.5822162924055125,
    "ceAvgMax": 2.75
  },
  {
    "id": "1601DCR152",
    "brand": "Anemoi",
    "model": "Boreas",
    "diameterCm": 152,
    "ceilingDistanceCm": 29,
    "testHeightCm": 221,
    "ratioDh": 0.19078947368421054,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 206,
    "powerMaxW": 37,
    "lwaMinDbA": 23.3,
    "lwaMaxDbA": 53.3,
    "vDirAssisMax": 1.72371295452746,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.22,
    "ceDirDeboutMax": 5.42,
    "ventMax": 1.07152848145258,
    "ceEntrMax": 3.63,
    "vAvgMax": 1.1821102958844052,
    "ceAvgMax": 3.76
  },
  {
    "id": "1602DCR132",
    "brand": "Anemoi",
    "model": "Cefiro",
    "diameterCm": 132,
    "ceilingDistanceCm": 28.5,
    "testHeightCm": 221.5,
    "ratioDh": 0.2159090909090909,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 204,
    "powerMaxW": 35,
    "lwaMinDbA": 23.7,
    "lwaMaxDbA": 52,
    "vDirAssisMax": 1.85400573973439,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.31,
    "ceDirDeboutMax": 5.81,
    "ventMax": 1.06728399752318,
    "ceEntrMax": 3.62,
    "vAvgMax": 1.153247381161725,
    "ceAvgMax": 3.725
  },
  {
    "id": "1701DCR150",
    "brand": "LucciAIR",
    "model": "Louisville",
    "diameterCm": 150,
    "ceilingDistanceCm": 29,
    "testHeightCm": 221,
    "ratioDh": 0.19333333333333333,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 168,
    "powerMaxW": 38,
    "lwaMinDbA": 27.4,
    "lwaMaxDbA": 51.7,
    "vDirAssisMax": 1.64945900980086,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.17,
    "ceDirDeboutMax": 5.58,
    "ventMax": 1.09629835683168,
    "ceEntrMax": 3.66,
    "vAvgMax": 1.18473940998746,
    "ceAvgMax": 3.76
  },
  {
    "id": "1801DCR132",
    "brand": "Wixled",
    "model": "Hanoi",
    "diameterCm": 132,
    "ceilingDistanceCm": 29.5,
    "testHeightCm": 220.5,
    "ratioDh": 0.22348484848484848,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 187,
    "powerMaxW": 28,
    "lwaMinDbA": 29.3,
    "lwaMaxDbA": 48.7,
    "vDirAssisMax": 1.64797003226019,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.17,
    "ceDirDeboutMax": 5.45,
    "ventMax": 0.935542123787554,
    "ceEntrMax": 3.44,
    "vAvgMax": 1.0086015735221991,
    "ceAvgMax": 3.545
  },
  {
    "id": "1803DCF132",
    "brand": "Wixled",
    "model": "Bony",
    "diameterCm": 132,
    "ceilingDistanceCm": 19,
    "testHeightCm": 231,
    "ratioDh": 0.14393939393939395,
    "fixation": "Monobloc",
    "motor": "DC",
    "rpmMax": 192,
    "powerMaxW": 28,
    "lwaMinDbA": 27.4,
    "lwaMaxDbA": 48.5,
    "vDirAssisMax": 0.67896886518465,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 2.98,
    "ceDirDeboutMax": 3.91,
    "ventMax": 0.506114898180005,
    "ceEntrMax": 2.52,
    "vAvgMax": 0.5335656697201455,
    "ceAvgMax": 2.605
  },
  {
    "id": "1802ACR132",
    "brand": "Wixled",
    "model": "Taliou",
    "diameterCm": 132,
    "ceilingDistanceCm": 32,
    "testHeightCm": 218,
    "ratioDh": 0.24242424242424243,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 175,
    "powerMaxW": 56,
    "lwaMinDbA": 33.1,
    "lwaMaxDbA": 46.5,
    "vDirAssisMax": 1.41,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.98,
    "ceDirDeboutMax": 5.44,
    "ventMax": 0.86,
    "ceEntrMax": 3.33,
    "vAvgMax": 0.93,
    "ceAvgMax": 3.44
  },
  {
    "id": "1901ACR140",
    "brand": "RS Pro",
    "model": "136-9561",
    "diameterCm": 140,
    "ceilingDistanceCm": 34,
    "testHeightCm": 216,
    "ratioDh": 0.24285714285714285,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 270,
    "powerMaxW": 72,
    "lwaMinDbA": null,
    "lwaMaxDbA": 59.7,
    "vDirAssisMax": 1.48144003705865,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 4.04,
    "ceDirDeboutMax": 5.45,
    "ventMax": 1.00383595588201,
    "ceEntrMax": 3.54,
    "vAvgMax": 1.076309608500965,
    "ceAvgMax": 3.63
  },
  {
    "id": "2101ACR122",
    "brand": "Wiltec",
    "model": 65052,
    "diameterCm": 122,
    "ceilingDistanceCm": 25.5,
    "testHeightCm": 224.5,
    "ratioDh": 0.20901639344262296,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 189,
    "powerMaxW": 55,
    "lwaMinDbA": 37.7,
    "lwaMaxDbA": 46.7,
    "vDirAssisMax": 1.10946579075317,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.67,
    "ceDirDeboutMax": 4.82,
    "ventMax": 0.645167339869863,
    "ceEntrMax": 2.9,
    "vAvgMax": 0.6929706558186239,
    "ceAvgMax": 3.01
  },
  {
    "id": "2301ACR122",
    "brand": "Proficare",
    "model": "PC-DVL-3071",
    "diameterCm": 122,
    "ceilingDistanceCm": 23,
    "testHeightCm": 227,
    "ratioDh": 0.1885245901639344,
    "fixation": "Rotule/pivot",
    "motor": "AC",
    "rpmMax": 283,
    "powerMaxW": 63,
    "lwaMinDbA": 41.6,
    "lwaMaxDbA": 59.3,
    "vDirAssisMax": 1.26547487302368,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.84,
    "ceDirDeboutMax": 5.01,
    "ventMax": 0.700770049825743,
    "ceEntrMax": 3.03,
    "vAvgMax": 0.7555393559821525,
    "ceAvgMax": 3.135
  },
  {
    "id": "2401DCR117",
    "brand": "Frabilamp",
    "model": "Klod",
    "diameterCm": 117,
    "ceilingDistanceCm": 33,
    "testHeightCm": 217,
    "ratioDh": 0.28205128205128205,
    "fixation": "Rotule/pivot",
    "motor": "DC",
    "rpmMax": 233,
    "powerMaxW": 28,
    "lwaMinDbA": 27.6,
    "lwaMaxDbA": 51.9,
    "vDirAssisMax": 1.08953625109074,
    "vDirDeboutMax": null,
    "ceDirAssisMax": 3.65,
    "ceDirDeboutMax": 4.96,
    "ventMax": 0.685948180065333,
    "ceEntrMax": 2.99,
    "vAvgMax": 0.7325406123959465,
    "ceAvgMax": 3.085
  }
];

// src/core/constants.js
const EPS = 1e-9;
const SMALL_FAN_LIMIT = 2.13;
const SMALL_SAFETY_HEIGHT = 2.13;
const LARGE_SAFETY_HEIGHT = 3.05;
const MAX_GRID_FANS = 36;

const MOUNT_MODES = [
  {
    id: "standard",
    label: "Montage standard",
    factor: 0.35,
    penaltyText: "Pas de reduction explicite dans le guide.",
    severity: "ok"
  },
  {
    id: "low-profile",
    label: "Montage low-profile",
    factor: 0.25,
    penaltyText: "Reduction de vitesse d'air d'environ 15 % selon le guide.",
    severity: "warn"
  }
];

const FLUSH_MODE = {
  id: "flush",
  label: "Montage flush",
  factor: 0.15,
  penaltyText:
    "Perte de performance de plus de 40 % ; le guide demande de l'eviter au maximum.",
  severity: "alert"
};

// src/core/formatters.js
function parseNumber(input) {
  const raw = typeof input === "string" ? input : input.value;
  return Number.parseFloat(String(raw).replace(",", "."));
}

function formatInputValue(value) {
  return Number.isFinite(value) ? String(value).replace(".", ",") : "";
}

function formatNumber(value, digits = 2) {
  return new Intl.NumberFormat("fr-FR", {
    minimumFractionDigits: 0,
    maximumFractionDigits: digits
  }).format(value);
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll("\"", "&quot;")
    .replaceAll("'", "&#39;");
}

function formatMeters(value, digits = 2) {
  return `${formatNumber(value, digits)} m`;
}

function formatDiameterCm(value) {
  return `${Math.round(value * 100)}`;
}

function formatDiameterCmList(values) {
  return values
    .map((value) => formatDiameterCm(typeof value === "number" ? value : value.diameter))
    .join(", ");
}

function formatSquareMeters(value, digits = 2) {
  return `${formatNumber(value, digits)} m²`;
}

function formatFactor(value, digits = 2) {
  return formatNumber(value, digits);
}

function formatTemp(value, digits = 2) {
  return `${formatNumber(value, digits)} °C`;
}

function formatDb(value, digits = 1) {
  return `${formatNumber(value, digits)} dBA`;
}

function formatWatts(value, digits = 0) {
  return `${formatNumber(value, digits)} W`;
}

function formatDateTime(value = new Date()) {
  return new Intl.DateTimeFormat("fr-FR", {
    dateStyle: "medium",
    timeStyle: "short"
  }).format(value);
}

function formatDiameterList(values) {
  return values.map((diameter) => formatMeters(diameter)).join(", ");
}

function joinWithOr(values) {
  if (values.length <= 1) {
    return values[0] || "";
  }
  if (values.length === 2) {
    return `${values[0]} ou ${values[1]}`;
  }
  return `${values.slice(0, -1).join(", ")} ou ${values[values.length - 1]}`;
}

// src/app/dom.js
function getDomRefs() {
  return {
    form: document.getElementById("calculator-form"),
    simulationNameInput: document.getElementById("simulation-name"),
    lengthInput: document.getElementById("length"),
    widthInput: document.getElementById("width"),
    heightInput: document.getElementById("height"),
    allowStandardInput: document.getElementById("allow-standard"),
    allowLowInput: document.getElementById("allow-low"),
    tabAuto: document.getElementById("tab-auto"),
    tabManual: document.getElementById("tab-manual"),
    autoFields: document.getElementById("auto-fields"),
    manualFields: document.getElementById("manual-fields"),
    fanCountSelect: document.getElementById("fan-count"),
    manualDiameterInput: document.getElementById("manual-diameter"),
    manualMountSelect: document.getElementById("manual-mount"),
    resetButton: document.getElementById("reset-button"),
    emptyState: document.getElementById("empty-state"),
    resultsContent: document.getElementById("results-content"),
    summaryGrid: document.getElementById("summary-grid"),
    highlights: document.getElementById("highlights"),
    statusNote: document.getElementById("status-note"),
    resultsList: document.getElementById("results-list"),
    reportKicker: document.getElementById("report-kicker"),
    reportTitle: document.getElementById("report-title"),
    reportContext: document.getElementById("report-context"),
    reportMeta: document.getElementById("report-meta"),
    exportSelectionSummary: document.getElementById("export-selection-summary"),
    exportPdfButton: document.getElementById("export-pdf-button"),
    catalogSearchInput: document.getElementById("catalog-search"),
    catalogBrandInput: document.getElementById("catalog-brand"),
    catalogDiameterInput: document.getElementById("catalog-diameter"),
    catalogMotorInput: document.getElementById("catalog-motor"),
    catalogFixationInput: document.getElementById("catalog-fixation"),
    catalogSortInput: document.getElementById("catalog-sort"),
    catalogResetButton: document.getElementById("catalog-reset-button"),
    catalogSummary: document.getElementById("catalog-summary"),
    catalogTableHost: document.getElementById("catalog-table-host")
  };
}

// src/app/state.js
function createAppState() {
  return {
    mode: "auto",
    latestReportState: null
  };
}

function setLatestReportState(state, latestReportState) {
  state.latestReportState = latestReportState;
}

function createSelectedReportOptionKeys(items) {
  return items.map((item) => item.key);
}

function getSelectableReportOptions(reportState) {
  if (!reportState) {
    return [];
  }

  if (reportState.kind === "uniformity-ok") {
    return reportState.candidates || [];
  }

  return [];
}

function getSelectedReportOptions(reportState) {
  const selectableOptions = getSelectableReportOptions(reportState);
  if (selectableOptions.length === 0) {
    return [];
  }

  const selectedKeys = new Set(
    reportState.selectedOptionKeys?.length
      ? reportState.selectedOptionKeys
      : createSelectedReportOptionKeys(selectableOptions)
  );

  return selectableOptions.filter((item) => selectedKeys.has(item.key));
}

function toggleLatestReportOptionSelection(state, optionKey, selected) {
  const reportState = state.latestReportState;
  const selectableOptions = getSelectableReportOptions(reportState);
  if (!reportState || selectableOptions.length === 0) {
    return;
  }

  const selectableKeys = selectableOptions.map((item) => item.key);
  if (!selectableKeys.includes(optionKey)) {
    return;
  }

  const nextSelectedKeys = new Set(
    reportState.selectedOptionKeys?.length
      ? reportState.selectedOptionKeys
      : createSelectedReportOptionKeys(selectableOptions)
  );

  if (selected) {
    nextSelectedKeys.add(optionKey);
  } else {
    nextSelectedKeys.delete(optionKey);
  }

  reportState.selectedOptionKeys = selectableKeys.filter((key) => nextSelectedKeys.has(key));
}

function resetState(state) {
  state.mode = "auto";
  state.latestReportState = null;
}

// src/core/messages.js
function buildHeightDiameterRequirementMessage(room, modes, realDiameters) {
  const maxRealDiameter = realDiameters.length > 0 ? realDiameters[realDiameters.length - 1] : null;
  if (!room || !Number.isFinite(room.height) || !Number.isFinite(maxRealDiameter) || modes.length === 0) {
    return "";
  }

  const blockedModes = modes
    .map((mode) => ({
      mode,
      minimumDiameter: room.height / (2 + mode.factor)
    }))
    .filter((entry) => entry.minimumDiameter > maxRealDiameter + EPS);

  if (blockedModes.length === 0) {
    return "";
  }

  const requiredText = joinWithOr(
    blockedModes.map(
      (entry) => `${formatMeters(entry.minimumDiameter)} en ${entry.mode.label.toLowerCase()}`
    )
  );

  return `La base BRASSE II embarquee s'arrete a ${formatMeters(maxRealDiameter)} (${formatDiameterCm(maxRealDiameter)} cm). Avec ${formatMeters(room.height)} de HSP, il faudrait ici en theorie au moins ${requiredText}.`;
}

function getCandidateWarnings(candidate) {
  const warnings = [];
  if (!candidate.recommendedSmallHeightMet && candidate.fanClass === "small") {
    warnings.push(
      "La recommandation d'aisance sur la hauteur sous pales (> 1,4 D) n'est pas atteinte."
    );
  }
  if (candidate.mountMode.id === "low-profile") {
    warnings.push(candidate.mountMode.penaltyText);
  }
  return warnings;
}

// src/core/brasse2.js
/**
 * Enrichit les modeles BRASSE II avec des indicateurs de compatibilite pour une
 * option (dimensionnement theorique, montage, FCC derive).
 */
function getBrasse2ModelsForCandidate(candidate, brasse2Models) {
  const maxDiameterCm = Math.round(candidate.theoreticalMaxDiameter * 100);

  return brasse2Models.map((model) => {
    const modelDiameter = model.diameterCm / 100;
    const coverageFactor = getCoverageFactorForDiameter(candidate.cellArea, modelDiameter);
    const coverageValid = isCoverageFactorValid(coverageFactor);
    const sizeFits = model.diameterCm <= maxDiameterCm + 0.5;
    const targetCeilingDistanceCm = candidate.mountMode.factor * modelDiameter * 100;

    return {
      ...model,
      sizing: {
        diameter: modelDiameter,
        coverageFactor,
        coverageValid,
        sizeFits
      },
      mountFits: model.ceilingDistanceCm <= targetCeilingDistanceCm + 0.5,
      mountDeltaCm: Number((targetCeilingDistanceCm - model.ceilingDistanceCm).toFixed(1))
    };
  });
}

function pickBestModel(models, comparator) {
  return [...models].sort(comparator)[0] || null;
}

function compareComfort(a, b) {
  if (Math.abs(b.ceDirDeboutMax - a.ceDirDeboutMax) > EPS) {
    return b.ceDirDeboutMax - a.ceDirDeboutMax;
  }
  if (Math.abs(a.lwaMaxDbA - b.lwaMaxDbA) > EPS) {
    return a.lwaMaxDbA - b.lwaMaxDbA;
  }
  return a.powerMaxW - b.powerMaxW;
}

function compareEfficiency(a, b) {
  if (Math.abs(b.cfeDirDeboutMax - a.cfeDirDeboutMax) > EPS) {
    return b.cfeDirDeboutMax - a.cfeDirDeboutMax;
  }
  if (Math.abs(a.lwaMaxDbA - b.lwaMaxDbA) > EPS) {
    return a.lwaMaxDbA - b.lwaMaxDbA;
  }
  return b.ceDirDeboutMax - a.ceDirDeboutMax;
}

function compareAcoustics(a, b) {
  if (Math.abs(a.lwaMaxDbA - b.lwaMaxDbA) > EPS) {
    return a.lwaMaxDbA - b.lwaMaxDbA;
  }
  if (Math.abs(b.ceDirDeboutMax - a.ceDirDeboutMax) > EPS) {
    return b.ceDirDeboutMax - a.ceDirDeboutMax;
  }
  return a.powerMaxW - b.powerMaxW;
}

function compareCoverage(a, b) {
  const coverageA = Number.isFinite(a.sizing?.coverageFactor) ? a.sizing.coverageFactor : -Infinity;
  const coverageB = Number.isFinite(b.sizing?.coverageFactor) ? b.sizing.coverageFactor : -Infinity;

  if (Math.abs(coverageB - coverageA) > EPS) {
    return coverageB - coverageA;
  }
  return compareComfort(a, b);
}

function modelMountLabel(model) {
  return model.mountFits
    ? "Distance plafond/BA compatible"
    : "Distance plafond/BA a verifier";
}

function buildModelPicks(models) {
  return [
    { title: "FCC", model: pickBestModel(models, compareCoverage) },
    { title: "Confort", model: pickBestModel(models, compareComfort) },
    { title: "Efficacite", model: pickBestModel(models, compareEfficiency) },
    { title: "Acoustique", model: pickBestModel(models, compareAcoustics) }
  ].filter((pick) => Boolean(pick.model));
}

function getReportModelHighlights(candidate, brasse2Models) {
  return buildModelPicks(getBrasse2ModelsForCandidate(candidate, brasse2Models));
}

// src/core/catalog.js
function compareCatalogIdentity(a, b) {
  const brandDiff = String(a.brand).localeCompare(String(b.brand), "fr", {
    sensitivity: "base"
  });
  if (brandDiff !== 0) {
    return brandDiff;
  }
  const modelDiff = String(a.model).localeCompare(String(b.model), "fr", {
    sensitivity: "base"
  });
  if (modelDiff !== 0) {
    return modelDiff;
  }
  return String(a.id).localeCompare(String(b.id), "fr", {
    sensitivity: "base",
    numeric: true
  });
}

const CATALOG_SORTS = {
  "diameter-asc": (a, b) => a.diameterCm - b.diameterCm || compareCatalogIdentity(a, b),
  "diameter-desc": (a, b) => b.diameterCm - a.diameterCm || compareCatalogIdentity(a, b),
  comfort: (a, b) => compareComfort(a, b) || compareCatalogIdentity(a, b),
  efficiency: (a, b) => compareEfficiency(a, b) || compareCatalogIdentity(a, b),
  acoustics: (a, b) => compareAcoustics(a, b) || compareCatalogIdentity(a, b),
  power: (a, b) => a.powerMaxW - b.powerMaxW || compareCatalogIdentity(a, b),
  brand: compareCatalogIdentity
};

function normalizeCatalogSearch(value) {
  return String(value || "")
    .trim()
    .toLocaleLowerCase("fr")
    .normalize("NFD")
    .replaceAll(/\p{Diacritic}/gu, "");
}

function getCatalogFilterOptions(brasse2Models) {
  return {
    brands: [...new Set(brasse2Models.map((model) => model.brand))].sort((a, b) =>
      String(a).localeCompare(String(b), "fr", { sensitivity: "base" })
    ),
    diameters: [...new Set(brasse2Models.map((model) => model.diameterCm))].sort((a, b) => a - b),
    motors: [...new Set(brasse2Models.map((model) => model.motor))].sort(),
    fixations: [...new Set(brasse2Models.map((model) => model.fixation))].sort((a, b) =>
      String(a).localeCompare(String(b), "fr", { sensitivity: "base" })
    )
  };
}

function getCatalogActiveFilterLabels(filters) {
  const labels = [];
  if (filters.search) {
    labels.push(`recherche: ${filters.search}`);
  }
  if (filters.brand) {
    labels.push(`marque: ${filters.brand}`);
  }
  if (Number.isFinite(filters.diameterCm)) {
    labels.push(`diametre: ${filters.diameterCm} cm`);
  }
  if (filters.motor) {
    labels.push(`moteur: ${filters.motor}`);
  }
  if (filters.fixation) {
    labels.push(`fixation: ${filters.fixation}`);
  }
  return labels;
}

function getFilteredCatalogModels(brasse2Models, filters) {
  const search = normalizeCatalogSearch(filters.search);
  const comparator = CATALOG_SORTS[filters.sort] || CATALOG_SORTS["diameter-asc"];

  return brasse2Models
    .filter((model) => {
      if (search) {
        const haystack = normalizeCatalogSearch(
          `${model.id} ${model.brand} ${model.model} ${model.motor} ${model.fixation} ${model.diameterCm}`
        );
        if (!haystack.includes(search)) {
          return false;
        }
      }

      if (filters.brand && model.brand !== filters.brand) {
        return false;
      }
      if (Number.isFinite(filters.diameterCm) && model.diameterCm !== filters.diameterCm) {
        return false;
      }
      if (filters.motor && model.motor !== filters.motor) {
        return false;
      }
      if (filters.fixation && model.fixation !== filters.fixation) {
        return false;
      }
      return true;
    })
    .sort(comparator);
}

// src/core/calepinage.js
function getRealDiametersForIntervals(realDiameters, intervals) {
  return realDiameters
    .map((diameter) => {
      const matchingInterval = intervals.find(
        (interval) => diameter >= interval.lower - EPS && diameter <= interval.upper + EPS
      );

      if (!matchingInterval) {
        return null;
      }

      return {
        diameter,
        fanClass: matchingInterval.fanClass
      };
    })
    .filter(Boolean);
}

function getCoverageFactorForDiameter(cellArea, diameter) {
  return diameter / Math.sqrt(cellArea);
}

function isCoverageFactorValid(coverageFactor) {
  return coverageFactor >= 0.2 - EPS && coverageFactor <= 0.4 + EPS;
}

function getCompatibleRealDiameters(realDiameters, intervals, cellArea) {
  return getRealDiametersForIntervals(realDiameters, intervals)
    .map((option) => {
      const coverageFactor = getCoverageFactorForDiameter(cellArea, option.diameter);
      if (!isCoverageFactorValid(coverageFactor)) {
        return null;
      }

      return {
        diameter: option.diameter,
        fanClass: option.fanClass,
        coverageFactor
      };
    })
    .filter(Boolean);
}

/**
 * Calcule les intervalles de diametres encore compatibles avec les contraintes
 * de hauteur pour un mode de montage donne.
 */
function buildHeightFeasibility(height, mountFactor, dGeoMin, dGeoMax) {
  const lowerHeightBound = height / (2 + mountFactor);
  const intervals = [];

  const smallUpper = Math.min(
    SMALL_FAN_LIMIT - EPS,
    (height - SMALL_SAFETY_HEIGHT) / mountFactor
  );
  const smallLower = Math.max(dGeoMin, lowerHeightBound);
  if (smallUpper > smallLower + EPS) {
    intervals.push({
      fanClass: "small",
      lower: smallLower,
      upper: Math.min(dGeoMax, smallUpper)
    });
  }

  const largeUpper = Math.min(
    (height - LARGE_SAFETY_HEIGHT) / mountFactor,
    height / (0.8 + mountFactor),
    dGeoMax
  );
  const largeLower = Math.max(dGeoMin, lowerHeightBound, SMALL_FAN_LIMIT);
  if (largeUpper > largeLower + EPS) {
    intervals.push({
      fanClass: "large",
      lower: largeLower,
      upper: largeUpper
    });
  }

  return intervals.filter((interval) => interval.upper > interval.lower + EPS);
}

/**
 * Evalue une trame uniforme et retourne une option complete si elle respecte
 * toutes les contraintes geometriques et de hauteur.
 */
function evaluateCandidate(room, nx, ny, mountMode, realDiameters) {
  const cellLength = room.length / nx;
  const cellWidth = room.width / ny;
  const cellArea = cellLength * cellWidth;
  const cellShort = Math.min(cellLength, cellWidth);
  const cellLong = Math.max(cellLength, cellWidth);
  const formFactor = cellLong / cellShort;
  const coverageMinDiameter = 0.2 * Math.sqrt(cellArea);
  const coverageMaxDiameter = 0.4 * Math.sqrt(cellArea);
  const wallMaxDiameter = cellShort / 2;

  const spacings = [];
  if (nx > 1) {
    spacings.push(cellLength);
  }
  if (ny > 1) {
    spacings.push(cellWidth);
  }

  const interFanMaxDiameter =
    spacings.length > 0 ? Math.min(...spacings) / 2.5 : Number.POSITIVE_INFINITY;

  const dGeoMax = Math.min(coverageMaxDiameter, wallMaxDiameter, interFanMaxDiameter);
  if (dGeoMax <= coverageMinDiameter + EPS) {
    return null;
  }

  const intervals = buildHeightFeasibility(room.height, mountMode.factor, coverageMinDiameter, dGeoMax);
  if (intervals.length === 0) {
    return null;
  }

  const bestInterval = intervals.reduce(
    (best, current) => (!best || current.upper > best.upper ? current : best),
    null
  );
  const theoreticalMaxDiameter = bestInterval.upper;
  const compatibleRealDiameters = Array.isArray(realDiameters)
    ? getCompatibleRealDiameters(realDiameters, intervals, cellArea)
    : [];

  // Le dimensionnement retourne un diamètre théorique recommandé indépendant d'une liste de modèles.
  // La sélection d'un modèle réel (et donc d'un diamètre nominal) est laissée à l'utilisateur.
  const diameter = theoreticalMaxDiameter;
  const mountDistance = mountMode.factor * diameter;
  const bladeHeight = room.height - mountDistance;
  const coverageFactor = getCoverageFactorForDiameter(cellArea, diameter);
  const recommendedSmallHeight = 1.4 * diameter;
  const recommendedSmallHeightMet =
    bestInterval.fanClass === "small"
      ? bladeHeight >= recommendedSmallHeight - EPS
      : true;

  const coordinates = [];
  for (let ix = 0; ix < nx; ix += 1) {
    for (let iy = 0; iy < ny; iy += 1) {
      coordinates.push({
        x: (ix + 0.5) * cellLength,
        y: (iy + 0.5) * cellWidth
      });
    }
  }

  return {
    key: `${nx}x${ny}-${mountMode.id}`,
    nx,
    ny,
    fanCount: nx * ny,
    room,
    mountMode,
    cellLength,
    cellWidth,
    cellArea,
    cellShort,
    cellLong,
    formFactor,
    diameter,
    theoreticalMaxDiameter,
    coverageFactor,
    mountDistance,
    bladeHeight,
    recommendedSmallHeightMet,
    recommendedSmallHeight,
    wallClearance: cellShort / 2,
    interFanSpacing: spacings.length > 0 ? Math.min(...spacings) : null,
    geometryCaps: {
      coverageMinDiameter,
      coverageMaxDiameter,
      wallMaxDiameter,
      interFanMaxDiameter
    },
    compatibleRealDiameters,
    coordinates,
    fanClass: bestInterval.fanClass,
    strictAdvice:
      "Verifier la fiche du modele retenu si son diametre nominal est proche d'une limite geometrique ou d'une limite de hauteur du guide."
  };
}

function compareCandidates(a, b) {
  if (Math.abs(b.diameter - a.diameter) > EPS) {
    return b.diameter - a.diameter;
  }

  const ffA = Math.abs(a.formFactor - 1);
  const ffB = Math.abs(b.formFactor - 1);
  if (Math.abs(ffA - ffB) > EPS) {
    return ffA - ffB;
  }

  const covA = Math.abs(0.4 - a.coverageFactor);
  const covB = Math.abs(0.4 - b.coverageFactor);
  if (Math.abs(covA - covB) > EPS) {
    return covA - covB;
  }

  if (a.mountMode.id !== b.mountMode.id) {
    return a.mountMode.id === "standard" ? -1 : 1;
  }

  return a.fanCount - b.fanCount;
}

function enumerateCandidates(room, maxFans, modes, realDiameters) {
  const candidates = [];

  for (let nx = 1; nx <= maxFans; nx += 1) {
    for (let ny = 1; ny <= maxFans; ny += 1) {
      if (nx * ny > maxFans) {
        continue;
      }

      for (const mode of modes) {
        const candidate = evaluateCandidate(room, nx, ny, mode, realDiameters);
        if (candidate) {
          candidates.push(candidate);
        }
      }
    }
  }

  return candidates.sort(compareCandidates);
}

function getFallbackFlushCandidate(room, maxFans, realDiameters) {
  const candidates = [];

  for (let nx = 1; nx <= maxFans; nx += 1) {
    for (let ny = 1; ny <= maxFans; ny += 1) {
      if (nx * ny > maxFans) {
        continue;
      }
      const candidate = evaluateCandidate(room, nx, ny, FLUSH_MODE, realDiameters);
      if (candidate) {
        candidates.push(candidate);
      }
    }
  }

  return candidates.sort(compareCandidates)[0] || null;
}

function findBestGridForFanCount(room, fanCount) {
  let bestNx = 1;
  let bestNy = fanCount;
  let minFormFactor = Number.POSITIVE_INFINITY;

  for (let nx = 1; nx <= fanCount; nx += 1) {
    if (fanCount % nx !== 0) {
      continue;
    }
    const ny = fanCount / nx;
    const cellLength = room.length / nx;
    const cellWidth = room.width / ny;
    const formFactor = Math.max(cellLength, cellWidth) / Math.min(cellLength, cellWidth);

    if (formFactor < minFormFactor) {
      minFormFactor = formFactor;
      bestNx = nx;
      bestNy = ny;
    }
  }

  return { nx: bestNx, ny: bestNy };
}

function evaluateCustomCandidate(room, fanCount, diameter, mountMode, realDiameters) {
  const { nx, ny } = findBestGridForFanCount(room, fanCount);

  const cellLength = room.length / nx;
  const cellWidth = room.width / ny;
  const cellArea = cellLength * cellWidth;
  const cellShort = Math.min(cellLength, cellWidth);
  const cellLong = Math.max(cellLength, cellWidth);
  const formFactor = cellLong / cellShort;

  const coverageMinDiameter = 0.2 * Math.sqrt(cellArea);
  const coverageMaxDiameter = 0.4 * Math.sqrt(cellArea);

  const spacings = [];
  if (nx > 1) {
    spacings.push(cellLength);
  }
  if (ny > 1) {
    spacings.push(cellWidth);
  }
  const interFanSpacing = spacings.length > 0 ? Math.min(...spacings) : null;

  // Calculs hauteurs
  const mountDistance = mountMode.factor * diameter;
  const bladeHeight = room.height - mountDistance;
  const recommendedSmallHeight = 1.4 * diameter;

  // Validations individuelles des règles de conformité BRASSE
  const wallClearanceOk = cellShort / 2 >= diameter - EPS;
  const spacingOk = interFanSpacing === null || interFanSpacing >= 2.5 * diameter - EPS;
  const coverageFactor = diameter / Math.sqrt(cellArea);
  const coverageOk = coverageFactor >= 0.2 - EPS && coverageFactor <= 0.4 + EPS;

  const isSmall = diameter < 2.13;
  const safetyHeightLimit = isSmall ? 2.13 : 3.05;
  const safetyHeightOk = bladeHeight >= safetyHeightLimit - EPS;

  let heightRangeOk = false;
  if (isSmall) {
    heightRangeOk = bladeHeight <= 2 * diameter + EPS;
  } else {
    heightRangeOk = bladeHeight >= 0.8 * diameter - EPS && bladeHeight <= 2 * diameter + EPS;
  }

  const recommendedSmallHeightMet = isSmall ? bladeHeight >= recommendedSmallHeight - EPS : true;
  const conforming = wallClearanceOk && spacingOk && coverageOk && safetyHeightOk;

  // Calcul des diamètres réels compatibles pour information complémentaire
  const intervals = buildHeightFeasibility(room.height, mountMode.factor, coverageMinDiameter, Math.min(coverageMaxDiameter, cellShort / 2, spacings.length > 0 ? interFanSpacing / 2.5 : Number.POSITIVE_INFINITY));
  const compatibleRealDiameters = getCompatibleRealDiameters(realDiameters, intervals, cellArea);

  const coordinates = [];
  for (let ix = 0; ix < nx; ix += 1) {
    for (let iy = 0; iy < ny; iy += 1) {
      coordinates.push({
        x: (ix + 0.5) * cellLength,
        y: (iy + 0.5) * cellWidth
      });
    }
  }

  return {
    key: `custom-${nx}x${ny}-${mountMode.id}`,
    nx,
    ny,
    fanCount,
    room,
    mountMode,
    cellLength,
    cellWidth,
    cellArea,
    cellShort,
    cellLong,
    formFactor,
    diameter,
    theoreticalMaxDiameter: diameter,
    coverageFactor,
    mountDistance,
    bladeHeight,
    recommendedSmallHeightMet,
    recommendedSmallHeight,
    wallClearance: cellShort / 2,
    interFanSpacing,
    geometryCaps: {
      coverageMinDiameter,
      coverageMaxDiameter,
      wallMaxDiameter: cellShort / 2,
      interFanMaxDiameter: spacings.length > 0 ? interFanSpacing / 2.5 : Number.POSITIVE_INFINITY
    },
    compatibleRealDiameters,
    coordinates,
    fanClass: isSmall ? "small" : "large",
    isCustom: true,
    conformity: {
      conforming,
      wallClearanceOk,
      spacingOk,
      coverageOk,
      safetyHeightOk,
      heightRangeOk
    },
    strictAdvice: "Veuillez vérifier la conformité physique avec le fabricant de l'appareil choisi."
  };
}

function findBestMarketAlternative(candidates) {
  const compatible = candidates.filter((c) => c.compatibleRealDiameters?.length > 0);
  if (compatible.length === 0) {
    return null;
  }

  return [...compatible].sort((a, b) => {
    const maxA = Math.max(...a.compatibleRealDiameters.map((d) => d.diameter));
    const maxB = Math.max(...b.compatibleRealDiameters.map((d) => d.diameter));
    if (Math.abs(maxA - maxB) > 1e-5) {
      return maxB - maxA; // larger diameter first
    }
    if (a.fanCount !== b.fanCount) {
      return a.fanCount - b.fanCount; // fewer fans first
    }
    return compareCandidates(a, b);
  })[0];
}

function convertCandidateToMarketDiameter(candidate) {
  if (!candidate.compatibleRealDiameters || candidate.compatibleRealDiameters.length === 0) {
    return candidate;
  }

  const marketDiameter = Math.max(...candidate.compatibleRealDiameters.map((d) => d.diameter));
  const cellArea = candidate.cellArea;
  const coverageFactor = marketDiameter / Math.sqrt(cellArea);
  const mountDistance = candidate.mountMode.factor * marketDiameter;
  const bladeHeight = candidate.room.height - mountDistance;
  const recommendedSmallHeight = 1.4 * marketDiameter;
  const isSmall = marketDiameter < 2.13;
  const recommendedSmallHeightMet = isSmall ? bladeHeight >= recommendedSmallHeight - 1e-9 : true;

  return {
    ...candidate,
    diameter: marketDiameter,
    coverageFactor,
    mountDistance,
    bladeHeight,
    recommendedSmallHeight,
    recommendedSmallHeightMet,
    fanClass: isSmall ? "small" : "large",
    isMarketAlternative: true
  };
}

// src/ui/catalog.js
function setCatalogSelectOptions(select, options, emptyLabel, formatter = (value) => value) {
  select.innerHTML = [
    `<option value="">${escapeHtml(emptyLabel)}</option>`,
    ...options.map(
      (value) =>
        `<option value="${escapeHtml(String(value))}">${escapeHtml(formatter(value))}</option>`
    )
  ].join("");
}

function initializeCatalogFilters(dom, brasse2Models) {
  const options = getCatalogFilterOptions(brasse2Models);
  setCatalogSelectOptions(dom.catalogBrandInput, options.brands, "Toutes les marques");
  setCatalogSelectOptions(dom.catalogDiameterInput, options.diameters, "Tous les diametres", (value) => `${value} cm`);
  setCatalogSelectOptions(dom.catalogMotorInput, options.motors, "Tous les moteurs");
  setCatalogSelectOptions(dom.catalogFixationInput, options.fixations, "Toutes les fixations");
  dom.catalogSortInput.value = "diameter-asc";
}

function resetCatalogFilters(dom) {
  dom.catalogSearchInput.value = "";
  dom.catalogBrandInput.value = "";
  dom.catalogDiameterInput.value = "";
  dom.catalogMotorInput.value = "";
  dom.catalogFixationInput.value = "";
  dom.catalogSortInput.value = "diameter-asc";
}

function getCatalogFilterState(dom) {
  return {
    search: String(dom.catalogSearchInput.value || "").trim(),
    brand: dom.catalogBrandInput.value,
    diameterCm: Number.parseInt(dom.catalogDiameterInput.value, 10),
    motor: dom.catalogMotorInput.value,
    fixation: dom.catalogFixationInput.value,
    sort: dom.catalogSortInput.value || "diameter-asc"
  };
}

function renderCatalogTable(models) {
  if (models.length === 0) {
    return `
      <div class="notice warning">
        <strong>Aucun modele ne correspond aux filtres actifs.</strong>
        Ajustez les criteres ou reinitialisez les filtres pour retrouver la base complete.
      </div>
    `;
  }

  return `
    <div class="table-wrap catalog-table-wrap">
      <table>
        <thead>
          <tr>
            <th>Ref.</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Diam.</th>
            <th>Moteur</th>
            <th>Fixation</th>
            <th>Plafond/BA</th>
            <th>H test</th>
            <th>CE dir debout</th>
            <th>CE moyen</th>
            <th>LwA Vmin</th>
            <th>LwA Vmax</th>
            <th>P max</th>
          </tr>
        </thead>
        <tbody>
          ${models
            .map(
              (model) => `
                <tr>
                  <td>${escapeHtml(model.id)}</td>
                  <td>${escapeHtml(model.brand)}</td>
                  <td>${escapeHtml(model.model)}</td>
                  <td>${escapeHtml(String(model.diameterCm))}</td>
                  <td>${escapeHtml(model.motor)}</td>
                  <td>${escapeHtml(model.fixation)}</td>
                  <td>${formatNumber(model.ceilingDistanceCm, 1)} cm</td>
                  <td>${formatNumber(model.testHeightCm, 1)} cm</td>
                  <td>${formatTemp(model.ceDirDeboutMax)}</td>
                  <td>${formatTemp(model.ceAvgMax)}</td>
                  <td>${formatDb(model.lwaMinDbA)}</td>
                  <td>${formatDb(model.lwaMaxDbA)}</td>
                  <td>${formatWatts(model.powerMaxW, 0)}</td>
                </tr>
              `
            )
            .join("")}
        </tbody>
      </table>
    </div>
  `;
}

function renderCatalog(dom, brasse2Models) {
  const filters = getCatalogFilterState(dom);
  const models = getFilteredCatalogModels(brasse2Models, filters);
  const activeFilters = getCatalogActiveFilterLabels(filters);

  dom.catalogSummary.innerHTML = `
    <strong>${models.length}</strong> modele${models.length > 1 ? "s" : ""} affiche${models.length > 1 ? "s" : ""}
    sur ${brasse2Models.length}
    ${activeFilters.length > 0 ? ` • Filtres actifs : ${escapeHtml(activeFilters.join(" • "))}` : ""}
  `;
  dom.catalogTableHost.innerHTML = renderCatalogTable(models);
}

// src/ui/planSvg.js
function svgLabelBox(x, y, label, anchor = "middle", fontSize = 10.5) {
  const textWidth = Math.max(54, label.length * (fontSize * 0.6));
  let rectX = x - textWidth / 2 - 6;

  if (anchor === "start") {
    rectX = x - 6;
  } else if (anchor === "end") {
    rectX = x - textWidth - 6;
  }

  return `
    <g>
      <rect x="${rectX}" y="${y - 12}" width="${textWidth + 12}" height="18" rx="9"
        fill="rgba(255,250,242,0.96)" stroke="rgba(29,47,44,0.12)" />
      <text x="${x}" y="${y + 1.5}" text-anchor="${anchor}" font-size="${fontSize}" fill="#163531" font-weight="700">${label}</text>
    </g>
  `;
}

function svgDimensionLine(
  x1,
  y1,
  x2,
  y2,
  label,
  anchor = "middle",
  offsetX = 0,
  offsetY = -8,
  fontSize = 10.5
) {
  const labelX = (x1 + x2) / 2 + offsetX;
  const labelY = (y1 + y2) / 2 + offsetY;
  const isHorizontal = Math.abs(x2 - x1) >= Math.abs(y2 - y1);
  const startTick = isHorizontal
    ? `<line x1="${x1}" y1="${y1 - 5}" x2="${x1}" y2="${y1 + 5}" stroke="#b86d21" stroke-width="2" />`
    : `<line x1="${x1 - 5}" y1="${y1}" x2="${x1 + 5}" y2="${y1}" stroke="#b86d21" stroke-width="2" />`;
  const endTick = isHorizontal
    ? `<line x1="${x2}" y1="${y2 - 5}" x2="${x2}" y2="${y2 + 5}" stroke="#b86d21" stroke-width="2" />`
    : `<line x1="${x2 - 5}" y1="${y2}" x2="${x2 + 5}" y2="${y2}" stroke="#b86d21" stroke-width="2" />`;

  return `
    <g>
      <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="#b86d21" stroke-width="2" />
      ${startTick}
      ${endTick}
      ${svgLabelBox(labelX, labelY, label, anchor, fontSize)}
    </g>
  `;
}

function svgFanSymbol(cx, cy, radius, index) {
  const bladeLength = radius * 0.78;
  const bladeWidth = Math.max(7, radius * 0.24);
  const hubRadius = Math.max(6, radius * 0.16);
  const bladeShape = `
    <path d="
      M ${hubRadius * 0.35} ${-bladeWidth / 2}
      Q ${bladeLength * 0.48} ${-bladeWidth * 0.72} ${bladeLength} 0
      Q ${bladeLength * 0.48} ${bladeWidth * 0.72} ${hubRadius * 0.35} ${bladeWidth / 2}
      Z
    " fill="#c9d3df" stroke="#6b7280" stroke-width="1.2" />
  `;

  return `
    <g>
      <circle cx="${cx}" cy="${cy}" r="${radius}" fill="rgba(31,79,130,0.06)" stroke="rgba(31,79,130,0.45)" stroke-width="1.6" stroke-dasharray="5 4" />
      <g transform="translate(${cx} ${cy})">
        <g transform="rotate(0)">${bladeShape}</g>
        <g transform="rotate(120)">${bladeShape}</g>
        <g transform="rotate(240)">${bladeShape}</g>
        <circle cx="0" cy="0" r="${hubRadius}" fill="#4b5563" />
        <circle cx="0" cy="0" r="${Math.max(2.6, hubRadius * 0.35)}" fill="#f9fafb" />
      </g>
      <circle cx="${cx}" cy="${cy}" r="5" fill="#111827" />
      <text x="${cx}" y="${cy + 3.8}" text-anchor="middle" font-size="9" fill="#ffffff" font-weight="700">${index + 1}</text>
    </g>
  `;
}

function getPlanCanvasMetrics(candidate) {
  const roomAspectRatio =
    Math.max(candidate.room.length, candidate.room.width) /
    Math.min(candidate.room.length, candidate.room.width);
  const rotateForFit = roomAspectRatio >= 2.2 && candidate.room.length > candidate.room.width;

  if (rotateForFit) {
    return {
      roomAspectRatio,
      rotateForFit,
      canvasWidth: 320,
      canvasHeight: 400,
      wrapMinHeight: 392
    };
  }

  if (roomAspectRatio >= 1.55) {
    return {
      roomAspectRatio,
      rotateForFit,
      canvasWidth: 360,
      canvasHeight: 290,
      wrapMinHeight: 320
    };
  }

  return {
    roomAspectRatio,
    rotateForFit,
    canvasWidth: 360,
    canvasHeight: 250,
    wrapMinHeight: 280
  };
}

function planWrapStyle(candidate) {
  const { wrapMinHeight } = getPlanCanvasMetrics(candidate);
  return `--plan-min-height:${wrapMinHeight}px;`;
}

function svgForCandidate(candidate) {
  const { roomAspectRatio, rotateForFit, canvasWidth, canvasHeight } = getPlanCanvasMetrics(candidate);
  const paddingX = 28;
  const paddingY = 28;
  const displayRoomLength = rotateForFit ? candidate.room.width : candidate.room.length;
  const displayRoomHeight = rotateForFit ? candidate.room.length : candidate.room.width;
  const scale = Math.min(
    (canvasWidth - paddingX * 2) / displayRoomLength,
    (canvasHeight - paddingY * 2) / displayRoomHeight
  );
  const width = canvasWidth;
  const height = canvasHeight;
  const roomWidth = displayRoomLength * scale;
  const roomHeight = displayRoomHeight * scale;
  const roomX = (canvasWidth - roomWidth) / 2;
  const roomY = (canvasHeight - roomHeight) / 2;
  const compactMode = roomAspectRatio >= 2.4;
  const labelFontSize = compactMode ? 9.2 : 10.5;
  const compactOffset = compactMode ? 2 : 0;
  const displayCoordinates = candidate.coordinates.map((point) =>
    rotateForFit ? { x: point.y, y: point.x } : { x: point.x, y: point.y }
  );
  const displayCellLength = rotateForFit ? candidate.cellWidth : candidate.cellLength;
  const displayCellHeight = rotateForFit ? candidate.cellLength : candidate.cellWidth;
  const displayNx = rotateForFit ? candidate.ny : candidate.nx;
  const displayNy = rotateForFit ? candidate.nx : candidate.ny;

  const gridLines = [];
  for (let i = 1; i < displayNx; i += 1) {
    const x = roomX + i * displayCellLength * scale;
    gridLines.push(
      `<line x1="${x}" y1="${roomY}" x2="${x}" y2="${roomY + roomHeight}" stroke="rgba(29,47,44,0.18)" stroke-dasharray="6 5" />`
    );
  }
  for (let j = 1; j < displayNy; j += 1) {
    const y = roomY + j * displayCellHeight * scale;
    gridLines.push(
      `<line x1="${roomX}" y1="${y}" x2="${roomX + roomWidth}" y2="${y}" stroke="rgba(29,47,44,0.18)" stroke-dasharray="6 5" />`
    );
  }

  const fans = displayCoordinates
    .map((point, index) => {
      const cx = roomX + point.x * scale;
      const cy = roomY + point.y * scale;
      const r = (candidate.diameter / 2) * scale;
      return svgFanSymbol(cx, cy, r, index);
    })
    .join("");

  const firstPoint = displayCoordinates[0];
  const firstCx = roomX + firstPoint.x * scale;
  const firstCy = roomY + firstPoint.y * scale;
  const radius = (candidate.diameter / 2) * scale;
  const measurements = [];

  measurements.push(
    svgDimensionLine(
      firstCx - radius,
      firstCy - radius - (compactMode ? 8 : 12),
      firstCx + radius,
      firstCy - radius - (compactMode ? 8 : 12),
      `D ${formatMeters(candidate.diameter)}`,
      "middle",
      0,
      compactMode ? -6 : -8,
      labelFontSize
    )
  );

  if (firstPoint.x <= firstPoint.y) {
    measurements.push(
      svgDimensionLine(
        roomX,
        Math.min(height - 14, firstCy + radius + (compactMode ? 12 : 16)),
        firstCx,
        Math.min(height - 14, firstCy + radius + (compactMode ? 12 : 16)),
        `Mur ${formatMeters(firstPoint.x)}`,
        "middle",
        0,
        compactMode ? -6 : -8,
        labelFontSize
      )
    );
  } else {
    measurements.push(
      svgDimensionLine(
        Math.min(width - 16, firstCx + radius + (compactMode ? 12 : 16)),
        roomY,
        Math.min(width - 16, firstCx + radius + (compactMode ? 12 : 16)),
        firstCy,
        `Mur ${formatMeters(firstPoint.y)}`,
        "start",
        8 + compactOffset,
        -2,
        labelFontSize
      )
    );
  }

  if (candidate.interFanSpacing) {
    const horizontalSpacing = displayNx > 1 ? displayCellLength : Number.POSITIVE_INFINITY;
    const verticalSpacing = displayNy > 1 ? displayCellHeight : Number.POSITIVE_INFINITY;

    if (horizontalSpacing <= verticalSpacing) {
      const secondCx = roomX + (firstPoint.x + displayCellLength) * scale;
      const lineY = Math.max(roomY + 10, firstCy - radius - (compactMode ? 26 : 34));
      measurements.push(
        svgDimensionLine(
          firstCx,
          lineY,
          secondCx,
          lineY,
          `Entraxe ${formatMeters(displayCellLength)}`,
          "middle",
          0,
          compactMode ? -6 : -8,
          labelFontSize
        )
      );
    } else {
      const secondCy = roomY + (firstPoint.y + displayCellHeight) * scale;
      const lineX = Math.min(width - 12, firstCx + radius + (compactMode ? 24 : 32));
      measurements.push(
        svgDimensionLine(
          lineX,
          firstCy,
          lineX,
          secondCy,
          `Entraxe ${formatMeters(displayCellHeight)}`,
          "start",
          8 + compactOffset,
          -2,
          labelFontSize
        )
      );
    }
  }

  return `
    <svg viewBox="0 0 ${width} ${height}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
      <rect x="${roomX}" y="${roomY}" width="${roomWidth}" height="${roomHeight}" rx="18" fill="#faf5eb" stroke="rgba(29,47,44,0.2)" stroke-width="2.5" />
      ${gridLines.join("")}
      ${fans}
      ${measurements.join("")}
    </svg>
  `;
}

// src/ui/reportHeader.js
function refreshReportHeader(
  dom,
  { simulationName, generatedAt = new Date() }
) {
  dom.reportKicker.textContent = `Rapport de simulation • le ${formatDateTime(generatedAt)}`;
  dom.reportTitle.textContent = simulationName;
  dom.reportContext.textContent = "";
  dom.reportContext.classList.add("hidden");

  const metaItems = [];
  dom.reportMeta.innerHTML = metaItems
    .map(
      ([label, value]) => `
        <article class="report-meta-card">
          <strong>${escapeHtml(label)}</strong>
          <span>${escapeHtml(value)}</span>
        </article>
      `
    )
    .join("");
  dom.reportMeta.classList.toggle("hidden", metaItems.length === 0);
}

function setExportAvailability(dom, enabled) {
  dom.exportPdfButton.disabled = !enabled;
}

function setExportSelectionSummary(dom, text = "") {
  if (!dom.exportSelectionSummary) {
    return;
  }

  dom.exportSelectionSummary.textContent = text;
  dom.exportSelectionSummary.classList.toggle("hidden", !text);
}

// src/ui/results.js
const modelSectionRegistry = new Map();
let nextModelSectionId = 1;

function registerModelSection(models) {
  const sectionId = `compatible-models-${nextModelSectionId}`;
  nextModelSectionId += 1;
  modelSectionRegistry.set(sectionId, models);
  return sectionId;
}

function getDefaultCompatibleModelsComparator(a, b) {
  if (Boolean(a.sizing?.sizeFits) !== Boolean(b.sizing?.sizeFits)) {
    return a.sizing?.sizeFits ? -1 : 1;
  }
  if (Boolean(a.sizing?.coverageValid) !== Boolean(b.sizing?.coverageValid)) {
    return a.sizing?.coverageValid ? -1 : 1;
  }
  if (Math.abs((b.sizing?.coverageFactor ?? -Infinity) - (a.sizing?.coverageFactor ?? -Infinity)) > EPS) {
    return (b.sizing?.coverageFactor ?? -Infinity) - (a.sizing?.coverageFactor ?? -Infinity);
  }
  return compareComfort(a, b);
}

function sortCompatibleModels(models, sortKey = "default") {
  const comparator =
    {
      default: getDefaultCompatibleModelsComparator,
      "diameter-desc": (a, b) =>
        b.diameterCm - a.diameterCm || getDefaultCompatibleModelsComparator(a, b),
      "diameter-asc": (a, b) =>
        a.diameterCm - b.diameterCm || getDefaultCompatibleModelsComparator(a, b),
      "fcc-desc": (a, b) => compareCoverage(a, b) || getDefaultCompatibleModelsComparator(a, b),
      comfort: (a, b) => compareComfort(a, b) || getDefaultCompatibleModelsComparator(a, b),
      efficiency: (a, b) =>
        compareEfficiency(a, b) || getDefaultCompatibleModelsComparator(a, b),
      acoustics: (a, b) =>
        compareAcoustics(a, b) || getDefaultCompatibleModelsComparator(a, b)
    }[sortKey] || getDefaultCompatibleModelsComparator;

  return [...models].sort(comparator);
}

function filterCompatibleModels(models, { search }) {
  const normalizedSearch = normalizeCatalogSearch(search);

  return models.filter((model) => {
    if (!normalizedSearch) {
      return true;
    }

    const haystack = normalizeCatalogSearch(
      [
        model.id,
        model.brand,
        model.model,
        model.motor,
        model.fixation,
        String(model.diameterCm)
      ].join(" ")
    );

    return haystack.includes(normalizedSearch);
  });
}

function renderModelsTableSummary(filteredCount, totalCount) {
  if (filteredCount === totalCount) {
    return `${totalCount} modele${totalCount > 1 ? "s" : ""}`;
  }

  return `${filteredCount} / ${totalCount} modele${totalCount > 1 ? "s" : ""}`;
}

function getNeutralMountSummary(items) {
  const labels = [
    ...new Set(
      items
        .map((item) => item.mountMode?.id)
        .filter(Boolean)
        .map((mountId) => (mountId === "low-profile" ? "low-profile" : "standard"))
    )
  ];

  return labels.length > 0 ? labels.join(" + ") : "Aucun";
}

function getMaxDiameterSummary(items) {
  const diameters = items.map((item) => item.diameter).filter(Number.isFinite);

  if (diameters.length === 0) {
    return {
      value: "Aucun",
      detail: "Aucun diametre reel compatible"
    };
  }

  const maxDiameter = Math.max(...diameters);

  return {
    value: `${formatDiameterCm(maxDiameter)} cm`,
    detail: "Plus grand diametre reel compatible sur les options affichees"
  };
}

function getDistinctCompatibleModels(items, brasse2Models) {
  const modelsById = new Map();

  items.forEach((item) => {
    getBrasse2ModelsForCandidate(item, brasse2Models).forEach((model) => {
      if (model?.sizing?.sizeFits && model?.sizing?.coverageValid) {
        modelsById.set(model.id, model);
      }
    });
  });

  return [...modelsById.values()];
}

function createSummaryCard(label, value, detail) {
  return `
    <article class="summary-card">
      <strong>${label}</strong>
      <span>${value}</span>
      <div class="hint">${detail}</div>
    </article>
  `;
}

function renderExportOptionToggle(optionKey, checked) {
  return `
    <label class="export-option-toggle">
      <input type="checkbox" data-export-option-key="${optionKey}" ${checked ? "checked" : ""}>
      <span>Inclure dans le PDF</span>
    </label>
  `;
}

function renderModelsTable(models) {
  if (models.length === 0) {
    return `
      <div class="notice warning">
        <strong>Aucun modele ne correspond aux filtres actifs.</strong>
        Elargissez la recherche ou reinitialisez les filtres de cette option.
      </div>
    `;
  }

  return `
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>Diam.</th>
            <th>FCC calc.</th>
            <th>OK dim.</th>
            <th>OK FCC</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Moteur</th>
            <th>Fixation</th>
            <th>Plafond/BA</th>
            <th>CE dir debout Vmax</th>
            <th>CE moyen Vmax</th>
            <th>LwA Vmin</th>
            <th>LwA Vmax</th>
            <th>Lecture montage</th>
          </tr>
        </thead>
        <tbody>
          ${models
            .map(
              (model) => `
                <tr>
                  <td>${model.diameterCm}</td>
                  <td>${Number.isFinite(model.sizing?.coverageFactor) ? formatFactor(model.sizing.coverageFactor) : "—"}</td>
                  <td>${model.sizing?.sizeFits ? "Oui" : "Non"}</td>
                  <td>${model.sizing?.coverageValid ? "Oui" : "Non"}</td>
                  <td>${model.brand}</td>
                  <td>${model.model}</td>
                  <td>${model.motor}</td>
                  <td>${model.fixation}</td>
                  <td>${formatNumber(model.ceilingDistanceCm, 1)} cm</td>
                  <td>${formatTemp(model.ceDirDeboutMax)}</td>
                  <td>${formatTemp(model.ceAvgMax)}</td>
                  <td>${formatDb(model.lwaMinDbA)}</td>
                  <td>${formatDb(model.lwaMaxDbA)}</td>
                  <td>${modelMountLabel(model)}</td>
                </tr>
              `
            )
            .join("")}
        </tbody>
      </table>
    </div>
  `;
}

function renderModelsToolbar(sectionId, models) {
  return `
    <div class="models-toolbar">
      <div class="field">
        <label for="${sectionId}-search">Recherche</label>
        <input id="${sectionId}-search" type="search" placeholder="Ref., marque, modele" data-model-search>
      </div>
      <div class="field">
        <label for="${sectionId}-sort">Tri</label>
        <select id="${sectionId}-sort" data-model-sort>
          <option value="default">Ordre recommande</option>
          <option value="fcc-desc">FCC reel decroissant</option>
          <option value="comfort">Confort</option>
          <option value="efficiency">Efficacite</option>
          <option value="acoustics">Acoustique</option>
          <option value="diameter-desc">Diametre decroissant</option>
          <option value="diameter-asc">Diametre croissant</option>
        </select>
      </div>
      <p class="models-toolbar-summary" data-models-summary>${renderModelsTableSummary(models.length, models.length)}</p>
    </div>
  `;
}

function renderModelsTablePanel(sectionId, models) {
  return `
    <div class="models-table-panel" data-model-section="${sectionId}">
      ${renderModelsToolbar(sectionId, models)}
      <div data-models-table-host>${renderModelsTable(sortCompatibleModels(models))}</div>
    </div>
  `;
}

function updateModelsTableSection(sectionElement) {
  if (!sectionElement) {
    return;
  }

  const sectionId = sectionElement.dataset.modelSection;
  const models = modelSectionRegistry.get(sectionId) || [];
  const search = sectionElement.querySelector("[data-model-search]")?.value || "";
  const sort = sectionElement.querySelector("[data-model-sort]")?.value || "default";

  const filteredModels = filterCompatibleModels(models, {
    search
  });
  const sortedModels = sortCompatibleModels(filteredModels, sort);
  const host = sectionElement.querySelector("[data-models-table-host]");
  const summary = sectionElement.querySelector("[data-models-summary]");

  if (host) {
    host.innerHTML = renderModelsTable(sortedModels);
  }
  if (summary) {
    summary.textContent = renderModelsTableSummary(filteredModels.length, models.length);
  }
}

function bindResultsInteractions(dom) {
  if (dom.resultsList.dataset.resultsInteractionsBound === "true") {
    return;
  }

  const refreshFromEvent = (event) => {
    const target = event.target;
    if (
      !target ||
      !target.matches?.(
        "[data-model-search], [data-model-sort]"
      )
    ) {
      return;
    }

    updateModelsTableSection(target.closest("[data-model-section]"));
  };

  dom.resultsList.addEventListener("input", refreshFromEvent);
  dom.resultsList.addEventListener("change", refreshFromEvent);
  dom.resultsList.dataset.resultsInteractionsBound = "true";
}

function resetResultsModelSections() {
  modelSectionRegistry.clear();
  nextModelSectionId = 1;
}

function renderBrasse2Section(candidate, brasse2Models, _realDiameters) {
  const models = getBrasse2ModelsForCandidate(candidate, brasse2Models);
  const filteredModels = models.filter((model) => model?.sizing?.sizeFits);

  if (filteredModels.length === 0) {
    return `
      <section class="models-shell">
        <div>
          <h4 class="section-title">Catalogue BRASSE II</h4>
          <p class="section-subtitle" style="margin-bottom:0;">
            Aucun modele du catalogue n'entre dans le diametre theorique recommande pour cette option.
          </p>
        </div>
      </section>
    `;
  }

  const sectionId = registerModelSection(filteredModels);

  return `
    <section class="models-shell">
      <div>
        <h4 class="section-title">Choisir un modele (catalogue BRASSE II)</h4>
        <p class="section-subtitle" style="margin-bottom:0;">
          Liste de modeles a comparer. Le dimensionnement ci-dessus est theorique ; choisissez ensuite un modele adapte.
        </p>
      </div>

      <details class="models-details">
        <summary>Voir les modeles du catalogue dans le diametre theorique (${filteredModels.length})</summary>
        ${renderModelsTablePanel(sectionId, filteredModels)}
      </details>
    </section>
  `;
}

function candidateCard(candidate, rank, brasse2Models, realDiameters, selectedOptionKeys) {
  const warnings = getCandidateWarnings(candidate);
  const isSelectedForExport = selectedOptionKeys.has(candidate.key);

  const title = candidate.isCustom ? "Configuration personnalisée" : `Option ${rank}`;
  let badge = "";
  let customAlertsHtml = "";

  if (candidate.isCustom) {
    const c = candidate.conformity;
    if (c.conforming) {
      badge = `<span class="badge success">Conforme</span>`;
      customAlertsHtml = `
        <div class="notice success" style="margin-bottom: 12px;">
          <strong>Calepinage conforme</strong>
          Cette configuration respecte toutes les regles de securite et de distance reglementaires.
        </div>
      `;
      if (!c.heightRangeOk) {
        const range = candidate.diameter < 2.13 ? "inférieure à 2 D" : "comprise entre 0,8 D et 2 D";
        customAlertsHtml += `
          <div class="notice warning" style="margin-bottom: 12px;">
            <strong>Hauteur de fonctionnement non optimale</strong> : la hauteur sous pales (${formatMeters(candidate.bladeHeight)}) doit être ${range} (${formatMeters(2 * candidate.diameter)} pour ce diametre) pour assurer un bon confort. Envisagez d'ajuster la longueur de la suspension (tige).
          </div>
        `;
      }
    } else {
      badge = `<span class="badge danger">Non conforme</span>`;
      const alerts = [];
      if (!c.wallClearanceOk) {
        alerts.push(`<strong>Distance aux murs insuffisante</strong> : le diametre (${formatMeters(candidate.diameter)}) depasse la distance entre le centre et le mur le plus proche (${formatMeters(candidate.wallClearance)}).`);
      }
      if (!c.spacingOk) {
        alerts.push(`<strong>Entraxe insuffisant</strong> : l'entraxe entre ventilateurs (${formatMeters(candidate.interFanSpacing)}) est inferieur a la limite reglementaire de 2.5 D (${formatMeters(2.5 * candidate.diameter)}).`);
      }
      if (!c.coverageOk) {
        alerts.push(`<strong>Facteur de couverture (FCC) non optimal</strong> : le FCC calcule (${formatFactor(candidate.coverageFactor)}) est en dehors de la plage reglementaire de [0,20 - 0,40].`);
      }
      if (!c.safetyHeightOk) {
        const limit = candidate.diameter < 2.13 ? "2,13 m" : "3,05 m";
        alerts.push(`<strong>Hauteur de securite insuffisante</strong> : la hauteur sous pales (${formatMeters(candidate.bladeHeight)}) est inferieure au seuil de securite obligatoire pour cette classe d'appareil (${limit}).`);
      }
      customAlertsHtml = `
        <div class="notice danger" style="margin-bottom: 12px;">
          <strong>Detail des non-conformites :</strong>
          <ul style="margin: 8px 0 0; padding-left: 18px;">
            ${alerts.map((a) => `<li style="margin-bottom: 4px;">${a}</li>`).join("")}
          </ul>
        </div>
      `;
      if (!c.heightRangeOk) {
        const range = candidate.diameter < 2.13 ? "inférieure à 2 D" : "comprise entre 0,8 D et 2 D";
        customAlertsHtml += `
          <div class="notice warning" style="margin-bottom: 12px;">
            <strong>Hauteur de fonctionnement non optimale</strong> : la hauteur sous pales (${formatMeters(candidate.bladeHeight)}) doit être ${range} (${formatMeters(2 * candidate.diameter)} pour ce diametre) pour assurer un bon confort. Envisagez d'ajuster la longueur de la suspension (tige).
          </div>
        `;
      }
    }
  } else if (candidate.isMarketAlternative) {
    badge = "";
    customAlertsHtml = `
      <div class="notice success" style="margin-bottom: 12px;">
        Le diametre choisi est compris dans les diametres courants.
      </div>
    `;
  }

  return `
    <article class="result-card">
      <div class="result-head">
        <div>
          <h3 class="result-title">${title}${badge}</h3>
          <p class="result-subtitle">
            ${candidate.fanCount} brasseur${candidate.fanCount > 1 ? "s" : ""} centre${candidate.fanCount > 1 ? "s" : ""}
            dans des cellules de ${formatMeters(candidate.cellLength)} × ${formatMeters(candidate.cellWidth)},
            avec un diamètre de ${formatMeters(candidate.diameter)}.
          </p>
        </div>
        ${renderExportOptionToggle(candidate.key, isSelectedForExport)}
      </div>

      ${customAlertsHtml}

      <div class="result-grid">
        <div class="plan-wrap" style="${planWrapStyle(candidate)}">${svgForCandidate(candidate)}</div>

        <div class="stack">
          <div class="metric-grid">
            <div class="metric-card">
              <strong>Diametre theorique recommande</strong>
              <span>${formatMeters(candidate.diameter)}</span>
            </div>
            <div class="metric-card">
              <strong>Facteur de forme</strong>
              <span>${formatFactor(candidate.formFactor)}</span>
            </div>
            <div class="metric-card">
              <strong>FCC reel</strong>
              <span>${formatFactor(candidate.coverageFactor)}</span>
            </div>
          </div>

          <div class="detail-list">
            <div class="detail-item">
              <strong>Montage</strong>
              <span>${candidate.mountMode.label}</span>
            </div>
            <div class="detail-item">
              <strong>Hauteur sous pales</strong>
              <span class="detail-value-stack">
                <span>${formatMeters(candidate.bladeHeight)}</span>
                <span class="detail-subtext">Plafond-pales: ${formatMeters(candidate.mountDistance)}</span>
              </span>
            </div>
            <div class="detail-item">
              <strong>Murs</strong>
              <span>Centre vers mur le plus proche: ${formatMeters(candidate.wallClearance)} &gt; ${formatMeters(candidate.diameter)}</span>
            </div>
            <div class="detail-item">
              <strong>Entre brasseurs</strong>
              <span>${candidate.interFanSpacing ? `${formatMeters(candidate.interFanSpacing)} &gt; ${formatNumber(2.5, 1)} × D` : "Non applicable (un seul brasseur)"}</span>
            </div>
            <div class="detail-item detail-item-stack">
              <strong>Diametres BRASSE II admissibles (info)</strong>
              <span>${
                candidate.compatibleRealDiameters?.length
                  ? candidate.compatibleRealDiameters.map((option) => formatDiameterCm(option.diameter)).join(", ")
                  : "Aucun diametre de la liste de reference"
              }</span>
            </div>
          </div>
        </div>
      </div>

      ${warnings.length > 0 ? `
        <div class="notice warning">
          <strong>Point d'attention.</strong>
          ${warnings.join(" ")}
        </div>
      ` : ""}

      ${renderBrasse2Section(candidate, brasse2Models, realDiameters)}

    </article>
  `;
}

function renderSummary(dom, room, candidates, brasse2Models) {
  const roomArea = room.length * room.width;
  const displayedCandidates = candidates.slice(0, 5);
  const compatibleModels = getDistinctCompatibleModels(displayedCandidates, brasse2Models);
  const diameterSummary = getMaxDiameterSummary(displayedCandidates);
  const mountSummary = getNeutralMountSummary(displayedCandidates);

  dom.summaryGrid.innerHTML = [
    createSummaryCard(
      "Piece",
      `${formatMeters(room.length)} × ${formatMeters(room.width)}`,
      `${formatSquareMeters(roomArea)} - HSP ${formatMeters(room.height)}`
    ),
    createSummaryCard(
      "Options valides",
      `${candidates.length}`,
      `Montages visibles : ${mountSummary}`
    ),
    createSummaryCard(
      "Diametre max",
      diameterSummary.value,
      diameterSummary.detail
    ),
    createSummaryCard(
      "Catalogue BRASSE II",
      compatibleModels.length > 0 ? `${compatibleModels.length} modeles compatibles` : "Aucun modele compatible",
      "Compatibilite calculee sur les options affichees"
    )
  ].join("");
  dom.highlights.innerHTML = "";
}

function renderStatusNote(
  dom,
  room,
  candidates,
  fallbackFlush,
  modes,
  realDiameters
) {
  const notes = [];
  const heightRequirementMessage = buildHeightDiameterRequirementMessage(room, modes, realDiameters);

  if (candidates.length > 0) {
    const best = candidates[0];
    if (best.mountMode.id === "low-profile") {
      notes.push(`
        <div class="notice warning">
          <strong>Le meilleur cas passe en low-profile.</strong>
          Le guide indique alors une baisse de vitesse d'air d'environ 15 %. Une variante standard est egalement
          affichee plus bas si elle existe.
        </div>
      `);
    }

    if (candidates.every((c) => c.compatibleRealDiameters.length === 0)) {
      notes.push(`
        <div class="notice warning">
          <strong>Aucun modèle compatible disponible dans le catalogue.</strong>
          Les configurations théoriques proposées nécessitent des diamètres supérieurs à la limite du catalogue BRASSE II embarqué (${formatMeters(realDiameters[realDiameters.length - 1])}).
          ${heightRequirementMessage}
        </div>
      `);
    }
  }

  if (fallbackFlush && candidates.length === 0) {
    notes.push(`
      <div class="notice danger">
        <strong>Aucune solution standard ou low-profile n'a ete trouvee.</strong>
        Un montage flush pourrait rouvrir une piste jusqu'a ${formatMeters(fallbackFlush.diameter)}, mais le guide
        annonce alors une perte de performance superieure a 40 % et demande de l'eviter au maximum.
        ${heightRequirementMessage}
      </div>
    `);
  }

  if (!fallbackFlush && candidates.length === 0) {
    notes.push(`
      <div class="notice danger">
        <strong>Aucun calepinage théorique standard ou low-profile n'est possible dans ce local.</strong>
        Le local ne permet pas d'implanter de brasseur d'air respectant les règles BRASSE de FCC, de distances et de hauteur.
        ${heightRequirementMessage}
      </div>
    `);
  }

  dom.statusNote.innerHTML = notes.join("");
}

function renderResults(dom, candidates, brasse2Models, realDiameters, selectedOptionKeys = []) {
  resetResultsModelSections();
  const selectedOptionKeySet = new Set(selectedOptionKeys);
  dom.resultsList.innerHTML = candidates
    .slice(0, 5)
    .map((candidate, index) =>
      candidateCard(candidate, index + 1, brasse2Models, realDiameters, selectedOptionKeySet)
    )
    .join("");
}

// src/report/pdf.js
function getAllReportOptions(state) {
  if (!state) {
    return [];
  }

  if (state.kind === "uniformity-ok") {
    return state.candidates || [];
  }

  return [];
}

function getReportOptionNumber(state, option) {
  const index = getAllReportOptions(state).findIndex((item) => item.key === option.key);
  return index >= 0 ? index + 1 : null;
}

function getMaxDiameterSummary(options) {
  const diameters = options.map((option) => option.diameter).filter(Number.isFinite);

  if (diameters.length === 0) {
    return {
      value: "Aucun",
      detail: "Aucun diametre reel compatible"
    };
  }

  const maxDiameter = Math.max(...diameters);
  return {
    value: `${formatDiameterCm(maxDiameter)} cm`,
    detail: "Plus grand diametre reel des options exportees"
  };
}

function renderReportWarningList(items) {
  if (!items || items.length === 0) {
    return "";
  }

  return `
    <div class="report-note report-note-warning">
      <strong>Points d'attention</strong>
      <ul>
        ${items.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
      </ul>
    </div>
  `;
}

function renderReportSummaryCards(items) {
  return `
    <div class="report-summary-grid">
      ${items
        .map(
          ([label, value, detail]) => `
            <article class="report-summary-card">
              <strong>${escapeHtml(label)}</strong>
              <span>${escapeHtml(value)}</span>
              <small>${escapeHtml(detail)}</small>
            </article>
          `
        )
        .join("")}
    </div>
  `;
}

function renderReportMetricGrid(items) {
  return `
    <div class="report-metrics">
      ${items
        .map(
          ([label, value]) => `
            <article class="report-metric">
              <strong>${escapeHtml(label)}</strong>
              <span>${escapeHtml(value)}</span>
            </article>
          `
        )
        .join("")}
    </div>
  `;
}

function renderReportTable(headers, rows, compact = false) {
  return `
    <table class="report-table${compact ? " compact" : ""}">
      <thead>
        <tr>
          ${headers.map((header) => `<th>${escapeHtml(header)}</th>`).join("")}
        </tr>
      </thead>
      <tbody>
        ${rows
          .map(
            (row) => `
              <tr>
                ${row.map((cell) => `<td>${escapeHtml(cell)}</td>`).join("")}
              </tr>
            `
          )
          .join("")}
      </tbody>
    </table>
  `;
}

function renderStudySummarySection(state, selectedOptions) {
  const roomArea = state.room.length * state.room.width;
  const maxDiameterSummary = getMaxDiameterSummary(selectedOptions);
  const baseCards = [
    [
      "Piece",
      `${formatMeters(state.room.length)} × ${formatMeters(state.room.width)}`,
      `${formatSquareMeters(roomArea)} - HSP ${formatMeters(state.room.height)}`
    ],
    [
      "Diametre max",
      maxDiameterSummary.value,
      maxDiameterSummary.detail
    ]
  ];

  return `
    <section class="report-block">
      <h2>Synthese de la piece etudiee</h2>
      ${renderReportSummaryCards(baseCards)}
    </section>
  `;
}

function renderSelectedOptionsOverview(state, selectedOptions) {
  if (selectedOptions.length === 0) {
    return `
      <section class="report-block">
        <h2>Synthese de ou des option(s) retenue(s) :</h2>
        <div class="report-note report-note-warning">
          <strong>Aucune option selectionnee</strong>
          <p>Cochez au moins une option dans l'outil pour l'inclure dans le rapport PDF.</p>
        </div>
      </section>
    `;
  }

  if (state.kind === "uniformity-ok") {
    const rows = selectedOptions.map((option) => [
      option.isCustom ? "Configuration personnalisee" : `Option ${getReportOptionNumber(state, option)}`,
      `${option.nx} × ${option.ny}`,
      formatMeters(option.diameter),
      option.mountMode.label,
      formatFactor(option.coverageFactor)
    ]);

    return `
      <section class="report-block">
        <h2>Synthese de ou des option(s) retenue(s) :</h2>
        ${renderReportTable(
          ["Option", "Trame", "Diametre theorique", "Montage", "FCC calc."],
          rows,
          true
        )}
      </section>
    `;
  }
}

function renderUniformityOptionPage(state, option) {
  const optionNumber = getReportOptionNumber(state, option);
  const kicker = option.isCustom ? "Configuration personnalisee" : `Option ${optionNumber}`;
  let sub = `${option.fanCount} brasseur${option.fanCount > 1 ? "s" : ""} • ${option.mountMode.label}`;
  if (option.isCustom) {
    sub = `${option.fanCount} brasseur${option.fanCount > 1 ? "s" : ""} • ${option.mountMode.label} • ${option.conformity.conforming ? "CONFORME" : "NON CONFORME"}`;
  } else if (option.isMarketAlternative) {
    sub = `${option.fanCount} brasseur${option.fanCount > 1 ? "s" : ""} • ${option.mountMode.label}`;
  }

  const customAlerts = [];
  if (option.isCustom) {
    const c = option.conformity;
    if (!c.wallClearanceOk) {
      customAlerts.push(`Distance aux murs insuffisante (diametre ${formatMeters(option.diameter)} > mur ${formatMeters(option.wallClearance)})`);
    }
    if (!c.spacingOk) {
      customAlerts.push(`Entraxe insuffisant (${formatMeters(option.interFanSpacing)} < 2.5 D)`);
    }
    if (!c.coverageOk) {
      customAlerts.push(`FCC hors limites [0.2 - 0.4] (${formatFactor(option.coverageFactor)})`);
    }
    if (!c.safetyHeightOk) {
      customAlerts.push(`Hauteur sous pales hors limites de securite (${formatMeters(option.bladeHeight)} < ${option.diameter < 2.13 ? "2.13m" : "3.05m"})`);
    }
    if (!c.heightRangeOk) {
      customAlerts.push(`Hauteur de fonctionnement non optimale (${formatMeters(option.bladeHeight)})`);
    }
  } else if (option.isMarketAlternative) {
    customAlerts.push("Le diametre choisi est compris dans les diametres courants.");
  }

  const allWarnings = [...customAlerts, ...getCandidateWarnings(option)];

  return `
    <section class="report-page report-option-page">
      <div class="report-section-head">
        <p class="report-section-kicker">${kicker}</p>
        <h2>${escapeHtml(`${option.nx} × ${option.ny} cellules`)}</h2>
        <p>${escapeHtml(sub)}</p>
      </div>

      ${renderReportMetricGrid([
        ["Diametre du brasseur", formatMeters(option.diameter)],
        ["Facteur de forme", formatFactor(option.formFactor)],
        ["FCC calc.", formatFactor(option.coverageFactor)],
        ["Hauteur sous pales", formatMeters(option.bladeHeight)],
        ["Plafond-pales", formatMeters(option.mountDistance)]
      ])}

      <div class="report-plan-block">
        <div class="report-plan">${svgForCandidate(option)}</div>
        <div class="report-side">
          ${renderReportTable(
            ["Lecture", "Valeur"],
            [
              ["Montage", option.mountMode.label],
              ["Cellule", `${formatMeters(option.cellLength)} × ${formatMeters(option.cellWidth)}`],
              ["Mur limitant", `${formatMeters(option.wallClearance)} > ${formatMeters(option.diameter)}`],
              [
                "Entraxe mini",
                option.interFanSpacing
                  ? `${formatMeters(option.interFanSpacing)} > 2,5 × D`
                  : "Non applicable"
              ]
            ],
            true
          )}
          ${renderReportWarningList(allWarnings)}
        </div>
      </div>
    </section>
  `;
}

function renderReportFirstPage(state, selectedOptions) {
  return `
    <section class="report-page report-first-page">
      <header class="report-cover">
        <p class="report-cover-kicker">Rapport de simulation • le ${escapeHtml(
          formatDateTime(state.generatedAt)
        )}</p>
        <h1>${escapeHtml(state.simulationName)}</h1>
        <p class="report-cover-lead">
          Synthese de la piece etudiee et des options selectionnees pour l'export PDF.
        </p>
      </header>

      ${renderStudySummarySection(state, selectedOptions)}
      ${renderSelectedOptionsOverview(state, selectedOptions)}
    </section>
  `;
}

function renderEmptyOrInvalidReport(state) {
  const roomBlock =
    state.room && Number.isFinite(state.room.length)
      ? renderReportSummaryCards([
          [
            "Piece",
            `${formatMeters(state.room.length)} × ${formatMeters(state.room.width)}`,
            `${formatSquareMeters(state.room.length * state.room.width)} - HSP ${formatMeters(
              state.room.height
            )}`
          ]
        ])
      : "";

  let title = "Calcul impossible";
  let text =
    "Les donnees saisies ne permettent pas de produire un rapport de calepinage exploitable.";

  if (state.kind === "uniformity-empty") {
    title = "Aucune solution compatible";
    text =
      "Aucun cas standard ou low-profile n'a pu etre valide avec les regles BRASSE de FCC, de distances et de hauteur.";
  }

  return `
    <section class="report-page report-first-page">
      <header class="report-cover">
        <p class="report-cover-kicker">Rapport de simulation • le ${escapeHtml(
          formatDateTime(state.generatedAt)
        )}</p>
        <h1>${escapeHtml(state.simulationName)}</h1>
      </header>
      ${roomBlock}
      <section class="report-block">
        <h2>Conclusion</h2>
        <div class="report-note report-note-warning">
          <strong>${escapeHtml(title)}</strong>
          <p>${escapeHtml(text)}</p>
        </div>
        ${
          state.issues?.length
            ? `
              <div class="report-note" style="margin-top: 10px;">
                <strong>Points releves</strong>
                <ul>
                  ${state.issues.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
                </ul>
              </div>
            `
            : ""
        }
      </section>
    </section>
  `;
}

function buildPdfReportStyles() {
  return `
    @page { size: A4; margin: 12mm; }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "Segoe UI", Arial, sans-serif;
      color: #17212b;
      background: #ffffff;
      line-height: 1.45;
    }
    .report-root {
      max-width: 186mm;
      margin: 0 auto;
    }
    .report-page + .report-page {
      break-before: page;
      page-break-before: always;
    }
    .report-cover {
      display: grid;
      gap: 8px;
      padding-bottom: 12px;
      border-bottom: 1px solid #d6d9df;
      margin-bottom: 16px;
    }
    .report-cover-kicker,
    .report-section-kicker {
      margin: 0;
      color: #6b7280;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }
    .report-cover h1,
    .report-section-head h2 {
      margin: 0;
      font-size: 22px;
      line-height: 1.15;
    }
    .report-cover-lead {
      margin: 0;
      color: #4b5563;
      font-size: 12px;
    }
    .report-section-head p {
      margin: 4px 0 0;
      color: #4b5563;
      font-size: 12px;
    }
    .report-summary-grid,
    .report-metrics {
      display: grid;
      gap: 10px;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .report-summary-card,
    .report-metric {
      border: 1px solid #d6d9df;
      border-radius: 10px;
      padding: 10px 12px;
      background: #f8fafc;
    }
    .report-summary-card strong,
    .report-metric strong {
      display: block;
      margin-bottom: 4px;
      color: #6b7280;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }
    .report-summary-card span,
    .report-metric span {
      display: block;
      font-size: 13px;
      font-weight: 700;
      color: #111827;
    }
    .report-summary-card small {
      display: block;
      margin-top: 4px;
      color: #4b5563;
      font-size: 11px;
      line-height: 1.4;
    }
    .report-block,
    .report-section,
    .report-note,
    .report-plan-block {
      break-inside: avoid;
      page-break-inside: avoid;
    }
    .report-block + .report-block,
    .report-section + .report-section {
      margin-top: 16px;
    }
    .report-block h2,
    .report-section h3 {
      margin: 0 0 10px;
      font-size: 15px;
      line-height: 1.25;
    }
    .report-section-head {
      display: grid;
      gap: 2px;
      margin-bottom: 12px;
      padding-bottom: 12px;
      border-bottom: 1px solid #d6d9df;
    }
    .report-plan-block {
      display: grid;
      gap: 14px;
      grid-template-columns: minmax(0, 1.25fr) minmax(0, 0.95fr);
      align-items: start;
      margin-top: 12px;
    }
    .report-plan {
      border: 1px solid #d6d9df;
      border-radius: 10px;
      padding: 10px;
      background: #f8fafc;
    }
    .report-plan svg {
      width: 100%;
      height: auto;
      display: block;
    }
    .report-side {
      display: grid;
      gap: 10px;
    }
    .report-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 11px;
    }
    .report-table th,
    .report-table td {
      padding: 8px 9px;
      border: 1px solid #d6d9df;
      vertical-align: top;
      text-align: left;
    }
    .report-table th {
      background: #f8fafc;
      color: #374151;
      font-weight: 700;
    }
    .report-table.compact th,
    .report-table.compact td {
      padding: 7px 8px;
      font-size: 10.5px;
    }
    .report-note {
      border: 1px solid #d6d9df;
      border-radius: 10px;
      padding: 10px 12px;
      background: #f8fafc;
    }
    .report-note-warning {
      background: #fff6e5;
      border-color: rgba(184, 109, 33, 0.24);
    }
    .report-note strong {
      display: block;
      margin-bottom: 6px;
      font-size: 11px;
    }
    .report-note p,
    .report-note ul {
      margin: 0;
      color: #4b5563;
      font-size: 11px;
    }
    .report-note ul {
      padding-left: 16px;
    }
    .report-footer {
      margin-top: 18px;
      padding-top: 10px;
      border-top: 1px solid #d6d9df;
      color: #4b5563;
      font-size: 10.5px;
    }
    .report-footer a {
      color: #1f4f82;
      text-decoration: none;
    }
    @media print {
      .report-root {
        max-width: none;
      }
    }
  `;
}

function buildPdfFilename(simulationName) {
  const base = simulationName
    .normalize("NFD")
    .replace(/[\u0300-\u036f]/g, "")
    .replace(/[^a-zA-Z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "")
    .toLowerCase();
  const stamp = new Intl.DateTimeFormat("sv-SE", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  }).format(new Date());
  return `${base || "simulation"}-${stamp}`;
}

function buildPdfReportDocument(state, _brasse2Models) {
  if (!state) {
    return "";
  }

  const selectedOptions = getSelectedReportOptions(state);
  let bodyContent = "";

  if (state.kind === "uniformity-ok") {
    bodyContent = `
      ${renderReportFirstPage(state, selectedOptions)}
      ${selectedOptions
        .map((option) => renderUniformityOptionPage(state, option))
        .join("")}
    `;
  } else {
    bodyContent = renderEmptyOrInvalidReport(state);
  }

  return `
    <!DOCTYPE html>
    <html lang="fr">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>${escapeHtml(buildPdfFilename(state.simulationName))}</title>
      <style>${buildPdfReportStyles()}</style>
    </head>
    <body>
      <main class="report-root">
        ${bodyContent}
        <footer class="report-footer">
          Fait par Nathan Château, <a href="mailto:chateaunathan@proton.me">chateaunathan@proton.me</a>
        </footer>
      </main>
    </body>
    </html>
  `;
}

function exportPdfReport(latestReportState, brasse2Models) {
  if (!latestReportState) {
    return;
  }

  const reportHtml = buildPdfReportDocument(latestReportState, brasse2Models);
  if (typeof window.open === "function") {
    const popup = window.open("", "_blank");
    if (popup && popup.document) {
      const triggerPrint = () => {
        try {
          popup.focus();
          popup.print();
        } catch (error) {
          console.error("Impossible de lancer l'impression du rapport.", error);
        }
      };

      popup.onload = () => {
        window.setTimeout(triggerPrint, 150);
      };
      popup.document.open();
      popup.document.write(reportHtml);
      popup.document.close();
      window.setTimeout(triggerPrint, 600);
      return;
    }
  }

  if (typeof window.print === "function") {
    window.print();
  }
}

// src/main.js
const dom = getDomRefs();
const state = createAppState();
const brasse2Models = BRASSE2_MODELS;
const realDiameters = [
  ...new Set(
    brasse2Models
      .map((model) => Number(model.diameterCm) / 100)
      .filter((value) => Number.isFinite(value) && value > 0)
  )
].sort((a, b) => a - b);

function getSimulationName() {
  const value = String(dom.simulationNameInput.value || "").trim();
  return value || "Simulation sans nom";
}

function getSelectedModes() {
  const modes = [];
  if (dom.allowStandardInput.checked) {
    modes.push(MOUNT_MODES[0]);
  }
  if (dom.allowLowInput.checked) {
    modes.push(MOUNT_MODES[1]);
  }
  return modes;
}

function getSelectedModesLabel(modes = getSelectedModes()) {
  const labels = modes.map((mode) => mode.label);
  return labels.length > 0 ? labels.join(" + ") : "Aucun";
}

function getRoomInputs() {
  return {
    length: parseNumber(dom.lengthInput),
    width: parseNumber(dom.widthInput),
    height: parseNumber(dom.heightInput)
  };
}

function updateHeader({ room, recommendation = "", generatedAt = new Date() }) {
  refreshReportHeader(dom, {
    simulationName: getSimulationName(),
    modesLabel: getSelectedModesLabel(),
    recommendation,
    generatedAt,
    room
  });
}

function validateInputs(values) {
  const issues = [];
  if (!(values.length > 0)) {
    issues.push("La longueur doit etre strictement positive.");
  }
  if (!(values.width > 0)) {
    issues.push("La largeur doit etre strictement positive.");
  }
  if (!(values.height > 0)) {
    issues.push("La hauteur sous plafond doit etre strictement positive.");
  }

  if (state.mode === "auto") {
    if (!dom.allowStandardInput.checked && !dom.allowLowInput.checked) {
      issues.push("Selectionnez au moins un type de montage.");
    }
  } else {
    const diameter = parseNumber(dom.manualDiameterInput);
    if (!(diameter > 0)) {
      issues.push("Le diametre du brasseur doit etre strictement positif.");
    }
  }
  return issues;
}

function updateExportControls() {
  const latestReportState = state.latestReportState;
  if (!latestReportState) {
    setExportAvailability(dom, false);
    setExportSelectionSummary(dom, "");
    return;
  }

  const selectedOptions = getSelectedReportOptions(latestReportState);
  if (selectedOptions.length === 0) {
    setExportAvailability(dom, latestReportState.kind !== "uniformity-ok");
    setExportSelectionSummary(
      dom,
      latestReportState.kind === "uniformity-ok" ? "Aucune option selectionnee pour le PDF" : ""
    );
    return;
  }

  const selectableCount =
    latestReportState.kind === "uniformity-ok" ? latestReportState.candidates.length : 0;

  setExportAvailability(dom, true);
  setExportSelectionSummary(
    dom,
    selectableCount > 0
      ? `${selectedOptions.length} option${selectedOptions.length > 1 ? "s" : ""} selectionnee${selectedOptions.length > 1 ? "s" : ""} sur ${selectableCount}`
      : ""
  );
}

function resetResultsVisibility() {
  dom.emptyState.classList.add("hidden");
  dom.resultsContent.classList.add("hidden");
  setExportAvailability(dom, false);
  setExportSelectionSummary(dom, "");
  setLatestReportState(state, null);
  resetResultsModelSections();
}

function renderInvalidState(rawValues, issues, generatedAt) {
  updateHeader({
    room: rawValues,
    recommendation: "Calcul impossible",
    generatedAt
  });
  setLatestReportState(state, {
    kind: "invalid",
    simulationName: getSimulationName(),
    room: rawValues,
    modesLabel: getSelectedModesLabel(),
    generatedAt,
    context: "Calcul impossible",
    issues
  });
  dom.statusNote.innerHTML = `
    <div class="notice danger">
      <strong>Calcul impossible.</strong><br>
      ${issues.join(" ")}
    </div>
  `;
  dom.summaryGrid.innerHTML = "";
  dom.highlights.innerHTML = "";
  dom.resultsList.innerHTML = "";
  dom.resultsContent.classList.remove("hidden");
}

function renderUniformityEmpty(room, fallbackFlush, generatedAt) {
  updateHeader({
    room,
    recommendation: "Aucune solution compatible",
    generatedAt
  });
  const modes = getSelectedModes();
  const heightRequirementMessage = buildHeightDiameterRequirementMessage(room, modes, realDiameters);
  setLatestReportState(state, {
    kind: "uniformity-empty",
    simulationName: getSimulationName(),
    room,
    modesLabel: getSelectedModesLabel(modes),
    generatedAt,
    context: "Aucune solution compatible",
    fallbackFlush,
    issues: [
      "Aucun cas standard ou low-profile n'a respecte les regles BRASSE de FCC, de distances et de hauteur.",
      ...(heightRequirementMessage ? [heightRequirementMessage] : [])
    ]
  });
  dom.summaryGrid.innerHTML = [
    createSummaryCard("Local", `${formatMeters(room.length)} × ${formatMeters(room.width)}`, `HSP ${formatMeters(room.height)}`),
    createSummaryCard("Resultat", "Aucune solution", "Aucun cas standard ou low-profile compatible"),
    createSummaryCard("Base BRASSE II", `${brasse2Models.length} modeles integres`, "Sans effet ici tant que la geometrie bloque")
  ].join("");
  dom.highlights.innerHTML = "";
  renderStatusNote(dom, room, [], fallbackFlush, modes, realDiameters);
  dom.resultsList.innerHTML = "";
  setExportAvailability(dom, true);
  dom.resultsContent.classList.remove("hidden");
}

function render() {
  const rawValues = getRoomInputs();
  const issues = validateInputs(rawValues);
  const generatedAt = new Date();

  resetResultsVisibility();

  if (issues.length > 0) {
    renderInvalidState(rawValues, issues, generatedAt);
    return;
  }

  const room = {
    length: rawValues.length,
    width: rawValues.width,
    height: rawValues.height
  };

  if (state.mode === "manual") {
    const diameter = parseNumber(dom.manualDiameterInput);
    const fanCount = Number(dom.fanCountSelect.value);
    const mountModeId = dom.manualMountSelect.value;
    let mountMode = MOUNT_MODES.find((m) => m.id === mountModeId);
    if (!mountMode && mountModeId === "flush") {
      mountMode = FLUSH_MODE;
    }

    const candidate = evaluateCustomCandidate(room, fanCount, diameter, mountMode, realDiameters);
    const recommendation = `Configuration personnalisee : ${candidate.nx} × ${candidate.ny} - ${candidate.fanCount} brasseur${candidate.fanCount > 1 ? "s" : ""} (${candidate.conformity.conforming ? "conforme" : "non conforme"})`;

    updateHeader({
      room,
      recommendation,
      generatedAt
    });

    setLatestReportState(state, {
      kind: "uniformity-ok",
      simulationName: getSimulationName(),
      room,
      candidates: [candidate],
      selectedOptionKeys: createSelectedReportOptionKeys([candidate]),
      modesLabel: candidate.mountMode.label,
      generatedAt,
      context: `Configuration personnalisee • trame : ${candidate.nx} × ${candidate.ny}`
    });

    renderSummary(dom, room, [candidate], brasse2Models);
    dom.statusNote.innerHTML = "";
    renderResults(
      dom,
      [candidate],
      brasse2Models,
      realDiameters,
      state.latestReportState?.selectedOptionKeys || []
    );
    updateExportControls();
    dom.resultsContent.classList.remove("hidden");
    return;
  }

  const modes = getSelectedModes();
  const candidates = enumerateCandidates(room, MAX_GRID_FANS, modes, realDiameters);

  if (candidates.length > 0) {
    let bestMarket = findBestMarketAlternative(candidates);
    if (bestMarket) {
      bestMarket = convertCandidateToMarketDiameter(bestMarket);
      const top5 = candidates.slice(0, 5);
      const idx = top5.findIndex((c) => c.key === bestMarket.key);
      if (idx >= 0) {
        candidates[idx] = bestMarket;
      } else {
        if (candidates.length >= 5) {
          candidates[4] = bestMarket;
        } else {
          candidates.push(bestMarket);
        }
      }
    }
  }

  const fallbackFlush =
    candidates.length === 0 ? getFallbackFlushCandidate(room, MAX_GRID_FANS, realDiameters) : null;

  if (candidates.length === 0) {
    renderUniformityEmpty(room, fallbackFlush, generatedAt);
    return;
  }

  updateHeader({
    room,
    recommendation: `Option recommandee : ${candidates[0].nx} × ${candidates[0].ny} - ${candidates[0].fanCount} brasseur${candidates[0].fanCount > 1 ? "s" : ""}`,
    generatedAt
  });
  setLatestReportState(state, {
    kind: "uniformity-ok",
    simulationName: getSimulationName(),
    room,
    candidates: candidates.slice(0, 5),
    selectedOptionKeys: createSelectedReportOptionKeys(candidates.slice(0, 5)),
    modesLabel: getSelectedModesLabel(modes),
    generatedAt,
    context: `Recherche d'uniformite • option recommandee : ${candidates[0].nx} × ${candidates[0].ny}`
  });
  renderSummary(dom, room, candidates, brasse2Models);
  renderStatusNote(dom, room, candidates, fallbackFlush, modes, realDiameters);
  renderResults(
    dom,
    candidates,
    brasse2Models,
    realDiameters,
    state.latestReportState?.selectedOptionKeys || []
  );
  updateExportControls();
  dom.resultsContent.classList.remove("hidden");
}

dom.form.addEventListener("submit", (event) => {
  event.preventDefault();
  render();
});

function switchTab(mode) {
  if (state.mode === mode) {
    return;
  }
  state.mode = mode;

  if (mode === "auto") {
    dom.tabAuto.classList.add("active");
    dom.tabManual.classList.remove("active");
    dom.autoFields.classList.add("active");
    dom.manualFields.classList.remove("active");
  } else {
    dom.tabAuto.classList.remove("active");
    dom.tabManual.classList.add("active");
    dom.autoFields.classList.remove("active");
    dom.manualFields.classList.add("active");
  }
  render();
}

dom.tabAuto.addEventListener("click", () => switchTab("auto"));
dom.tabManual.addEventListener("click", () => switchTab("manual"));

dom.resetButton.addEventListener("click", () => {
  dom.lengthInput.value = "9";
  dom.widthInput.value = "5";
  dom.heightInput.value = "2.5";
  dom.simulationNameInput.value = "";
  dom.allowStandardInput.checked = true;
  dom.allowLowInput.checked = true;
  dom.fanCountSelect.value = "4";
  dom.manualDiameterInput.value = "1.32";
  dom.manualMountSelect.value = "standard";

  state.mode = "auto";
  dom.tabAuto.classList.add("active");
  dom.tabManual.classList.remove("active");
  dom.autoFields.classList.add("active");
  dom.manualFields.classList.remove("active");

  resetState(state);
  render();
});

dom.simulationNameInput.addEventListener("input", () => {
  updateHeader({
    room: getRoomInputs()
  });
});

dom.exportPdfButton.addEventListener("click", () => {
  exportPdfReport(state.latestReportState, brasse2Models);
});

dom.catalogSearchInput.addEventListener("input", () => {
  renderCatalog(dom, brasse2Models);
});

[
  dom.catalogBrandInput,
  dom.catalogDiameterInput,
  dom.catalogMotorInput,
  dom.catalogFixationInput,
  dom.catalogSortInput
].forEach((input) => {
  input.addEventListener("change", () => {
    renderCatalog(dom, brasse2Models);
  });
});

dom.catalogResetButton.addEventListener("click", () => {
  resetCatalogFilters(dom);
  renderCatalog(dom, brasse2Models);
});

dom.resultsList.addEventListener("change", (event) => {
  const target = event.target;
  if (!target?.matches?.("[data-export-option-key]")) {
    return;
  }

  toggleLatestReportOptionSelection(state, target.dataset.exportOptionKey, target.checked);
  updateExportControls();
});

[
  dom.lengthInput,
  dom.widthInput,
  dom.heightInput,
  dom.allowStandardInput,
  dom.allowLowInput,
  dom.fanCountSelect,
  dom.manualDiameterInput,
  dom.manualMountSelect
].forEach(
  (input) => {
    input.addEventListener("change", () => {
      render();
    });
  }
);

updateHeader({
  room: getRoomInputs()
});
setExportAvailability(dom, false);
setExportSelectionSummary(dom, "");
bindResultsInteractions(dom);
initializeCatalogFilters(dom, brasse2Models);
renderCatalog(dom, brasse2Models);
render();
})();
