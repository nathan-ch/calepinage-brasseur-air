/* Generated file. Run "npm run build:browser" after editing src/. */
(function () {
  "use strict";

// brasse2-data.js
const BRASSE2_MODELS = [{"id":"0101ACR132","brand":"GoodHome","model":"Linto","diameterCm":132,"ceilingDistanceCm":30.0,"testHeightCm":220.0,"ratioDh":0.227,"fixation":"Rotule/pivot","motor":"AC","rpmMax":199,"powerMaxW":55.0,"lwaMaxDbA":37.6,"vDirAssisMax":0.629,"vDirDeboutMax":0.597,"ceDirAssisMax":2.86,"ceDirDeboutMax":3.53,"ventMax":0.732,"ceEntrMax":3.09,"vAvgMax":0.733,"ceAvgMax":3.09,"cfeDirDeboutMax":0.0642,"cfeAvgMax":0.0562},{"id":"0201ACR132","brand":"Hunter","model":"Industrie II","diameterCm":132,"ceilingDistanceCm":34.0,"testHeightCm":216.0,"ratioDh":0.258,"fixation":"Rotule/pivot","motor":"AC","rpmMax":158,"powerMaxW":65.0,"lwaMaxDbA":42.1,"vDirAssisMax":1.225,"vDirDeboutMax":1.394,"ceDirAssisMax":3.8,"ceDirDeboutMax":5.02,"ventMax":0.742,"ceEntrMax":3.11,"vAvgMax":0.795,"ceAvgMax":3.21,"cfeDirDeboutMax":0.0772,"cfeAvgMax":0.0494},{"id":"0202ACR112","brand":"Hunter","model":"Low Profile III","diameterCm":112,"ceilingDistanceCm":21.0,"testHeightCm":229.0,"ratioDh":0.188,"fixation":"Rotule/pivot","motor":"AC","rpmMax":241,"powerMaxW":62.0,"lwaMaxDbA":52.6,"vDirAssisMax":0.797,"vDirDeboutMax":1.094,"ceDirAssisMax":3.21,"ceDirDeboutMax":4.63,"ventMax":0.559,"ceEntrMax":2.68,"vAvgMax":0.615,"ceAvgMax":2.825,"cfeDirDeboutMax":0.0747,"cfeAvgMax":0.0456},{"id":"0203ACR132","brand":"Hunter","model":"Low Profile III","diameterCm":132,"ceilingDistanceCm":21.0,"testHeightCm":229.0,"ratioDh":0.159,"fixation":"Rotule/pivot","motor":"AC","rpmMax":185,"powerMaxW":66.0,"lwaMaxDbA":49.9,"vDirAssisMax":0.304,"vDirDeboutMax":0.394,"ceDirAssisMax":1.64,"ceDirDeboutMax":2.66,"ventMax":0.589,"ceEntrMax":2.76,"vAvgMax":0.59,"ceAvgMax":2.765,"cfeDirDeboutMax":0.0403,"cfeAvgMax":0.0419},{"id":"0204ACF132","brand":"Hunter","model":"Industrie II","diameterCm":132,"ceilingDistanceCm":19.0,"testHeightCm":231.0,"ratioDh":0.144,"fixation":"Monobloc","motor":"AC","rpmMax":161,"powerMaxW":69.0,"lwaMaxDbA":45.7,"vDirAssisMax":0.568,"vDirDeboutMax":0.568,"ceDirAssisMax":2.7,"ceDirDeboutMax":3.43,"ventMax":0.414,"ceEntrMax":2.18,"vAvgMax":0.428,"ceAvgMax":2.23,"cfeDirDeboutMax":0.0497,"cfeAvgMax":0.0323},{"id":"0301ACR132","brand":"Create","model":"Wind Wood","diameterCm":132,"ceilingDistanceCm":35.0,"testHeightCm":215.0,"ratioDh":0.265,"fixation":"Rotule/pivot","motor":"AC","rpmMax":168,"powerMaxW":68.0,"lwaMaxDbA":39.5,"vDirAssisMax":1.169,"vDirDeboutMax":1.387,"ceDirAssisMax":3.74,"ceDirDeboutMax":5.02,"ventMax":0.771,"ceEntrMax":3.17,"vAvgMax":0.828,"ceAvgMax":3.275,"cfeDirDeboutMax":0.0738,"cfeAvgMax":0.0482},{"id":"0302DCR152","brand":"Create","model":"Wind Large","diameterCm":152,"ceilingDistanceCm":47.0,"testHeightCm":203.0,"ratioDh":0.309,"fixation":"Rotule/pivot","motor":"DC","rpmMax":168,"powerMaxW":37.0,"lwaMaxDbA":42.2,"vDirAssisMax":1.538,"vDirDeboutMax":1.765,"ceDirAssisMax":4.09,"ceDirDeboutMax":5.38,"ventMax":1.043,"ceEntrMax":3.59,"vAvgMax":1.137,"ceAvgMax":3.7,"cfeDirDeboutMax":0.1454,"cfeAvgMax":0.1},{"id":"0303DCR132","brand":"Create","model":"Wind Stylance","diameterCm":132,"ceilingDistanceCm":46.5,"testHeightCm":203.5,"ratioDh":0.352,"fixation":"Rotule/pivot","motor":"DC","rpmMax":199,"powerMaxW":34.0,"lwaMaxDbA":45.1,"vDirAssisMax":1.393,"vDirDeboutMax":1.754,"ceDirAssisMax":3.96,"ceDirDeboutMax":5.37,"ventMax":0.898,"ceEntrMax":3.38,"vAvgMax":0.966,"ceAvgMax":3.48,"cfeDirDeboutMax":0.1579,"cfeAvgMax":0.1024},{"id":"0304DCF132","brand":"Create","model":"Wind Calm","diameterCm":132,"ceilingDistanceCm":24.0,"testHeightCm":226.0,"ratioDh":0.182,"fixation":"Monobloc","motor":"DC","rpmMax":138,"powerMaxW":13.0,"lwaMaxDbA":33.4,"vDirAssisMax":0.895,"vDirDeboutMax":1.252,"ceDirAssisMax":3.38,"ceDirDeboutMax":4.85,"ventMax":0.599,"ceEntrMax":2.79,"vAvgMax":0.642,"ceAvgMax":2.885,"cfeDirDeboutMax":0.3731,"cfeAvgMax":0.2219},{"id":"0305DCR132","brand":"Create","model":"Wind Stylance","diameterCm":132,"ceilingDistanceCm":37.0,"testHeightCm":213.0,"ratioDh":0.28,"fixation":"Rotule/pivot","motor":"DC","rpmMax":200,"powerMaxW":28.0,"lwaMaxDbA":43.4,"vDirAssisMax":1.469,"vDirDeboutMax":1.659,"ceDirAssisMax":4.03,"ceDirDeboutMax":5.29,"ventMax":0.91,"ceEntrMax":3.4,"vAvgMax":0.981,"ceAvgMax":3.505,"cfeDirDeboutMax":0.1889,"cfeAvgMax":0.1252},{"id":"0401DCR162","brand":"Turbobrise","model":"ERP160","diameterCm":162,"ceilingDistanceCm":34.0,"testHeightCm":216.0,"ratioDh":0.21,"fixation":"Rotule/pivot","motor":"DC","rpmMax":163,"powerMaxW":37.0,"lwaMaxDbA":50.9,"vDirAssisMax":1.552,"vDirDeboutMax":1.829,"ceDirAssisMax":4.1,"ceDirDeboutMax":5.43,"ventMax":1.01,"ceEntrMax":3.55,"vAvgMax":1.097,"ceAvgMax":3.655,"cfeDirDeboutMax":0.1468,"cfeAvgMax":0.0988},{"id":"0501DCR132","brand":"Big Ass Fans","model":"Haiku L","diameterCm":132,"ceilingDistanceCm":29.5,"testHeightCm":220.5,"ratioDh":0.223,"fixation":"Rotule/pivot","motor":"DC","rpmMax":204,"powerMaxW":22.0,"lwaMaxDbA":47.4,"vDirAssisMax":1.621,"vDirDeboutMax":1.848,"ceDirAssisMax":4.15,"ceDirDeboutMax":5.45,"ventMax":0.936,"ceEntrMax":3.44,"vAvgMax":1.011,"ceAvgMax":3.545,"cfeDirDeboutMax":0.2477,"cfeAvgMax":0.1611},{"id":"0701DCR086","brand":"Inspire","model":"Palmela S","diameterCm":86,"ceilingDistanceCm":29.0,"testHeightCm":221.0,"ratioDh":0.337,"fixation":"Rotule/pivot","motor":"DC","rpmMax":410,"powerMaxW":16.0,"lwaMaxDbA":50.2,"vDirAssisMax":1.738,"vDirDeboutMax":2.097,"ceDirAssisMax":4.23,"ceDirDeboutMax":5.63,"ventMax":0.798,"ceEntrMax":3.22,"vAvgMax":0.849,"ceAvgMax":3.305,"cfeDirDeboutMax":0.3519,"cfeAvgMax":0.2066},{"id":"0702DCR091","brand":"Inspire","model":"Siroco Plus","diameterCm":91,"ceilingDistanceCm":30.0,"testHeightCm":220.0,"ratioDh":0.33,"fixation":"Rotule/pivot","motor":"DC","rpmMax":214,"powerMaxW":33.0,"lwaMaxDbA":44.7,"vDirAssisMax":0.985,"vDirDeboutMax":1.378,"ceDirAssisMax":3.51,"ceDirDeboutMax":5.01,"ventMax":0.742,"ceEntrMax":3.11,"vAvgMax":0.778,"ceAvgMax":3.175,"cfeDirDeboutMax":0.1518,"cfeAvgMax":0.0962},{"id":"0703DCR107","brand":"Inspire","model":"Beach M","diameterCm":107,"ceilingDistanceCm":40.0,"testHeightCm":210.0,"ratioDh":0.374,"fixation":"Rotule/pivot","motor":"DC","rpmMax":212,"powerMaxW":43.0,"lwaMaxDbA":44.5,"vDirAssisMax":1.371,"vDirDeboutMax":1.887,"ceDirAssisMax":3.94,"ceDirDeboutMax":5.48,"ventMax":0.844,"ceEntrMax":3.3,"vAvgMax":0.903,"ceAvgMax":3.395,"cfeDirDeboutMax":0.1274,"cfeAvgMax":0.079},{"id":"0704DCR071","brand":"Inspire","model":"Beach S","diameterCm":71,"ceilingDistanceCm":26.0,"testHeightCm":224.0,"ratioDh":0.366,"fixation":"Rotule/pivot","motor":"DC","rpmMax":288,"powerMaxW":24.0,"lwaMaxDbA":44.2,"vDirAssisMax":1.118,"vDirDeboutMax":1.486,"ceDirAssisMax":3.68,"ceDirDeboutMax":5.12,"ventMax":0.717,"ceEntrMax":3.06,"vAvgMax":0.77,"ceAvgMax":3.165,"cfeDirDeboutMax":0.2133,"cfeAvgMax":0.1319},{"id":"0705DCR086","brand":"Inspire","model":"Palmela S","diameterCm":86,"ceilingDistanceCm":45.5,"testHeightCm":204.5,"ratioDh":0.529,"fixation":"Rotule/pivot","motor":"DC","rpmMax":401,"powerMaxW":15.0,"lwaMaxDbA":51.5,"vDirAssisMax":1.779,"vDirDeboutMax":2.12,"ceDirAssisMax":4.26,"ceDirDeboutMax":5.64,"ventMax":0.794,"ceEntrMax":3.21,"vAvgMax":0.847,"ceAvgMax":3.295,"cfeDirDeboutMax":0.376,"cfeAvgMax":0.2197},{"id":"0801DCR132","brand":"brasseurs-air-re2020","model":"IZYFAN","diameterCm":132,"ceilingDistanceCm":31.0,"testHeightCm":219.0,"ratioDh":0.235,"fixation":"Rotule/pivot","motor":"DC","rpmMax":224,"powerMaxW":46.0,"lwaMaxDbA":53.8,"vDirAssisMax":2.135,"vDirDeboutMax":2.411,"ceDirAssisMax":4.47,"ceDirDeboutMax":5.82,"ventMax":1.232,"ceEntrMax":3.81,"vAvgMax":1.33,"ceAvgMax":3.905,"cfeDirDeboutMax":0.1265,"cfeAvgMax":0.0849},{"id":"0802DCF132","brand":"brasseurs-air-re2020","model":"Samarat","diameterCm":132,"ceilingDistanceCm":15.0,"testHeightCm":235.0,"ratioDh":0.114,"fixation":"Monobloc","motor":"DC","rpmMax":203,"powerMaxW":42.0,"lwaMaxDbA":50.1,"vDirAssisMax":0.646,"vDirDeboutMax":0.628,"ceDirAssisMax":2.9,"ceDirDeboutMax":3.63,"ventMax":0.517,"ceEntrMax":2.55,"vAvgMax":0.522,"ceAvgMax":2.575,"cfeDirDeboutMax":0.0864,"cfeAvgMax":0.0613},{"id":"0803DCF132","brand":"brasseurs-air-re2020","model":"Samarat","diameterCm":132,"ceilingDistanceCm":43.0,"testHeightCm":207.0,"ratioDh":0.326,"fixation":"Monobloc","motor":"DC","rpmMax":200,"powerMaxW":44.0,"lwaMaxDbA":50.0,"vDirAssisMax":1.9,"vDirDeboutMax":2.18,"ceDirAssisMax":4.34,"ceDirDeboutMax":5.68,"ventMax":1.13,"ceEntrMax":3.7,"vAvgMax":1.215,"ceAvgMax":3.79,"cfeDirDeboutMax":0.1291,"cfeAvgMax":0.0861},{"id":"0901DCR135","brand":"S&P","model":"HTD135DCOAK","diameterCm":135,"ceilingDistanceCm":33.0,"testHeightCm":217.0,"ratioDh":0.244,"fixation":"Rotule/pivot","motor":"DC","rpmMax":199,"powerMaxW":28.0,"lwaMaxDbA":47.0,"vDirAssisMax":1.195,"vDirDeboutMax":1.587,"ceDirAssisMax":3.77,"ceDirDeboutMax":5.22,"ventMax":0.824,"ceEntrMax":3.26,"vAvgMax":0.894,"ceAvgMax":3.38,"cfeDirDeboutMax":0.1864,"cfeAvgMax":0.1207},{"id":"0902ACR100","brand":"S&P","model":"HTB90RC","diameterCm":100,"ceilingDistanceCm":36.0,"testHeightCm":214.0,"ratioDh":0.36,"fixation":"Rotule/pivot","motor":"AC","rpmMax":250,"powerMaxW":61.0,"lwaMaxDbA":50.7,"vDirAssisMax":1.668,"vDirDeboutMax":1.97,"ceDirAssisMax":4.18,"ceDirDeboutMax":5.54,"ventMax":0.882,"ceEntrMax":3.36,"vAvgMax":0.944,"ceAvgMax":3.455,"cfeDirDeboutMax":0.0908,"cfeAvgMax":0.0566},{"id":"0903ACR140","brand":"S&P","model":"HTB140RC","diameterCm":140,"ceilingDistanceCm":36.0,"testHeightCm":214.0,"ratioDh":0.257,"fixation":"Rotule/pivot","motor":"AC","rpmMax":288,"powerMaxW":53.552,"lwaMaxDbA":59.4,"vDirAssisMax":1.454,"vDirDeboutMax":1.6,"ceDirAssisMax":4.02,"ceDirDeboutMax":5.24,"ventMax":0.876,"ceEntrMax":3.35,"vAvgMax":0.953,"ceAvgMax":3.465,"cfeDirDeboutMax":0.0978,"cfeAvgMax":0.0647},{"id":"1001ACR105","brand":"Westinghouse","model":"Princess euro","diameterCm":105,"ceilingDistanceCm":27.5,"testHeightCm":222.5,"ratioDh":0.262,"fixation":"Rotule/pivot","motor":"AC","rpmMax":222,"powerMaxW":45.0,"lwaMaxDbA":46.4,"vDirAssisMax":0.94,"vDirDeboutMax":1.283,"ceDirAssisMax":3.45,"ceDirDeboutMax":4.89,"ventMax":0.588,"ceEntrMax":2.76,"vAvgMax":0.627,"ceAvgMax":2.85,"cfeDirDeboutMax":0.1087,"cfeAvgMax":0.0633},{"id":"1002ACR142","brand":"Westinghouse","model":"Industrial","diameterCm":142,"ceilingDistanceCm":44.5,"testHeightCm":205.5,"ratioDh":0.313,"fixation":"Rotule/pivot","motor":"AC","rpmMax":262,"powerMaxW":61.0,"lwaMaxDbA":58.7,"vDirAssisMax":1.533,"vDirDeboutMax":1.709,"ceDirAssisMax":4.08,"ceDirDeboutMax":5.33,"ventMax":0.951,"ceEntrMax":3.46,"vAvgMax":1.03,"ceAvgMax":3.57,"cfeDirDeboutMax":0.0874,"cfeAvgMax":0.0585},{"id":"1003ACR076","brand":"Westinghouse","model":"Turbo Swirl","diameterCm":76,"ceilingDistanceCm":23.5,"testHeightCm":226.5,"ratioDh":0.309,"fixation":"Rotule/pivot","motor":"AC","rpmMax":270,"powerMaxW":50.0,"lwaMaxDbA":49.6,"vDirAssisMax":1.13,"vDirDeboutMax":1.492,"ceDirAssisMax":3.7,"ceDirDeboutMax":5.13,"ventMax":0.708,"ceEntrMax":3.04,"vAvgMax":0.757,"ceAvgMax":3.135,"cfeDirDeboutMax":0.1026,"cfeAvgMax":0.0627},{"id":"1004ACR132","brand":"Westinghouse","model":"Bendan","diameterCm":132,"ceilingDistanceCm":24.0,"testHeightCm":226.0,"ratioDh":0.182,"fixation":"Rotule/pivot","motor":"AC","rpmMax":182,"powerMaxW":60.0,"lwaMaxDbA":49.3,"vDirAssisMax":0.978,"vDirDeboutMax":1.21,"ceDirAssisMax":3.5,"ceDirDeboutMax":4.8,"ventMax":0.611,"ceEntrMax":2.82,"vAvgMax":0.667,"ceAvgMax":2.96,"cfeDirDeboutMax":0.08,"cfeAvgMax":0.0493},{"id":"1101DCR128","brand":"Faro","model":"Just fan","diameterCm":128,"ceilingDistanceCm":28.0,"testHeightCm":222.0,"ratioDh":0.219,"fixation":"Rotule/pivot","motor":"DC","rpmMax":184,"powerMaxW":29.0,"lwaMaxDbA":38.6,"vDirAssisMax":1.044,"vDirDeboutMax":1.474,"ceDirAssisMax":3.59,"ceDirDeboutMax":5.11,"ventMax":0.683,"ceEntrMax":2.99,"vAvgMax":0.733,"ceAvgMax":3.09,"cfeDirDeboutMax":0.1762,"cfeAvgMax":0.1066},{"id":"1101DCR128B","brand":"Faro","model":"Just fan","diameterCm":128,"ceilingDistanceCm":38.0,"testHeightCm":212.0,"ratioDh":0.297,"fixation":"Rotule/pivot","motor":"DC","rpmMax":178,"powerMaxW":30.0,"lwaMaxDbA":43.3,"vDirAssisMax":1.414,"vDirDeboutMax":1.538,"ceDirAssisMax":3.98,"ceDirDeboutMax":5.18,"ventMax":0.79,"ceEntrMax":3.2,"vAvgMax":0.85,"ceAvgMax":3.305,"cfeDirDeboutMax":0.1727,"cfeAvgMax":0.1102},{"id":"1102DCR132","brand":"Faro","model":"Nu","diameterCm":132,"ceilingDistanceCm":31.0,"testHeightCm":219.0,"ratioDh":0.235,"fixation":"Rotule/pivot","motor":"DC","rpmMax":220,"powerMaxW":31.0,"lwaMaxDbA":44.1,"vDirAssisMax":1.054,"vDirDeboutMax":1.154,"ceDirAssisMax":3.6,"ceDirDeboutMax":4.72,"ventMax":0.661,"ceEntrMax":2.94,"vAvgMax":0.723,"ceAvgMax":3.08,"cfeDirDeboutMax":0.1523,"cfeAvgMax":0.0994},{"id":"1103DCR142","brand":"Faro","model":"Malvinas","diameterCm":142,"ceilingDistanceCm":63.0,"testHeightCm":187.0,"ratioDh":0.444,"fixation":"Rotule/pivot","motor":"DC","rpmMax":236,"powerMaxW":75.0,"lwaMaxDbA":55.6,"vDirAssisMax":1.164,"vDirDeboutMax":1.314,"ceDirAssisMax":3.73,"ceDirDeboutMax":4.93,"ventMax":0.851,"ceEntrMax":3.31,"vAvgMax":0.914,"ceAvgMax":3.41,"cfeDirDeboutMax":0.0657,"cfeAvgMax":0.0455},{"id":"1201DCF132","brand":"Fanelite","model":"ARATUA","diameterCm":132,"ceilingDistanceCm":21.0,"testHeightCm":229.0,"ratioDh":0.159,"fixation":"Monobloc","motor":"DC","rpmMax":167,"powerMaxW":34.0,"lwaMaxDbA":43.9,"vDirAssisMax":0.735,"vDirDeboutMax":0.952,"ceDirAssisMax":3.1,"ceDirDeboutMax":4.39,"ventMax":0.605,"ceEntrMax":2.8,"vAvgMax":0.688,"ceAvgMax":3.005,"cfeDirDeboutMax":0.1291,"cfeAvgMax":0.0884},{"id":"1401ACR132","brand":"Casafan","model":"Titanum","diameterCm":132,"ceilingDistanceCm":26.0,"testHeightCm":224.0,"ratioDh":0.197,"fixation":"Rotule/pivot","motor":"AC","rpmMax":266,"powerMaxW":66.0,"lwaMaxDbA":55.1,"vDirAssisMax":1.364,"vDirDeboutMax":1.723,"ceDirAssisMax":3.94,"ceDirDeboutMax":5.35,"ventMax":0.802,"ceEntrMax":3.22,"vAvgMax":0.865,"ceAvgMax":3.335,"cfeDirDeboutMax":0.0811,"cfeAvgMax":0.0505},{"id":"1402DCF142","brand":"CasaFan","model":"Eco Pallas","diameterCm":142,"ceilingDistanceCm":20.0,"testHeightCm":230.0,"ratioDh":0.141,"fixation":"Monobloc","motor":"DC","rpmMax":138,"powerMaxW":26.0,"lwaMaxDbA":50.6,"vDirAssisMax":0.745,"vDirDeboutMax":1.094,"ceDirAssisMax":3.12,"ceDirDeboutMax":4.63,"ventMax":0.535,"ceEntrMax":2.61,"vAvgMax":0.568,"ceAvgMax":2.71,"cfeDirDeboutMax":0.1781,"cfeAvgMax":0.1042},{"id":"1403DCR152","brand":"Casafan","model":"Eco genuino","diameterCm":152,"ceilingDistanceCm":35.0,"testHeightCm":215.0,"ratioDh":0.23,"fixation":"Rotule/pivot","motor":"DC","rpmMax":155,"powerMaxW":17.0,"lwaMaxDbA":32.3,"vDirAssisMax":1.21,"vDirDeboutMax":1.284,"ceDirAssisMax":3.79,"ceDirDeboutMax":4.89,"ventMax":0.771,"ceEntrMax":3.17,"vAvgMax":0.846,"ceAvgMax":3.295,"cfeDirDeboutMax":0.2876,"cfeAvgMax":0.1938},{"id":"1404DCF140","brand":"Casafan","model":"Eco regento","diameterCm":140,"ceilingDistanceCm":27.0,"testHeightCm":223.0,"ratioDh":0.193,"fixation":"Monobloc","motor":"DC","rpmMax":138,"powerMaxW":26.0,"lwaMaxDbA":36.2,"vDirAssisMax":0.995,"vDirDeboutMax":1.339,"ceDirAssisMax":3.53,"ceDirDeboutMax":4.96,"ventMax":0.666,"ceEntrMax":2.95,"vAvgMax":0.727,"ceAvgMax":3.075,"cfeDirDeboutMax":0.1908,"cfeAvgMax":0.1183},{"id":"1405ACF132","brand":"Casafan","model":"Classic Flat III","diameterCm":132,"ceilingDistanceCm":19.0,"testHeightCm":231.0,"ratioDh":0.144,"fixation":"Monobloc","motor":"AC","rpmMax":48,"powerMaxW":25.0,"lwaMaxDbA":42.6,"vDirAssisMax":0.215,"vDirDeboutMax":0.245,"ceDirAssisMax":0.96,"ceDirDeboutMax":1.54,"ventMax":0.172,"ceEntrMax":0.5,"vAvgMax":0.595,"ceAvgMax":2.775,"cfeDirDeboutMax":0.0616,"cfeAvgMax":0.111},{"id":"1501DCR100","brand":"Leds C4","model":"Xaloc","diameterCm":100,"ceilingDistanceCm":27.0,"testHeightCm":223.0,"ratioDh":0.27,"fixation":"Rotule/pivot","motor":"DC","rpmMax":149,"powerMaxW":6.0,"lwaMaxDbA":45.8,"vDirAssisMax":0.5,"vDirDeboutMax":0.686,"ceDirAssisMax":2.5,"ceDirDeboutMax":3.8,"ventMax":0.323,"ceEntrMax":1.75,"vAvgMax":0.582,"ceAvgMax":2.75,"cfeDirDeboutMax":0.6333,"cfeAvgMax":0.4583},{"id":"1601DCR152","brand":"Anemoi","model":"Boreas","diameterCm":152,"ceilingDistanceCm":29.0,"testHeightCm":221.0,"ratioDh":0.191,"fixation":"Rotule/pivot","motor":"DC","rpmMax":206,"powerMaxW":37.0,"lwaMaxDbA":53.3,"vDirAssisMax":1.724,"vDirDeboutMax":1.81,"ceDirAssisMax":4.22,"ceDirDeboutMax":5.42,"ventMax":1.072,"ceEntrMax":3.63,"vAvgMax":1.182,"ceAvgMax":3.76,"cfeDirDeboutMax":0.1465,"cfeAvgMax":0.1016},{"id":"1602DCR132","brand":"Anemoi","model":"Cefiro","diameterCm":132,"ceilingDistanceCm":28.5,"testHeightCm":221.5,"ratioDh":0.216,"fixation":"Rotule/pivot","motor":"DC","rpmMax":204,"powerMaxW":35.0,"lwaMaxDbA":52.0,"vDirAssisMax":1.854,"vDirDeboutMax":2.402,"ceDirAssisMax":4.31,"ceDirDeboutMax":5.81,"ventMax":1.067,"ceEntrMax":3.62,"vAvgMax":1.153,"ceAvgMax":3.725,"cfeDirDeboutMax":0.166,"cfeAvgMax":0.1064},{"id":"1701DCR150","brand":"LucciAIR","model":"Louisville","diameterCm":150,"ceilingDistanceCm":29.0,"testHeightCm":221.0,"ratioDh":0.193,"fixation":"Rotule/pivot","motor":"DC","rpmMax":168,"powerMaxW":38.0,"lwaMaxDbA":51.7,"vDirAssisMax":1.649,"vDirDeboutMax":2.027,"ceDirAssisMax":4.17,"ceDirDeboutMax":5.58,"ventMax":1.096,"ceEntrMax":3.66,"vAvgMax":1.185,"ceAvgMax":3.76,"cfeDirDeboutMax":0.1468,"cfeAvgMax":0.0989},{"id":"1801DCR132","brand":"Wixled","model":"Hanoi","diameterCm":132,"ceilingDistanceCm":29.5,"testHeightCm":220.5,"ratioDh":0.223,"fixation":"Rotule/pivot","motor":"DC","rpmMax":187,"powerMaxW":28.0,"lwaMaxDbA":48.7,"vDirAssisMax":1.648,"vDirDeboutMax":1.847,"ceDirAssisMax":4.17,"ceDirDeboutMax":5.45,"ventMax":0.936,"ceEntrMax":3.44,"vAvgMax":1.009,"ceAvgMax":3.545,"cfeDirDeboutMax":0.1946,"cfeAvgMax":0.1266},{"id":"1803DCF132","brand":"Wixled","model":"Bony","diameterCm":132,"ceilingDistanceCm":19.0,"testHeightCm":231.0,"ratioDh":0.144,"fixation":"Monobloc","motor":"DC","rpmMax":192,"powerMaxW":28.0,"lwaMaxDbA":48.5,"vDirAssisMax":0.679,"vDirDeboutMax":0.729,"ceDirAssisMax":2.98,"ceDirDeboutMax":3.91,"ventMax":0.506,"ceEntrMax":2.52,"vAvgMax":0.534,"ceAvgMax":2.605,"cfeDirDeboutMax":0.1396,"cfeAvgMax":0.093},{"id":"1802ACR132","brand":"Wixled","model":"Taliou","diameterCm":132,"ceilingDistanceCm":32.0,"testHeightCm":218.0,"ratioDh":0.242,"fixation":"Rotule/pivot","motor":"AC","rpmMax":175,"powerMaxW":56.0,"lwaMaxDbA":46.5,"vDirAssisMax":1.41,"vDirDeboutMax":1.84,"ceDirAssisMax":3.98,"ceDirDeboutMax":5.44,"ventMax":0.86,"ceEntrMax":3.33,"vAvgMax":0.93,"ceAvgMax":3.44,"cfeDirDeboutMax":0.0971,"cfeAvgMax":0.0614},{"id":"1901ACR140","brand":"RS Pro","model":"136-9561","diameterCm":140,"ceilingDistanceCm":34.0,"testHeightCm":216.0,"ratioDh":0.243,"fixation":"Rotule/pivot","motor":"AC","rpmMax":270,"powerMaxW":72.0,"lwaMaxDbA":59.7,"vDirAssisMax":1.481,"vDirDeboutMax":1.845,"ceDirAssisMax":4.04,"ceDirDeboutMax":5.45,"ventMax":1.004,"ceEntrMax":3.54,"vAvgMax":1.076,"ceAvgMax":3.63,"cfeDirDeboutMax":0.0757,"cfeAvgMax":0.0504},{"id":"2101ACR122","brand":"Wiltec","model":"65052","diameterCm":122,"ceilingDistanceCm":25.5,"testHeightCm":224.5,"ratioDh":0.209,"fixation":"Rotule/pivot","motor":"DC","rpmMax":189,"powerMaxW":55.0,"lwaMaxDbA":46.7,"vDirAssisMax":1.109,"vDirDeboutMax":1.223,"ceDirAssisMax":3.67,"ceDirDeboutMax":4.82,"ventMax":0.645,"ceEntrMax":2.9,"vAvgMax":0.693,"ceAvgMax":3.01,"cfeDirDeboutMax":0.0876,"cfeAvgMax":0.0547},{"id":"2301ACR122","brand":"Proficare","model":"PC-DVL-3071","diameterCm":122,"ceilingDistanceCm":23.0,"testHeightCm":227.0,"ratioDh":0.189,"fixation":"Rotule/pivot","motor":"AC","rpmMax":283,"powerMaxW":63.0,"lwaMaxDbA":59.3,"vDirAssisMax":1.265,"vDirDeboutMax":1.384,"ceDirAssisMax":3.84,"ceDirDeboutMax":5.01,"ventMax":0.701,"ceEntrMax":3.03,"vAvgMax":0.756,"ceAvgMax":3.135,"cfeDirDeboutMax":0.0795,"cfeAvgMax":0.0498},{"id":"2401DCR117","brand":"Frabilamp","model":"Klod","diameterCm":117,"ceilingDistanceCm":33.0,"testHeightCm":217.0,"ratioDh":0.282,"fixation":"Rotule/pivot","motor":"DC","rpmMax":233,"powerMaxW":28.0,"lwaMaxDbA":51.9,"vDirAssisMax":1.09,"vDirDeboutMax":1.34,"ceDirAssisMax":3.65,"ceDirDeboutMax":4.96,"ventMax":0.686,"ceEntrMax":2.99,"vAvgMax":0.733,"ceAvgMax":3.085,"cfeDirDeboutMax":0.1771,"cfeAvgMax":0.1102}];

// src/core/constants.js
const EPS = 1e-9;
const SMALL_FAN_LIMIT = 2.13;
const SMALL_SAFETY_HEIGHT = 2.13;
const LARGE_SAFETY_HEIGHT = 3.05;
const MAX_GRID_FANS = 36;

const MOUNT_MODES = [
  {
    id: "standard",
    label: "Montage standard",
    factor: 0.35,
    penaltyText: "Pas de reduction explicite dans le guide.",
    severity: "ok"
  },
  {
    id: "low-profile",
    label: "Montage low-profile",
    factor: 0.25,
    penaltyText: "Reduction de vitesse d'air d'environ 15 % selon le guide.",
    severity: "warn"
  }
];

const FLUSH_MODE = {
  id: "flush",
  label: "Montage flush",
  factor: 0.15,
  penaltyText:
    "Perte de performance de plus de 40 % ; le guide demande de l'eviter au maximum.",
  severity: "alert"
};

// src/core/formatters.js
function parseNumber(input) {
  const raw = typeof input === "string" ? input : input.value;
  return Number.parseFloat(String(raw).replace(",", "."));
}

function formatInputValue(value) {
  return Number.isFinite(value) ? String(value).replace(".", ",") : "";
}

function formatNumber(value, digits = 2) {
  return new Intl.NumberFormat("fr-FR", {
    minimumFractionDigits: 0,
    maximumFractionDigits: digits
  }).format(value);
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll("\"", "&quot;")
    .replaceAll("'", "&#39;");
}

function formatMeters(value, digits = 2) {
  return `${formatNumber(value, digits)} m`;
}

function formatDiameterCm(value) {
  return `${Math.round(value * 100)}`;
}

function formatDiameterCmList(values) {
  return values
    .map((value) => formatDiameterCm(typeof value === "number" ? value : value.diameter))
    .join(", ");
}

function formatSquareMeters(value, digits = 2) {
  return `${formatNumber(value, digits)} m²`;
}

function formatFactor(value, digits = 2) {
  return formatNumber(value, digits);
}

function formatTemp(value, digits = 2) {
  return `${formatNumber(value, digits)} °C`;
}

function formatDb(value, digits = 1) {
  return `${formatNumber(value, digits)} dBA`;
}

function formatWatts(value, digits = 0) {
  return `${formatNumber(value, digits)} W`;
}

function formatDateTime(value = new Date()) {
  return new Intl.DateTimeFormat("fr-FR", {
    dateStyle: "medium",
    timeStyle: "short"
  }).format(value);
}

function formatDiameterList(values) {
  return values.map((diameter) => formatMeters(diameter)).join(", ");
}

function joinWithOr(values) {
  if (values.length <= 1) {
    return values[0] || "";
  }
  if (values.length === 2) {
    return `${values[0]} ou ${values[1]}`;
  }
  return `${values.slice(0, -1).join(", ")} ou ${values[values.length - 1]}`;
}

// src/app/dom.js
function getDomRefs() {
  return {
    form: document.getElementById("calculator-form"),
    simulationNameInput: document.getElementById("simulation-name"),
    lengthInput: document.getElementById("length"),
    widthInput: document.getElementById("width"),
    heightInput: document.getElementById("height"),
    ceilingEnabledInput: document.getElementById("ceiling-enabled"),
    ceilingConfig: document.getElementById("ceiling-config"),
    ceilingAnchorXInput: document.getElementById("ceiling-anchor-x"),
    ceilingAnchorYInput: document.getElementById("ceiling-anchor-y"),
    ceilingEditorSummary: document.getElementById("ceiling-editor-summary"),
    ceilingEditorCanvas: document.getElementById("ceiling-editor-canvas"),
    clearCeilingLuminairesButton: document.getElementById("clear-ceiling-luminaires"),
    allowStandardInput: document.getElementById("allow-standard"),
    allowLowInput: document.getElementById("allow-low"),
    resetButton: document.getElementById("reset-button"),
    emptyState: document.getElementById("empty-state"),
    resultsContent: document.getElementById("results-content"),
    summaryGrid: document.getElementById("summary-grid"),
    highlights: document.getElementById("highlights"),
    statusNote: document.getElementById("status-note"),
    resultsList: document.getElementById("results-list"),
    reportKicker: document.getElementById("report-kicker"),
    reportTitle: document.getElementById("report-title"),
    reportContext: document.getElementById("report-context"),
    reportMeta: document.getElementById("report-meta"),
    exportSelectionSummary: document.getElementById("export-selection-summary"),
    exportPdfButton: document.getElementById("export-pdf-button"),
    catalogSearchInput: document.getElementById("catalog-search"),
    catalogBrandInput: document.getElementById("catalog-brand"),
    catalogDiameterInput: document.getElementById("catalog-diameter"),
    catalogMotorInput: document.getElementById("catalog-motor"),
    catalogFixationInput: document.getElementById("catalog-fixation"),
    catalogSortInput: document.getElementById("catalog-sort"),
    catalogResetButton: document.getElementById("catalog-reset-button"),
    catalogSummary: document.getElementById("catalog-summary"),
    catalogTableHost: document.getElementById("catalog-table-host")
  };
}

// src/app/state.js
function createDefaultCeilingLayout() {
  return {
    enabled: false,
    tileSize: 0.6,
    xAnchorMode: "symmetric",
    yAnchorMode: "symmetric",
    luminaireTiles: new Set()
  };
}

function createAppState() {
  return {
    latestReportState: null,
    ceilingLayout: createDefaultCeilingLayout()
  };
}

function setLatestReportState(state, latestReportState) {
  state.latestReportState = latestReportState;
}

function updateCeilingLayout(state, patch) {
  state.ceilingLayout = {
    ...state.ceilingLayout,
    ...patch,
    luminaireTiles:
      patch.luminaireTiles instanceof Set
        ? patch.luminaireTiles
        : new Set(state.ceilingLayout.luminaireTiles)
  };
}

function toggleCeilingLuminaireTile(state, tileKey) {
  const nextLuminaireTiles = new Set(state.ceilingLayout.luminaireTiles);

  if (nextLuminaireTiles.has(tileKey)) {
    nextLuminaireTiles.delete(tileKey);
  } else {
    nextLuminaireTiles.add(tileKey);
  }

  updateCeilingLayout(state, { luminaireTiles: nextLuminaireTiles });
}

function clearCeilingLuminaires(state) {
  updateCeilingLayout(state, { luminaireTiles: new Set() });
}

function createSelectedReportOptionKeys(items) {
  return items.map((item) => item.key);
}

function getSelectableReportOptions(reportState) {
  if (!reportState) {
    return [];
  }

  if (reportState.kind === "uniformity-ok") {
    return reportState.candidates || [];
  }

  return [];
}

function getSelectedReportOptions(reportState) {
  const selectableOptions = getSelectableReportOptions(reportState);
  if (selectableOptions.length === 0) {
    return [];
  }

  const selectedKeys = new Set(
    reportState.selectedOptionKeys?.length
      ? reportState.selectedOptionKeys
      : createSelectedReportOptionKeys(selectableOptions)
  );

  return selectableOptions.filter((item) => selectedKeys.has(item.key));
}

function toggleLatestReportOptionSelection(state, optionKey, selected) {
  const reportState = state.latestReportState;
  const selectableOptions = getSelectableReportOptions(reportState);
  if (!reportState || selectableOptions.length === 0) {
    return;
  }

  const selectableKeys = selectableOptions.map((item) => item.key);
  if (!selectableKeys.includes(optionKey)) {
    return;
  }

  const nextSelectedKeys = new Set(
    reportState.selectedOptionKeys?.length
      ? reportState.selectedOptionKeys
      : createSelectedReportOptionKeys(selectableOptions)
  );

  if (selected) {
    nextSelectedKeys.add(optionKey);
  } else {
    nextSelectedKeys.delete(optionKey);
  }

  reportState.selectedOptionKeys = selectableKeys.filter((key) => nextSelectedKeys.has(key));
}

function resetState(state) {
  state.latestReportState = null;
  state.ceilingLayout = createDefaultCeilingLayout();
}

// src/core/messages.js
function buildHeightDiameterRequirementMessage(room, modes, realDiameters) {
  const maxRealDiameter = realDiameters.length > 0 ? realDiameters[realDiameters.length - 1] : null;
  if (!room || !Number.isFinite(room.height) || !Number.isFinite(maxRealDiameter) || modes.length === 0) {
    return "";
  }

  const blockedModes = modes
    .map((mode) => ({
      mode,
      minimumDiameter: room.height / (2 + mode.factor)
    }))
    .filter((entry) => entry.minimumDiameter > maxRealDiameter + EPS);

  if (blockedModes.length === 0) {
    return "";
  }

  const requiredText = joinWithOr(
    blockedModes.map(
      (entry) => `${formatMeters(entry.minimumDiameter)} en ${entry.mode.label.toLowerCase()}`
    )
  );

  return `La base BRASSE II embarquee s'arrete a ${formatMeters(maxRealDiameter)} (${formatDiameterCm(maxRealDiameter)} cm). Avec ${formatMeters(room.height)} de HSP, il faudrait ici en theorie au moins ${requiredText}.`;
}

function getCandidateWarnings(candidate) {
  const warnings = [];
  if (!candidate.recommendedSmallHeightMet && candidate.fanClass === "small") {
    warnings.push(
      "La recommandation d'aisance sur la hauteur sous pales (> 1,4 D) n'est pas atteinte."
    );
  }
  if (candidate.mountMode.id === "low-profile") {
    warnings.push(candidate.mountMode.penaltyText);
  }
  return warnings;
}

// src/core/brasse2.js
/**
 * Retourne tous les modeles BRASSE II compatibles avec les diametres admissibles
 * d'une option de calepinage.
 */
function getBrasse2ModelsForCandidate(candidate, brasse2Models) {
  const optionsByDiameterCm = new Map(
    candidate.compatibleRealDiameters.map((option) => [Math.round(option.diameter * 100), option])
  );

  return brasse2Models
    .filter((model) => optionsByDiameterCm.has(model.diameterCm))
    .map((model) => ({
      ...model,
      compatibleOption: optionsByDiameterCm.get(model.diameterCm),
      mountFits:
        model.ceilingDistanceCm <=
        candidate.mountMode.factor * optionsByDiameterCm.get(model.diameterCm).diameter * 100 + 0.5,
      mountDeltaCm: Number(
        (
          candidate.mountMode.factor * optionsByDiameterCm.get(model.diameterCm).diameter * 100 -
          model.ceilingDistanceCm
        ).toFixed(1)
      ),
      isSelectedDiameter: Math.abs(model.diameterCm - Math.round(candidate.diameter * 100)) <= 0.5
    }));
}

function pickBestModel(models, comparator) {
  return [...models].sort(comparator)[0] || null;
}

function compareComfort(a, b) {
  if (Math.abs(b.ceDirDeboutMax - a.ceDirDeboutMax) > EPS) {
    return b.ceDirDeboutMax - a.ceDirDeboutMax;
  }
  if (Math.abs(a.lwaMaxDbA - b.lwaMaxDbA) > EPS) {
    return a.lwaMaxDbA - b.lwaMaxDbA;
  }
  return a.powerMaxW - b.powerMaxW;
}

function compareEfficiency(a, b) {
  if (Math.abs(b.cfeDirDeboutMax - a.cfeDirDeboutMax) > EPS) {
    return b.cfeDirDeboutMax - a.cfeDirDeboutMax;
  }
  if (Math.abs(a.lwaMaxDbA - b.lwaMaxDbA) > EPS) {
    return a.lwaMaxDbA - b.lwaMaxDbA;
  }
  return b.ceDirDeboutMax - a.ceDirDeboutMax;
}

function compareAcoustics(a, b) {
  if (Math.abs(a.lwaMaxDbA - b.lwaMaxDbA) > EPS) {
    return a.lwaMaxDbA - b.lwaMaxDbA;
  }
  if (Math.abs(b.ceDirDeboutMax - a.ceDirDeboutMax) > EPS) {
    return b.ceDirDeboutMax - a.ceDirDeboutMax;
  }
  return a.powerMaxW - b.powerMaxW;
}

function compareCoverage(a, b) {
  const coverageA = Number.isFinite(a.compatibleOption?.coverageFactor)
    ? a.compatibleOption.coverageFactor
    : -Infinity;
  const coverageB = Number.isFinite(b.compatibleOption?.coverageFactor)
    ? b.compatibleOption.coverageFactor
    : -Infinity;

  if (Math.abs(coverageB - coverageA) > EPS) {
    return coverageB - coverageA;
  }
  return compareComfort(a, b);
}

function modelMountLabel(model) {
  return model.mountFits
    ? "Distance plafond/BA compatible"
    : "Distance plafond/BA a verifier";
}

function buildModelPicks(models) {
  return [
    { title: "FCC", model: pickBestModel(models, compareCoverage) },
    { title: "Confort", model: pickBestModel(models, compareComfort) },
    { title: "Efficacite", model: pickBestModel(models, compareEfficiency) },
    { title: "Acoustique", model: pickBestModel(models, compareAcoustics) }
  ].filter((pick) => Boolean(pick.model));
}

function getReportModelHighlights(candidate, brasse2Models) {
  return buildModelPicks(getBrasse2ModelsForCandidate(candidate, brasse2Models));
}

// src/core/catalog.js
function compareCatalogIdentity(a, b) {
  const brandDiff = String(a.brand).localeCompare(String(b.brand), "fr", {
    sensitivity: "base"
  });
  if (brandDiff !== 0) {
    return brandDiff;
  }
  const modelDiff = String(a.model).localeCompare(String(b.model), "fr", {
    sensitivity: "base"
  });
  if (modelDiff !== 0) {
    return modelDiff;
  }
  return String(a.id).localeCompare(String(b.id), "fr", {
    sensitivity: "base",
    numeric: true
  });
}

const CATALOG_SORTS = {
  "diameter-asc": (a, b) => a.diameterCm - b.diameterCm || compareCatalogIdentity(a, b),
  "diameter-desc": (a, b) => b.diameterCm - a.diameterCm || compareCatalogIdentity(a, b),
  comfort: (a, b) => compareComfort(a, b) || compareCatalogIdentity(a, b),
  efficiency: (a, b) => compareEfficiency(a, b) || compareCatalogIdentity(a, b),
  acoustics: (a, b) => compareAcoustics(a, b) || compareCatalogIdentity(a, b),
  power: (a, b) => a.powerMaxW - b.powerMaxW || compareCatalogIdentity(a, b),
  brand: compareCatalogIdentity
};

function normalizeCatalogSearch(value) {
  return String(value || "")
    .trim()
    .toLocaleLowerCase("fr")
    .normalize("NFD")
    .replaceAll(/\p{Diacritic}/gu, "");
}

function getCatalogFilterOptions(brasse2Models) {
  return {
    brands: [...new Set(brasse2Models.map((model) => model.brand))].sort((a, b) =>
      String(a).localeCompare(String(b), "fr", { sensitivity: "base" })
    ),
    diameters: [...new Set(brasse2Models.map((model) => model.diameterCm))].sort((a, b) => a - b),
    motors: [...new Set(brasse2Models.map((model) => model.motor))].sort(),
    fixations: [...new Set(brasse2Models.map((model) => model.fixation))].sort((a, b) =>
      String(a).localeCompare(String(b), "fr", { sensitivity: "base" })
    )
  };
}

function getCatalogActiveFilterLabels(filters) {
  const labels = [];
  if (filters.search) {
    labels.push(`recherche: ${filters.search}`);
  }
  if (filters.brand) {
    labels.push(`marque: ${filters.brand}`);
  }
  if (Number.isFinite(filters.diameterCm)) {
    labels.push(`diametre: ${filters.diameterCm} cm`);
  }
  if (filters.motor) {
    labels.push(`moteur: ${filters.motor}`);
  }
  if (filters.fixation) {
    labels.push(`fixation: ${filters.fixation}`);
  }
  return labels;
}

function getFilteredCatalogModels(brasse2Models, filters) {
  const search = normalizeCatalogSearch(filters.search);
  const comparator = CATALOG_SORTS[filters.sort] || CATALOG_SORTS["diameter-asc"];

  return brasse2Models
    .filter((model) => {
      if (search) {
        const haystack = normalizeCatalogSearch(
          `${model.id} ${model.brand} ${model.model} ${model.motor} ${model.fixation} ${model.diameterCm}`
        );
        if (!haystack.includes(search)) {
          return false;
        }
      }

      if (filters.brand && model.brand !== filters.brand) {
        return false;
      }
      if (Number.isFinite(filters.diameterCm) && model.diameterCm !== filters.diameterCm) {
        return false;
      }
      if (filters.motor && model.motor !== filters.motor) {
        return false;
      }
      if (filters.fixation && model.fixation !== filters.fixation) {
        return false;
      }
      return true;
    })
    .sort(comparator);
}

// src/core/ceilingGrid.js
const CEILING_TILE_SIZE = 0.6;
const CEILING_ANCHOR_MODES = ["min_side", "max_side", "symmetric"];

function ceilingGridRound(value) {
  return Number(value.toFixed(6));
}

function buildCeilingAxisTiles(length, tileSize, anchorMode, axisName) {
  const safeLength = Math.max(0, length);
  const fullTileCount = Math.floor((safeLength + EPS) / tileSize);
  const rawRemainder = safeLength - fullTileCount * tileSize;
  const remainder = rawRemainder > EPS ? rawRemainder : 0;
  const tiles = [];
  let cursor = 0;

  const pushTile = (min, max, kind) => {
    if (max - min <= EPS) {
      return;
    }

    tiles.push({
      axis: axisName,
      index: tiles.length,
      key: `${axisName}-${tiles.length}`,
      min: ceilingGridRound(min),
      max: ceilingGridRound(max),
      size: ceilingGridRound(max - min),
      center: ceilingGridRound((min + max) / 2),
      isCut: kind === "cut"
    });
  };

  if (remainder === 0 || anchorMode === "min_side") {
    for (let index = 0; index < fullTileCount; index += 1) {
      pushTile(cursor, cursor + tileSize, "full");
      cursor += tileSize;
    }
    if (remainder > 0) {
      pushTile(cursor, safeLength, "cut");
    }
  } else if (anchorMode === "max_side") {
    pushTile(0, remainder, "cut");
    cursor = remainder;
    for (let index = 0; index < fullTileCount; index += 1) {
      pushTile(cursor, cursor + tileSize, "full");
      cursor += tileSize;
    }
  } else {
    const sideCut = remainder / 2;
    pushTile(0, sideCut, "cut");
    cursor = sideCut;
    for (let index = 0; index < fullTileCount; index += 1) {
      pushTile(cursor, cursor + tileSize, "full");
      cursor += tileSize;
    }
    pushTile(cursor, safeLength, "cut");
  }

  const boundaries = [...new Set([0, ...tiles.flatMap((tile) => [tile.min, tile.max]), safeLength])].sort(
    (a, b) => a - b
  );

  return {
    axis: axisName,
    length: safeLength,
    tileSize,
    anchorMode,
    tiles,
    boundaries,
    centers: tiles.map((tile) => ({
      index: tile.index,
      key: tile.key,
      center: tile.center,
      min: tile.min,
      max: tile.max,
      isCut: tile.isCut
    })),
    fullTilesCount: tiles.filter((tile) => !tile.isCut).length,
    cutTilesCount: tiles.filter((tile) => tile.isCut).length
  };
}

function buildCeilingGrid(room, ceilingLayout) {
  if (!room || !(room.length > 0) || !(room.width > 0)) {
    return null;
  }

  const tileSize = ceilingLayout?.tileSize || CEILING_TILE_SIZE;
  const xAxis = buildCeilingAxisTiles(
    room.length,
    tileSize,
    ceilingLayout?.xAnchorMode || "symmetric",
    "x"
  );
  const yAxis = buildCeilingAxisTiles(
    room.width,
    tileSize,
    ceilingLayout?.yAnchorMode || "symmetric",
    "y"
  );
  const luminaireTiles =
    ceilingLayout?.luminaireTiles instanceof Set ? ceilingLayout.luminaireTiles : new Set();
  const tiles = [];
  const tileMap = new Map();

  xAxis.tiles.forEach((xTile) => {
    yAxis.tiles.forEach((yTile) => {
      const key = `${xTile.index}:${yTile.index}`;
      const tile = {
        key,
        xIndex: xTile.index,
        yIndex: yTile.index,
        minX: xTile.min,
        maxX: xTile.max,
        minY: yTile.min,
        maxY: yTile.max,
        width: xTile.size,
        height: yTile.size,
        centerX: xTile.center,
        centerY: yTile.center,
        isCut: xTile.isCut || yTile.isCut,
        hasLuminaire: luminaireTiles.has(key)
      };

      tiles.push(tile);
      tileMap.set(key, tile);
    });
  });

  return {
    room,
    tileSize,
    xAnchorMode: xAxis.anchorMode,
    yAnchorMode: yAxis.anchorMode,
    xAxis,
    yAxis,
    tiles,
    tileMap,
    luminaireTiles,
    luminairesCount: tiles.filter((tile) => tile.hasLuminaire).length,
    fullTilesCount: tiles.filter((tile) => !tile.isCut).length,
    cutTilesCount: tiles.filter((tile) => tile.isCut).length
  };
}

// src/core/ceilingAssessment.js
function ceilingAssessmentValueKey(value) {
  return value.toFixed(6);
}

function getUniqueCoordinateValues(values) {
  const seen = new Set();
  const uniqueValues = [];

  values.forEach((value) => {
    const key = ceilingAssessmentValueKey(value);
    if (seen.has(key)) {
      return;
    }
    seen.add(key);
    uniqueValues.push(value);
  });

  return uniqueValues;
}

function findCenterMatch(axisCenters, value) {
  return axisCenters.find((center) => Math.abs(center.center - value) <= EPS) || null;
}

function buildAxisShiftCandidates(axisValues, axisCenters) {
  if (axisValues.length === 0 || axisCenters.length === 0) {
    return [];
  }

  const referenceValue = axisValues[0];
  const seenShifts = new Set();
  const candidates = [];

  axisCenters.forEach((center) => {
    const delta = center.center - referenceValue;
    const deltaKey = ceilingAssessmentValueKey(delta);

    if (seenShifts.has(deltaKey)) {
      return;
    }

    const centerByValueKey = new Map();
    let valid = true;

    axisValues.forEach((value) => {
      const shiftedValue = value + delta;
      const match = findCenterMatch(axisCenters, shiftedValue);

      if (!match) {
        valid = false;
        return;
      }

      centerByValueKey.set(ceilingAssessmentValueKey(value), match);
    });

    if (!valid) {
      return;
    }

    seenShifts.add(deltaKey);
    candidates.push({
      delta,
      centerByValueKey
    });
  });

  return candidates.sort((a, b) => Math.abs(a.delta) - Math.abs(b.delta));
}

function getMinimumClearanceForCoordinates(room, coordinates) {
  return coordinates.reduce((bestDistance, point) => {
    const nextDistance = Math.min(point.x, room.length - point.x, point.y, room.width - point.y);
    return Math.min(bestDistance, nextDistance);
  }, Number.POSITIVE_INFINITY);
}

function createIncompatibleAssessment(reasonCode, reasonText, extra = {}) {
  return {
    enabled: true,
    compatible: false,
    reasonCode,
    reasonText,
    dx: 0,
    dy: 0,
    luminaireConflict: false,
    wallClearance: null,
    appliedCoordinates: extra.appliedCoordinates || null,
    shiftApplied: false,
    visualCheckNote: "Verifier visuellement la proximite luminaire / brasseur.",
    ...extra
  };
}

function assessOptionCeilingCompatibility(option, ceilingGrid) {
  if (!ceilingGrid) {
    return {
      enabled: false,
      compatible: true,
      reasonCode: null,
      reasonText: "",
      dx: 0,
      dy: 0,
      luminaireConflict: false,
      wallClearance: option.wallClearance,
      appliedCoordinates: option.coordinates,
      shiftApplied: false,
      visualCheckNote: ""
    };
  }

  const uniqueXValues = getUniqueCoordinateValues(option.coordinates.map((point) => point.x));
  const uniqueYValues = getUniqueCoordinateValues(option.coordinates.map((point) => point.y));
  const xShiftCandidates = buildAxisShiftCandidates(uniqueXValues, ceilingGrid.xAxis.centers);
  const yShiftCandidates = buildAxisShiftCandidates(uniqueYValues, ceilingGrid.yAxis.centers);

  if (xShiftCandidates.length === 0 || yShiftCandidates.length === 0) {
    return createIncompatibleAssessment(
      "rail-alignment",
      "Aucun decalage global ne permet de faire tomber tous les centres sur des dalles du faux plafond.",
      {
        appliedCoordinates: option.coordinates
      }
    );
  }

  const combinedCandidates = [];
  xShiftCandidates.forEach((xShift) => {
    yShiftCandidates.forEach((yShift) => {
      combinedCandidates.push({
        dx: xShift.delta,
        dy: yShift.delta,
        distance: Math.hypot(xShift.delta, yShift.delta),
        xShift,
        yShift
      });
    });
  });
  combinedCandidates.sort((a, b) => a.distance - b.distance);

  let sawLuminaireConflict = false;
  let sawWallConflict = false;

  for (const combinedCandidate of combinedCandidates) {
    const appliedCoordinates = option.coordinates.map((point) => ({
      x: point.x + combinedCandidate.dx,
      y: point.y + combinedCandidate.dy
    }));
    const wallClearance = getMinimumClearanceForCoordinates(option.room, appliedCoordinates);

    if (wallClearance + EPS < option.diameter) {
      sawWallConflict = true;
      continue;
    }

    let hasLuminaireConflict = false;

    for (const point of option.coordinates) {
      const xCenter =
        combinedCandidate.xShift.centerByValueKey.get(ceilingAssessmentValueKey(point.x)) || null;
      const yCenter =
        combinedCandidate.yShift.centerByValueKey.get(ceilingAssessmentValueKey(point.y)) || null;

      if (!xCenter || !yCenter) {
        hasLuminaireConflict = true;
        break;
      }

      const tileKey = `${xCenter.index}:${yCenter.index}`;
      const tile = ceilingGrid.tileMap.get(tileKey);

      if (tile?.hasLuminaire) {
        hasLuminaireConflict = true;
        break;
      }
    }

    if (hasLuminaireConflict) {
      sawLuminaireConflict = true;
      continue;
    }

    return {
      enabled: true,
      compatible: true,
      reasonCode: null,
      reasonText:
        Math.abs(combinedCandidate.dx) > EPS || Math.abs(combinedCandidate.dy) > EPS
          ? "Les centres peuvent etre decales globalement pour tomber dans des dalles du faux plafond."
          : "Les centres theoriques tombent deja dans des dalles du faux plafond.",
      dx: combinedCandidate.dx,
      dy: combinedCandidate.dy,
      luminaireConflict: false,
      wallClearance,
      appliedCoordinates,
      shiftApplied:
        Math.abs(combinedCandidate.dx) > EPS || Math.abs(combinedCandidate.dy) > EPS,
      visualCheckNote: "Verifier visuellement la proximite luminaire / brasseur."
    };
  }

  if (sawLuminaireConflict) {
    return createIncompatibleAssessment(
      "luminaire-conflict",
      "Les positions alignees sur les dalles disponibles tombent sur une ou plusieurs dalles de luminaire existantes.",
      {
        appliedCoordinates: option.coordinates,
        luminaireConflict: true
      }
    );
  }

  if (sawWallConflict) {
    return createIncompatibleAssessment(
      "wall-conflict",
      "Le decalage necessaire pour suivre le faux plafond fait passer au moins un centre trop pres d'un mur.",
      {
        appliedCoordinates: option.coordinates
      }
    );
  }

  return createIncompatibleAssessment(
    "incompatible",
    "Le faux plafond ne permet pas de trouver une variante globale compatible sur cette trame.",
    {
      appliedCoordinates: option.coordinates
    }
  );
}

// src/core/calepinage.js
function getRealDiametersForIntervals(realDiameters, intervals) {
  return realDiameters
    .map((diameter) => {
      const matchingInterval = intervals.find(
        (interval) => diameter >= interval.lower - EPS && diameter <= interval.upper + EPS
      );

      if (!matchingInterval) {
        return null;
      }

      return {
        diameter,
        fanClass: matchingInterval.fanClass
      };
    })
    .filter(Boolean);
}

function getCoverageFactorForDiameter(cellArea, diameter) {
  return diameter / Math.sqrt(cellArea);
}

function isCoverageFactorValid(coverageFactor) {
  return coverageFactor >= 0.2 - EPS && coverageFactor <= 0.4 + EPS;
}

function getCompatibleRealDiameters(realDiameters, intervals, cellArea) {
  return getRealDiametersForIntervals(realDiameters, intervals)
    .map((option) => {
      const coverageFactor = getCoverageFactorForDiameter(cellArea, option.diameter);
      if (!isCoverageFactorValid(coverageFactor)) {
        return null;
      }

      return {
        diameter: option.diameter,
        fanClass: option.fanClass,
        coverageFactor
      };
    })
    .filter(Boolean);
}

/**
 * Calcule les intervalles de diametres encore compatibles avec les contraintes
 * de hauteur pour un mode de montage donne.
 */
function buildHeightFeasibility(height, mountFactor, dGeoMin, dGeoMax) {
  const lowerHeightBound = height / (2 + mountFactor);
  const intervals = [];

  const smallUpper = Math.min(
    SMALL_FAN_LIMIT - EPS,
    (height - SMALL_SAFETY_HEIGHT) / mountFactor
  );
  const smallLower = Math.max(dGeoMin, lowerHeightBound);
  if (smallUpper > smallLower + EPS) {
    intervals.push({
      fanClass: "small",
      lower: smallLower,
      upper: Math.min(dGeoMax, smallUpper)
    });
  }

  const largeUpper = Math.min(
    (height - LARGE_SAFETY_HEIGHT) / mountFactor,
    height / (0.8 + mountFactor),
    dGeoMax
  );
  const largeLower = Math.max(dGeoMin, lowerHeightBound, SMALL_FAN_LIMIT);
  if (largeUpper > largeLower + EPS) {
    intervals.push({
      fanClass: "large",
      lower: largeLower,
      upper: largeUpper
    });
  }

  return intervals.filter((interval) => interval.upper > interval.lower + EPS);
}

/**
 * Evalue une trame uniforme et retourne une option complete si elle respecte
 * toutes les contraintes geometriques et de hauteur.
 */
function evaluateCandidate(room, nx, ny, mountMode, realDiameters) {
  const cellLength = room.length / nx;
  const cellWidth = room.width / ny;
  const cellArea = cellLength * cellWidth;
  const cellShort = Math.min(cellLength, cellWidth);
  const cellLong = Math.max(cellLength, cellWidth);
  const formFactor = cellLong / cellShort;
  const coverageMinDiameter = 0.2 * Math.sqrt(cellArea);
  const coverageMaxDiameter = 0.4 * Math.sqrt(cellArea);
  const wallMaxDiameter = cellShort / 2;

  const spacings = [];
  if (nx > 1) {
    spacings.push(cellLength);
  }
  if (ny > 1) {
    spacings.push(cellWidth);
  }

  const interFanMaxDiameter =
    spacings.length > 0 ? Math.min(...spacings) / 2.5 : Number.POSITIVE_INFINITY;

  const dGeoMax = Math.min(coverageMaxDiameter, wallMaxDiameter, interFanMaxDiameter);
  if (dGeoMax <= coverageMinDiameter + EPS) {
    return null;
  }

  const intervals = buildHeightFeasibility(room.height, mountMode.factor, coverageMinDiameter, dGeoMax);
  if (intervals.length === 0) {
    return null;
  }

  const bestInterval = intervals.reduce(
    (best, current) => (!best || current.upper > best.upper ? current : best),
    null
  );
  const theoreticalMaxDiameter = bestInterval.upper;
  const compatibleRealDiameters = getCompatibleRealDiameters(realDiameters, intervals, cellArea);
  if (compatibleRealDiameters.length === 0) {
    return null;
  }

  const selectedRealDiameter = compatibleRealDiameters[compatibleRealDiameters.length - 1];
  const diameter = selectedRealDiameter.diameter;
  const mountDistance = mountMode.factor * diameter;
  const bladeHeight = room.height - mountDistance;
  const coverageFactor = selectedRealDiameter.coverageFactor;
  const recommendedSmallHeight = 1.4 * diameter;
  const recommendedSmallHeightMet =
    selectedRealDiameter.fanClass === "small"
      ? bladeHeight >= recommendedSmallHeight - EPS
      : true;

  const coordinates = [];
  for (let ix = 0; ix < nx; ix += 1) {
    for (let iy = 0; iy < ny; iy += 1) {
      coordinates.push({
        x: (ix + 0.5) * cellLength,
        y: (iy + 0.5) * cellWidth
      });
    }
  }

  return {
    key: `${nx}x${ny}-${mountMode.id}`,
    nx,
    ny,
    fanCount: nx * ny,
    room,
    mountMode,
    cellLength,
    cellWidth,
    cellArea,
    cellShort,
    cellLong,
    formFactor,
    diameter,
    theoreticalMaxDiameter,
    coverageFactor,
    mountDistance,
    bladeHeight,
    recommendedSmallHeightMet,
    recommendedSmallHeight,
    wallClearance: cellShort / 2,
    interFanSpacing: spacings.length > 0 ? Math.min(...spacings) : null,
    geometryCaps: {
      coverageMinDiameter,
      coverageMaxDiameter,
      wallMaxDiameter,
      interFanMaxDiameter
    },
    compatibleRealDiameters,
    coordinates,
    fanClass: selectedRealDiameter.fanClass,
    strictAdvice:
      "Verifier la fiche du modele retenu si son diametre nominal est proche d'une limite geometrique ou d'une limite de hauteur du guide."
  };
}

function compareCandidates(a, b) {
  if (Math.abs(b.diameter - a.diameter) > EPS) {
    return b.diameter - a.diameter;
  }

  const ffA = Math.abs(a.formFactor - 1);
  const ffB = Math.abs(b.formFactor - 1);
  if (Math.abs(ffA - ffB) > EPS) {
    return ffA - ffB;
  }

  const covA = Math.abs(0.4 - a.coverageFactor);
  const covB = Math.abs(0.4 - b.coverageFactor);
  if (Math.abs(covA - covB) > EPS) {
    return covA - covB;
  }

  if (a.mountMode.id !== b.mountMode.id) {
    return a.mountMode.id === "standard" ? -1 : 1;
  }

  return a.fanCount - b.fanCount;
}

function enumerateCandidates(room, maxFans, modes, realDiameters) {
  const candidates = [];

  for (let nx = 1; nx <= maxFans; nx += 1) {
    for (let ny = 1; ny <= maxFans; ny += 1) {
      if (nx * ny > maxFans) {
        continue;
      }

      for (const mode of modes) {
        const candidate = evaluateCandidate(room, nx, ny, mode, realDiameters);
        if (candidate) {
          candidates.push(candidate);
        }
      }
    }
  }

  return candidates.sort(compareCandidates);
}

function getFallbackFlushCandidate(room, maxFans, realDiameters) {
  const candidates = [];

  for (let nx = 1; nx <= maxFans; nx += 1) {
    for (let ny = 1; ny <= maxFans; ny += 1) {
      if (nx * ny > maxFans) {
        continue;
      }
      const candidate = evaluateCandidate(room, nx, ny, FLUSH_MODE, realDiameters);
      if (candidate) {
        candidates.push(candidate);
      }
    }
  }

  return candidates.sort(compareCandidates)[0] || null;
}

// src/ui/catalog.js
function setCatalogSelectOptions(select, options, emptyLabel, formatter = (value) => value) {
  select.innerHTML = [
    `<option value="">${escapeHtml(emptyLabel)}</option>`,
    ...options.map(
      (value) =>
        `<option value="${escapeHtml(String(value))}">${escapeHtml(formatter(value))}</option>`
    )
  ].join("");
}

function initializeCatalogFilters(dom, brasse2Models) {
  const options = getCatalogFilterOptions(brasse2Models);
  setCatalogSelectOptions(dom.catalogBrandInput, options.brands, "Toutes les marques");
  setCatalogSelectOptions(dom.catalogDiameterInput, options.diameters, "Tous les diametres", (value) => `${value} cm`);
  setCatalogSelectOptions(dom.catalogMotorInput, options.motors, "Tous les moteurs");
  setCatalogSelectOptions(dom.catalogFixationInput, options.fixations, "Toutes les fixations");
  dom.catalogSortInput.value = "diameter-asc";
}

function resetCatalogFilters(dom) {
  dom.catalogSearchInput.value = "";
  dom.catalogBrandInput.value = "";
  dom.catalogDiameterInput.value = "";
  dom.catalogMotorInput.value = "";
  dom.catalogFixationInput.value = "";
  dom.catalogSortInput.value = "diameter-asc";
}

function getCatalogFilterState(dom) {
  return {
    search: String(dom.catalogSearchInput.value || "").trim(),
    brand: dom.catalogBrandInput.value,
    diameterCm: Number.parseInt(dom.catalogDiameterInput.value, 10),
    motor: dom.catalogMotorInput.value,
    fixation: dom.catalogFixationInput.value,
    sort: dom.catalogSortInput.value || "diameter-asc"
  };
}

function renderCatalogTable(models) {
  if (models.length === 0) {
    return `
      <div class="notice warning">
        <strong>Aucun modele ne correspond aux filtres actifs.</strong>
        Ajustez les criteres ou reinitialisez les filtres pour retrouver la base complete.
      </div>
    `;
  }

  return `
    <div class="table-wrap catalog-table-wrap">
      <table>
        <thead>
          <tr>
            <th>Ref.</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Diam.</th>
            <th>Moteur</th>
            <th>Fixation</th>
            <th>Plafond/BA</th>
            <th>H test</th>
            <th>CE dir debout</th>
            <th>CFE dir debout</th>
            <th>CE moyen</th>
            <th>LwA</th>
            <th>P max</th>
          </tr>
        </thead>
        <tbody>
          ${models
            .map(
              (model) => `
                <tr>
                  <td>${escapeHtml(model.id)}</td>
                  <td>${escapeHtml(model.brand)}</td>
                  <td>${escapeHtml(model.model)}</td>
                  <td>${escapeHtml(String(model.diameterCm))}</td>
                  <td>${escapeHtml(model.motor)}</td>
                  <td>${escapeHtml(model.fixation)}</td>
                  <td>${formatNumber(model.ceilingDistanceCm, 1)} cm</td>
                  <td>${formatNumber(model.testHeightCm, 1)} cm</td>
                  <td>${formatTemp(model.ceDirDeboutMax)}</td>
                  <td>${formatNumber(model.cfeDirDeboutMax, 4)} °C/W</td>
                  <td>${formatTemp(model.ceAvgMax)}</td>
                  <td>${formatDb(model.lwaMaxDbA)}</td>
                  <td>${formatWatts(model.powerMaxW, 0)}</td>
                </tr>
              `
            )
            .join("")}
        </tbody>
      </table>
    </div>
  `;
}

function renderCatalog(dom, brasse2Models) {
  const filters = getCatalogFilterState(dom);
  const models = getFilteredCatalogModels(brasse2Models, filters);
  const activeFilters = getCatalogActiveFilterLabels(filters);

  dom.catalogSummary.innerHTML = `
    <strong>${models.length}</strong> modele${models.length > 1 ? "s" : ""} affiche${models.length > 1 ? "s" : ""}
    sur ${brasse2Models.length}
    ${activeFilters.length > 0 ? ` • Filtres actifs : ${escapeHtml(activeFilters.join(" • "))}` : ""}
  `;
  dom.catalogTableHost.innerHTML = renderCatalogTable(models);
}

// src/ui/planSvg.js
const ROOM_CORNER_RADIUS = 6;

function svgLabelBox(x, y, label, anchor = "middle", fontSize = 10.5) {
  const textWidth = Math.max(54, label.length * (fontSize * 0.6));
  let rectX = x - textWidth / 2 - 6;

  if (anchor === "start") {
    rectX = x - 6;
  } else if (anchor === "end") {
    rectX = x - textWidth - 6;
  }

  return `
    <g>
      <rect x="${rectX}" y="${y - 12}" width="${textWidth + 12}" height="18" rx="9"
        fill="rgba(255,250,242,0.96)" stroke="rgba(29,47,44,0.12)" />
      <text x="${x}" y="${y + 1.5}" text-anchor="${anchor}" font-size="${fontSize}" fill="#163531" font-weight="700">${label}</text>
    </g>
  `;
}

function svgDimensionLine(
  x1,
  y1,
  x2,
  y2,
  label,
  anchor = "middle",
  offsetX = 0,
  offsetY = -8,
  fontSize = 10.5
) {
  const labelX = (x1 + x2) / 2 + offsetX;
  const labelY = (y1 + y2) / 2 + offsetY;
  const isHorizontal = Math.abs(x2 - x1) >= Math.abs(y2 - y1);
  const startTick = isHorizontal
    ? `<line x1="${x1}" y1="${y1 - 5}" x2="${x1}" y2="${y1 + 5}" stroke="#b86d21" stroke-width="2" />`
    : `<line x1="${x1 - 5}" y1="${y1}" x2="${x1 + 5}" y2="${y1}" stroke="#b86d21" stroke-width="2" />`;
  const endTick = isHorizontal
    ? `<line x1="${x2}" y1="${y2 - 5}" x2="${x2}" y2="${y2 + 5}" stroke="#b86d21" stroke-width="2" />`
    : `<line x1="${x2 - 5}" y1="${y2}" x2="${x2 + 5}" y2="${y2}" stroke="#b86d21" stroke-width="2" />`;

  return `
    <g>
      <line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="#b86d21" stroke-width="2" />
      ${startTick}
      ${endTick}
      ${svgLabelBox(labelX, labelY, label, anchor, fontSize)}
    </g>
  `;
}

function svgFanSymbol(cx, cy, radius, index) {
  const bladeLength = radius * 0.78;
  const bladeWidth = Math.max(7, radius * 0.24);
  const hubRadius = Math.max(6, radius * 0.16);
  const bladeShape = `
    <path d="
      M ${hubRadius * 0.35} ${-bladeWidth / 2}
      Q ${bladeLength * 0.48} ${-bladeWidth * 0.72} ${bladeLength} 0
      Q ${bladeLength * 0.48} ${bladeWidth * 0.72} ${hubRadius * 0.35} ${bladeWidth / 2}
      Z
    " fill="#c9d3df" stroke="#6b7280" stroke-width="1.2" />
  `;

  return `
    <g>
      <circle cx="${cx}" cy="${cy}" r="${radius}" fill="rgba(31,79,130,0.06)" stroke="rgba(31,79,130,0.45)" stroke-width="1.6" stroke-dasharray="5 4" />
      <g transform="translate(${cx} ${cy})">
        <g transform="rotate(0)">${bladeShape}</g>
        <g transform="rotate(120)">${bladeShape}</g>
        <g transform="rotate(240)">${bladeShape}</g>
        <circle cx="0" cy="0" r="${hubRadius}" fill="#4b5563" />
        <circle cx="0" cy="0" r="${Math.max(2.6, hubRadius * 0.35)}" fill="#f9fafb" />
      </g>
      <circle cx="${cx}" cy="${cy}" r="5" fill="#111827" />
      <text x="${cx}" y="${cy + 3.8}" text-anchor="middle" font-size="9" fill="#ffffff" font-weight="700">${index + 1}</text>
    </g>
  `;
}

function getPlanCanvasMetrics(item) {
  const roomAspectRatio =
    Math.max(item.room.length, item.room.width) / Math.min(item.room.length, item.room.width);
  const rotateForFit = roomAspectRatio >= 2.2 && item.room.length > item.room.width;

  if (rotateForFit) {
    return {
      roomAspectRatio,
      rotateForFit,
      canvasWidth: 320,
      canvasHeight: 400,
      wrapMinHeight: 392
    };
  }

  if (roomAspectRatio >= 1.55) {
    return {
      roomAspectRatio,
      rotateForFit,
      canvasWidth: 360,
      canvasHeight: 290,
      wrapMinHeight: 320
    };
  }

  return {
    roomAspectRatio,
    rotateForFit,
    canvasWidth: 360,
    canvasHeight: 250,
    wrapMinHeight: 280
  };
}

function planWrapStyle(item) {
  const { wrapMinHeight } = getPlanCanvasMetrics(item);
  return `--plan-min-height:${wrapMinHeight}px;`;
}

function buildPlanGeometry(room) {
  const { roomAspectRatio, rotateForFit, canvasWidth, canvasHeight } = getPlanCanvasMetrics({ room });
  const paddingX = 28;
  const paddingY = 28;
  const displayRoomLength = rotateForFit ? room.width : room.length;
  const displayRoomHeight = rotateForFit ? room.length : room.width;
  const scale = Math.min(
    (canvasWidth - paddingX * 2) / displayRoomLength,
    (canvasHeight - paddingY * 2) / displayRoomHeight
  );
  const roomWidth = displayRoomLength * scale;
  const roomHeight = displayRoomHeight * scale;
  const roomX = (canvasWidth - roomWidth) / 2;
  const roomY = (canvasHeight - roomHeight) / 2;

  return {
    roomAspectRatio,
    rotateForFit,
    width: canvasWidth,
    height: canvasHeight,
    roomX,
    roomY,
    roomWidth,
    roomHeight,
    scale,
    displayRoomLength,
    displayRoomHeight,
    compactMode: roomAspectRatio >= 2.4,
    labelFontSize: roomAspectRatio >= 2.4 ? 9.2 : 10.5,
    compactOffset: roomAspectRatio >= 2.4 ? 2 : 0,
    projectPoint(point) {
      const displayPoint = rotateForFit ? { x: point.y, y: point.x } : point;
      return {
        x: roomX + displayPoint.x * scale,
        y: roomY + displayPoint.y * scale
      };
    },
    projectRect(rectangle) {
      const minPoint = rotateForFit
        ? { x: rectangle.minY, y: rectangle.minX }
        : { x: rectangle.minX, y: rectangle.minY };
      const maxPoint = rotateForFit
        ? { x: rectangle.maxY, y: rectangle.maxX }
        : { x: rectangle.maxX, y: rectangle.maxY };

      return {
        x: roomX + Math.min(minPoint.x, maxPoint.x) * scale,
        y: roomY + Math.min(minPoint.y, maxPoint.y) * scale,
        width: Math.abs(maxPoint.x - minPoint.x) * scale,
        height: Math.abs(maxPoint.y - minPoint.y) * scale
      };
    }
  };
}

function getRenderedCoordinates(item) {
  const ceilingAssessment = item.ceilingAssessment;

  if (ceilingAssessment?.enabled && ceilingAssessment.compatible && ceilingAssessment.appliedCoordinates) {
    return ceilingAssessment.appliedCoordinates;
  }

  return item.coordinates;
}

function renderCeilingOverlay(geometry, ceilingGrid) {
  if (!ceilingGrid) {
    return "";
  }

  return ceilingGrid.tiles
    .map((tile) => {
      const rectangle = geometry.projectRect(tile);
      const fill = tile.hasLuminaire
        ? "rgba(184,109,33,0.22)"
        : tile.isCut
          ? "rgba(29,47,44,0.05)"
          : "rgba(255,255,255,0.28)";
      const stroke = tile.hasLuminaire ? "rgba(184,109,33,0.64)" : "rgba(29,47,44,0.12)";

      return `
        <rect
          x="${rectangle.x}"
          y="${rectangle.y}"
          width="${rectangle.width}"
          height="${rectangle.height}"
          rx="6"
          fill="${fill}"
          stroke="${stroke}"
          stroke-width="${tile.hasLuminaire ? 1.3 : 1}"
        />
      `;
    })
    .join("");
}

function renderTheoreticalGhostCenters(geometry, item, displayCoordinates) {
  const assessment = item.ceilingAssessment;

  if (!assessment?.compatible || !assessment.shiftApplied) {
    return "";
  }

  return item.coordinates
    .map((point, index) => {
      const renderedPoint = geometry.projectPoint(point);
      const shiftedPoint = geometry.projectPoint(displayCoordinates[index]);
      return `
        <g>
          <line
            x1="${renderedPoint.x}"
            y1="${renderedPoint.y}"
            x2="${shiftedPoint.x}"
            y2="${shiftedPoint.y}"
            stroke="rgba(107,114,128,0.5)"
            stroke-width="1.2"
            stroke-dasharray="4 4"
          />
          <circle cx="${renderedPoint.x}" cy="${renderedPoint.y}" r="3.2" fill="rgba(107,114,128,0.65)" />
        </g>
      `;
    })
    .join("");
}

function renderUniformGrid(geometry, item) {
  const displayCellLength = geometry.rotateForFit ? item.cellWidth : item.cellLength;
  const displayCellHeight = geometry.rotateForFit ? item.cellLength : item.cellWidth;
  const displayNx = geometry.rotateForFit ? item.ny : item.nx;
  const displayNy = geometry.rotateForFit ? item.nx : item.ny;
  const gridLines = [];

  for (let index = 1; index < displayNx; index += 1) {
    const x = geometry.roomX + index * displayCellLength * geometry.scale;
    gridLines.push(
      `<line x1="${x}" y1="${geometry.roomY}" x2="${x}" y2="${geometry.roomY + geometry.roomHeight}" stroke="rgba(29,47,44,0.18)" stroke-dasharray="6 5" />`
    );
  }

  for (let index = 1; index < displayNy; index += 1) {
    const y = geometry.roomY + index * displayCellHeight * geometry.scale;
    gridLines.push(
      `<line x1="${geometry.roomX}" y1="${y}" x2="${geometry.roomX + geometry.roomWidth}" y2="${y}" stroke="rgba(29,47,44,0.18)" stroke-dasharray="6 5" />`
    );
  }

  return gridLines.join("");
}

function renderFans(geometry, item, displayCoordinates) {
  return displayCoordinates
    .map((point, index) => {
      const renderedPoint = geometry.projectPoint(point);
      const radius = (item.diameter / 2) * geometry.scale;
      return svgFanSymbol(renderedPoint.x, renderedPoint.y, radius, index);
    })
    .join("");
}

function getNearestWallMeasurement(room, point) {
  return [
    { axis: "x", boundary: 0, clearance: point.x },
    { axis: "x", boundary: room.length, clearance: room.length - point.x },
    { axis: "y", boundary: 0, clearance: point.y },
    { axis: "y", boundary: room.width, clearance: room.width - point.y }
  ].sort((a, b) => a.clearance - b.clearance)[0];
}

function renderCandidateMeasurements(geometry, candidate, displayCoordinates) {
  if (displayCoordinates.length === 0) {
    return "";
  }

  const firstPoint = displayCoordinates[0];
  const firstRenderedPoint = geometry.projectPoint(firstPoint);
  const radius = (candidate.diameter / 2) * geometry.scale;
  const measurements = [];

  measurements.push(
    svgDimensionLine(
      firstRenderedPoint.x - radius,
      firstRenderedPoint.y - radius - (geometry.compactMode ? 8 : 12),
      firstRenderedPoint.x + radius,
      firstRenderedPoint.y - radius - (geometry.compactMode ? 8 : 12),
      `D ${formatMeters(candidate.diameter)}`,
      "middle",
      0,
      geometry.compactMode ? -6 : -8,
      geometry.labelFontSize
    )
  );

  const nearestWall = getNearestWallMeasurement(candidate.room, firstPoint);
  const displayedWallClearance =
    candidate.ceilingAssessment?.wallClearance || candidate.wallClearance;

  if (nearestWall.axis === "x") {
    const wallX = nearestWall.boundary === 0 ? geometry.roomX : geometry.roomX + geometry.roomWidth;
    const lineY = Math.min(
      geometry.height - 14,
      firstRenderedPoint.y + radius + (geometry.compactMode ? 12 : 16)
    );

    measurements.push(
      svgDimensionLine(
        wallX,
        lineY,
        firstRenderedPoint.x,
        lineY,
        `Mur ${formatMeters(displayedWallClearance)}`,
        "middle",
        0,
        geometry.compactMode ? -6 : -8,
        geometry.labelFontSize
      )
    );
  } else {
    const wallY = nearestWall.boundary === 0 ? geometry.roomY : geometry.roomY + geometry.roomHeight;
    const lineX = Math.min(
      geometry.width - 16,
      firstRenderedPoint.x + radius + (geometry.compactMode ? 12 : 16)
    );

    measurements.push(
      svgDimensionLine(
        lineX,
        wallY,
        lineX,
        firstRenderedPoint.y,
        `Mur ${formatMeters(displayedWallClearance)}`,
        "start",
        8 + geometry.compactOffset,
        -2,
        geometry.labelFontSize
      )
    );
  }

  if (candidate.interFanSpacing) {
    const displayCellLength = geometry.rotateForFit ? candidate.cellWidth : candidate.cellLength;
    const displayCellHeight = geometry.rotateForFit ? candidate.cellLength : candidate.cellWidth;
    const displayNx = geometry.rotateForFit ? candidate.ny : candidate.nx;
    const displayNy = geometry.rotateForFit ? candidate.nx : candidate.ny;
    const horizontalSpacing = displayNx > 1 ? displayCellLength : Number.POSITIVE_INFINITY;
    const verticalSpacing = displayNy > 1 ? displayCellHeight : Number.POSITIVE_INFINITY;

    if (horizontalSpacing <= verticalSpacing) {
      const secondRenderedPoint = geometry.projectPoint({
        x: firstPoint.x + displayCellLength,
        y: firstPoint.y
      });
      const lineY = Math.max(
        geometry.roomY + 10,
        firstRenderedPoint.y - radius - (geometry.compactMode ? 26 : 34)
      );
      measurements.push(
        svgDimensionLine(
          firstRenderedPoint.x,
          lineY,
          secondRenderedPoint.x,
          lineY,
          `Entraxe ${formatMeters(candidate.interFanSpacing)}`,
          "middle",
          0,
          geometry.compactMode ? -6 : -8,
          geometry.labelFontSize
        )
      );
    } else {
      const secondRenderedPoint = geometry.projectPoint({
        x: firstPoint.x,
        y: firstPoint.y + displayCellHeight
      });
      const lineX = Math.min(
        geometry.width - 12,
        firstRenderedPoint.x + radius + (geometry.compactMode ? 24 : 32)
      );
      measurements.push(
        svgDimensionLine(
          lineX,
          firstRenderedPoint.y,
          lineX,
          secondRenderedPoint.y,
          `Entraxe ${formatMeters(candidate.interFanSpacing)}`,
          "start",
          8 + geometry.compactOffset,
          -2,
          geometry.labelFontSize
        )
      );
    }
  }

  return measurements.join("");
}

function svgForCandidate(candidate) {
  const geometry = buildPlanGeometry(candidate.room);
  const displayCoordinates = getRenderedCoordinates(candidate);
  const fans = renderFans(geometry, candidate, displayCoordinates);
  const theoreticalGhosts = renderTheoreticalGhostCenters(geometry, candidate, displayCoordinates);
  const measurements = renderCandidateMeasurements(geometry, candidate, displayCoordinates);

  return `
    <svg viewBox="0 0 ${geometry.width} ${geometry.height}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
      <rect x="${geometry.roomX}" y="${geometry.roomY}" width="${geometry.roomWidth}" height="${geometry.roomHeight}" rx="${ROOM_CORNER_RADIUS}" fill="#faf5eb" stroke="rgba(29,47,44,0.2)" stroke-width="2.5" />
      ${renderCeilingOverlay(geometry, candidate.ceilingGrid)}
      ${renderUniformGrid(geometry, candidate)}
      ${theoreticalGhosts}
      ${fans}
      ${measurements}
    </svg>
  `;
}

// src/ui/ceilingEditor.js
const CEILING_EDITOR_ROOM_CORNER_RADIUS = 6;

function buildCeilingEditorGeometry(room) {
  const { rotateForFit, canvasWidth, canvasHeight } = getPlanCanvasMetrics({ room });
  const paddingX = 28;
  const paddingY = 28;
  const displayRoomLength = rotateForFit ? room.width : room.length;
  const displayRoomHeight = rotateForFit ? room.length : room.width;
  const scale = Math.min(
    (canvasWidth - paddingX * 2) / displayRoomLength,
    (canvasHeight - paddingY * 2) / displayRoomHeight
  );
  const roomWidth = displayRoomLength * scale;
  const roomHeight = displayRoomHeight * scale;
  const roomX = (canvasWidth - roomWidth) / 2;
  const roomY = (canvasHeight - roomHeight) / 2;

  return {
    rotateForFit,
    width: canvasWidth,
    height: canvasHeight,
    scale,
    roomWidth,
    roomHeight,
    roomX,
    roomY,
    projectRect(rectangle) {
      const minX = rotateForFit ? rectangle.minY : rectangle.minX;
      const maxX = rotateForFit ? rectangle.maxY : rectangle.maxX;
      const minY = rotateForFit ? rectangle.minX : rectangle.minY;
      const maxY = rotateForFit ? rectangle.maxX : rectangle.maxY;

      return {
        x: roomX + Math.min(minX, maxX) * scale,
        y: roomY + Math.min(minY, maxY) * scale,
        width: Math.abs(maxX - minX) * scale,
        height: Math.abs(maxY - minY) * scale
      };
    }
  };
}

function renderCeilingEditorSvg(room, ceilingGrid) {
  const geometry = buildCeilingEditorGeometry(room);
  const tileShapes = ceilingGrid.tiles
    .map((tile) => {
      const rectangle = geometry.projectRect(tile);
      const fill = tile.hasLuminaire
        ? "rgba(184,109,33,0.28)"
        : tile.isCut
          ? "rgba(29,47,44,0.06)"
          : "rgba(255,255,255,0.88)";
      const stroke = tile.hasLuminaire ? "rgba(184,109,33,0.72)" : "rgba(29,47,44,0.14)";

      return `
        <rect
          x="${rectangle.x}"
          y="${rectangle.y}"
          width="${rectangle.width}"
          height="${rectangle.height}"
          rx="6"
          fill="${fill}"
          stroke="${stroke}"
          stroke-width="${tile.hasLuminaire ? 1.4 : 1}"
          data-luminaire-tile-key="${tile.key}"
          class="ceiling-editor-tile${tile.hasLuminaire ? " is-luminaire" : ""}"
        />
      `;
    })
    .join("");

  return `
    <svg viewBox="0 0 ${geometry.width} ${geometry.height}" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
      <rect
        x="${geometry.roomX}"
        y="${geometry.roomY}"
        width="${geometry.roomWidth}"
        height="${geometry.roomHeight}"
        rx="${CEILING_EDITOR_ROOM_CORNER_RADIUS}"
        fill="#faf5eb"
        stroke="rgba(29,47,44,0.2)"
        stroke-width="2.5"
      />
      ${tileShapes}
    </svg>
  `;
}

function renderCeilingEditor(dom, state, room) {
  const isEnabled = !!state.ceilingLayout.enabled;

  dom.ceilingConfig.classList.toggle("hidden", !isEnabled);
  dom.ceilingAnchorXInput.value = state.ceilingLayout.xAnchorMode;
  dom.ceilingAnchorYInput.value = state.ceilingLayout.yAnchorMode;

  if (!isEnabled) {
    dom.ceilingEditorSummary.textContent = "";
    dom.ceilingEditorCanvas.innerHTML = "";
    dom.clearCeilingLuminairesButton.disabled = true;
    return null;
  }

  const ceilingGrid = buildCeilingGrid(room, state.ceilingLayout);

  if (!ceilingGrid) {
    dom.ceilingEditorSummary.textContent = "";
    dom.ceilingEditorCanvas.innerHTML = "";
    dom.clearCeilingLuminairesButton.disabled = true;
    return null;
  }

  dom.ceilingEditorSummary.innerHTML = `
    ${ceilingGrid.tiles.length} dalles visibles, dont ${ceilingGrid.cutTilesCount} dalle${ceilingGrid.cutTilesCount > 1 ? "s" : ""} recoupée${ceilingGrid.cutTilesCount > 1 ? "s" : ""}.
    ${ceilingGrid.luminairesCount} luminaire${ceilingGrid.luminairesCount > 1 ? "s" : ""} placé${ceilingGrid.luminairesCount > 1 ? "s" : ""}.
    Piece ${formatMeters(room.length)} × ${formatMeters(room.width)} (${formatSquareMeters(room.length * room.width)}).
  `;
  dom.ceilingEditorCanvas.innerHTML = renderCeilingEditorSvg(room, ceilingGrid);
  dom.clearCeilingLuminairesButton.disabled = ceilingGrid.luminairesCount === 0;

  return ceilingGrid;
}

// src/ui/reportHeader.js
function refreshReportHeader(
  dom,
  { simulationName, generatedAt = new Date() }
) {
  dom.reportKicker.textContent = `Rapport de simulation • le ${formatDateTime(generatedAt)}`;
  dom.reportTitle.textContent = simulationName;
  dom.reportContext.textContent = "";
  dom.reportContext.classList.add("hidden");

  const metaItems = [];
  dom.reportMeta.innerHTML = metaItems
    .map(
      ([label, value]) => `
        <article class="report-meta-card">
          <strong>${escapeHtml(label)}</strong>
          <span>${escapeHtml(value)}</span>
        </article>
      `
    )
    .join("");
  dom.reportMeta.classList.toggle("hidden", metaItems.length === 0);
}

function setExportAvailability(dom, enabled) {
  dom.exportPdfButton.disabled = !enabled;
}

function setExportSelectionSummary(dom, text = "") {
  if (!dom.exportSelectionSummary) {
    return;
  }

  dom.exportSelectionSummary.textContent = text;
  dom.exportSelectionSummary.classList.toggle("hidden", !text);
}

// src/ui/results.js
const modelSectionRegistry = new Map();
let nextModelSectionId = 1;

function registerModelSection(models) {
  const sectionId = `compatible-models-${nextModelSectionId}`;
  nextModelSectionId += 1;
  modelSectionRegistry.set(sectionId, models);
  return sectionId;
}

function getDefaultCompatibleModelsComparator(a, b) {
  if (a.isSelectedDiameter !== b.isSelectedDiameter) {
    return a.isSelectedDiameter ? -1 : 1;
  }
  if (Math.abs(b.compatibleOption.diameter - a.compatibleOption.diameter) > EPS) {
    return b.compatibleOption.diameter - a.compatibleOption.diameter;
  }
  return compareComfort(a, b);
}

function sortCompatibleModels(models, sortKey = "default") {
  const comparator =
    {
      default: getDefaultCompatibleModelsComparator,
      "diameter-desc": (a, b) =>
        b.diameterCm - a.diameterCm || getDefaultCompatibleModelsComparator(a, b),
      "diameter-asc": (a, b) =>
        a.diameterCm - b.diameterCm || getDefaultCompatibleModelsComparator(a, b),
      "fcc-desc": (a, b) => compareCoverage(a, b) || getDefaultCompatibleModelsComparator(a, b),
      comfort: (a, b) => compareComfort(a, b) || getDefaultCompatibleModelsComparator(a, b),
      efficiency: (a, b) =>
        compareEfficiency(a, b) || getDefaultCompatibleModelsComparator(a, b),
      acoustics: (a, b) =>
        compareAcoustics(a, b) || getDefaultCompatibleModelsComparator(a, b)
    }[sortKey] || getDefaultCompatibleModelsComparator;

  return [...models].sort(comparator);
}

function filterCompatibleModels(models, { search }) {
  const normalizedSearch = normalizeCatalogSearch(search);

  return models.filter((model) => {
    if (!normalizedSearch) {
      return true;
    }

    const haystack = normalizeCatalogSearch(
      [
        model.id,
        model.brand,
        model.model,
        model.motor,
        model.fixation,
        String(model.diameterCm)
      ].join(" ")
    );

    return haystack.includes(normalizedSearch);
  });
}

function renderModelsTableSummary(filteredCount, totalCount) {
  if (filteredCount === totalCount) {
    return `${totalCount} modele${totalCount > 1 ? "s" : ""}`;
  }

  return `${filteredCount} / ${totalCount} modele${totalCount > 1 ? "s" : ""}`;
}

function getNeutralMountSummary(items) {
  const labels = [
    ...new Set(
      items
        .map((item) => item.mountMode?.id)
        .filter(Boolean)
        .map((mountId) => (mountId === "low-profile" ? "low-profile" : "standard"))
    )
  ];

  return labels.length > 0 ? labels.join(" + ") : "Aucun";
}

function getMaxDiameterSummary(items) {
  const diameters = items.map((item) => item.diameter).filter(Number.isFinite);

  if (diameters.length === 0) {
    return {
      value: "Aucun",
      detail: "Aucun diametre reel compatible"
    };
  }

  const maxDiameter = Math.max(...diameters);

  return {
    value: `${formatDiameterCm(maxDiameter)} cm`,
    detail: "Plus grand diametre reel compatible sur les options affichees"
  };
}

function getDistinctCompatibleModels(items, brasse2Models) {
  const modelsById = new Map();

  items.forEach((item) => {
    getBrasse2ModelsForCandidate(item, brasse2Models).forEach((model) => {
      modelsById.set(model.id, model);
    });
  });

  return [...modelsById.values()];
}

function createSummaryCard(label, value, detail) {
  return `
    <article class="summary-card">
      <strong>${label}</strong>
      <span>${value}</span>
      <div class="hint">${detail}</div>
    </article>
  `;
}

function renderExportOptionToggle(optionKey, checked) {
  return `
    <label class="export-option-toggle">
      <input type="checkbox" data-export-option-key="${optionKey}" ${checked ? "checked" : ""}>
      <span>Inclure dans le PDF</span>
    </label>
  `;
}

function renderModelCard(title, model) {
  if (!model) {
    return "";
  }
  const hasCoverageFactor = Number.isFinite(model.compatibleOption.coverageFactor);

  return `
    <article class="model-card">
      <strong>${title}</strong>
      <h4>${model.brand} - ${model.model}</h4>
      <p>${model.motor} • ${model.fixation} • ${model.diameterCm} cm${model.isSelectedDiameter ? " • diametre retenu" : ""}</p>
      <div class="model-stats">
        <span>${hasCoverageFactor ? `FCC reel du calepinage: ${formatFactor(model.compatibleOption.coverageFactor)}` : `Diametre BRASSE II compatible: ${model.diameterCm} cm`}</span>
        <span>CE direct debout Vmax: ${formatTemp(model.ceDirDeboutMax)}</span>
        <span>CFE direct debout Vmax: ${formatNumber(model.cfeDirDeboutMax, 4)} °C/W</span>
        <span>LwA Vmax: ${formatDb(model.lwaMaxDbA)}</span>
        <span>${modelMountLabel(model)} (${formatNumber(model.ceilingDistanceCm, 1)} cm de base)</span>
      </div>
    </article>
  `;
}

function formatCeilingShiftLabel(assessment) {
  if (!assessment?.shiftApplied) {
    return "Aucun decalage";
  }

  return `${formatNumber(assessment.dx * 100, 0)} cm / ${formatNumber(assessment.dy * 100, 0)} cm`;
}

function renderCeilingDetailItems(item) {
  const assessment = item.ceilingAssessment;

  if (!assessment?.enabled) {
    return "";
  }

  return `
    <div class="detail-item">
      <strong>Compatibilite faux plafond</strong>
      <span>${assessment.compatible ? "Compatible" : "Non compatible"}</span>
    </div>
    <div class="detail-item">
      <strong>Decalage de trame</strong>
      <span>${formatCeilingShiftLabel(assessment)}</span>
    </div>
    <div class="detail-item">
      <strong>Conflit luminaire</strong>
      <span>${assessment.luminaireConflict ? "Oui" : "Non"}</span>
    </div>
  `;
}

function renderCeilingNotice(item) {
  const assessment = item.ceilingAssessment;

  if (!assessment?.enabled) {
    return "";
  }

  return `
    <div class="notice ${assessment.compatible ? "warning" : "danger"}">
      <strong>${assessment.compatible ? "Lecture faux plafond." : "Faux plafond non compatible."}</strong>
      ${assessment.reasonText}
      ${assessment.visualCheckNote ? ` ${assessment.visualCheckNote}` : ""}
    </div>
  `;
}

function renderModelsTable(models) {
  if (models.length === 0) {
    return `
      <div class="notice warning">
        <strong>Aucun modele ne correspond aux filtres actifs.</strong>
        Elargissez la recherche ou reinitialisez les filtres de cette option.
      </div>
    `;
  }

  return `
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>Diam.</th>
            <th>FCC reel</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Moteur</th>
            <th>Fixation</th>
            <th>Plafond/BA</th>
            <th>CE dir debout Vmax</th>
            <th>CFE dir debout Vmax</th>
            <th>CE moyen Vmax</th>
            <th>LwA Vmax</th>
            <th>Lecture montage</th>
          </tr>
        </thead>
        <tbody>
          ${models
            .map(
              (model) => `
                <tr>
                  <td>${model.diameterCm}</td>
                  <td>${Number.isFinite(model.compatibleOption.coverageFactor) ? formatFactor(model.compatibleOption.coverageFactor) : "—"}</td>
                  <td>${model.brand}</td>
                  <td>${model.model}</td>
                  <td>${model.motor}</td>
                  <td>${model.fixation}</td>
                  <td>${formatNumber(model.ceilingDistanceCm, 1)} cm</td>
                  <td>${formatTemp(model.ceDirDeboutMax)}</td>
                  <td>${formatNumber(model.cfeDirDeboutMax, 4)} °C/W</td>
                  <td>${formatTemp(model.ceAvgMax)}</td>
                  <td>${formatDb(model.lwaMaxDbA)}</td>
                  <td>${modelMountLabel(model)}</td>
                </tr>
              `
            )
            .join("")}
        </tbody>
      </table>
    </div>
  `;
}

function renderModelsToolbar(sectionId, models) {
  return `
    <div class="models-toolbar">
      <div class="field">
        <label for="${sectionId}-search">Recherche</label>
        <input id="${sectionId}-search" type="search" placeholder="Ref., marque, modele" data-model-search>
      </div>
      <div class="field">
        <label for="${sectionId}-sort">Tri</label>
        <select id="${sectionId}-sort" data-model-sort>
          <option value="default">Ordre recommande</option>
          <option value="fcc-desc">FCC reel decroissant</option>
          <option value="comfort">Confort</option>
          <option value="efficiency">Efficacite</option>
          <option value="acoustics">Acoustique</option>
          <option value="diameter-desc">Diametre decroissant</option>
          <option value="diameter-asc">Diametre croissant</option>
        </select>
      </div>
      <p class="models-toolbar-summary" data-models-summary>${renderModelsTableSummary(models.length, models.length)}</p>
    </div>
  `;
}

function renderModelsTablePanel(sectionId, models) {
  return `
    <div class="models-table-panel" data-model-section="${sectionId}">
      ${renderModelsToolbar(sectionId, models)}
      <div data-models-table-host>${renderModelsTable(sortCompatibleModels(models))}</div>
    </div>
  `;
}

function updateModelsTableSection(sectionElement) {
  if (!sectionElement) {
    return;
  }

  const sectionId = sectionElement.dataset.modelSection;
  const models = modelSectionRegistry.get(sectionId) || [];
  const search = sectionElement.querySelector("[data-model-search]")?.value || "";
  const sort = sectionElement.querySelector("[data-model-sort]")?.value || "default";

  const filteredModels = filterCompatibleModels(models, {
    search
  });
  const sortedModels = sortCompatibleModels(filteredModels, sort);
  const host = sectionElement.querySelector("[data-models-table-host]");
  const summary = sectionElement.querySelector("[data-models-summary]");

  if (host) {
    host.innerHTML = renderModelsTable(sortedModels);
  }
  if (summary) {
    summary.textContent = renderModelsTableSummary(filteredModels.length, models.length);
  }
}

function bindResultsInteractions(dom) {
  if (dom.resultsList.dataset.resultsInteractionsBound === "true") {
    return;
  }

  const refreshFromEvent = (event) => {
    const target = event.target;
    if (
      !target ||
      !target.matches?.(
        "[data-model-search], [data-model-sort]"
      )
    ) {
      return;
    }

    updateModelsTableSection(target.closest("[data-model-section]"));
  };

  dom.resultsList.addEventListener("input", refreshFromEvent);
  dom.resultsList.addEventListener("change", refreshFromEvent);
  dom.resultsList.dataset.resultsInteractionsBound = "true";
}

function resetResultsModelSections() {
  modelSectionRegistry.clear();
  nextModelSectionId = 1;
}

function renderBrasse2Section(candidate, brasse2Models, realDiameters) {
  const models = getBrasse2ModelsForCandidate(candidate, brasse2Models);

  if (models.length === 0) {
    return `
      <section class="models-shell">
        <div>
          <h4 class="section-title">Modeles pré-sélectionnés</h4>
          <p class="section-subtitle" style="margin-bottom:0;">
            Filtrage sur les diametres BRASSE II admissibles pour ce calepinage.
          </p>
        </div>
        <div class="notice warning">
          <strong>Aucun modele BRASSE II compatible.</strong>
          La base embarquee couvre ici les diametres disponibles dans BRASSE II :
          <code>${formatDiameterList(realDiameters)}</code>.
        </div>
      </section>
    `;
  }

  const sectionId = registerModelSection(models);
  const modelPicks = buildModelPicks(models);

  return `
    <section class="models-shell">
      <div>
        <h4 class="section-title">Modeles pré-sélectionnés</h4>
        <p class="section-subtitle" style="margin-bottom:0;">
          Filtrage sur tous les diametres admissibles. Les cartes ci-dessous lisent le meilleur FCC du calepinage,
          puis les indicateurs BRASSE a Vmax : confort direct debout, efficacite directe debout et acoustique.
        </p>
      </div>

      <div class="models-grid">
        ${modelPicks.map((pick) => renderModelCard(pick.title, pick.model)).join("")}
      </div>

      <details class="models-details">
        <summary>Voir tous les modeles BRASSE II compatibles (${models.length})</summary>
        ${renderModelsTablePanel(sectionId, models)}
      </details>
    </section>
  `;
}

function candidateCard(candidate, rank, brasse2Models, realDiameters, selectedOptionKeys) {
  const warnings = getCandidateWarnings(candidate);
  const isSelectedForExport = selectedOptionKeys.has(candidate.key);

  return `
    <article class="result-card">
      <div class="result-head">
        <div>
          <h3 class="result-title">Option ${rank}</h3>
          <p class="result-subtitle">
            ${candidate.fanCount} brasseur${candidate.fanCount > 1 ? "s" : ""} centre${candidate.fanCount > 1 ? "s" : ""}
            dans des cellules de ${formatMeters(candidate.cellLength)} × ${formatMeters(candidate.cellWidth)},
            avec un diametre BRASSE II retenu de ${formatMeters(candidate.diameter)}.
          </p>
        </div>
        ${renderExportOptionToggle(candidate.key, isSelectedForExport)}
      </div>

      <div class="result-grid">
        <div class="plan-wrap" style="${planWrapStyle(candidate)}">${svgForCandidate(candidate)}</div>

        <div class="stack">
          <div class="metric-grid">
            <div class="metric-card">
              <strong>Diametre réel retenu</strong>
              <span>${formatMeters(candidate.diameter)}</span>
            </div>
            <div class="metric-card">
              <strong>Diametre theorique max</strong>
              <span>${formatMeters(candidate.theoreticalMaxDiameter)}</span>
            </div>
            <div class="metric-card">
              <strong>Facteur de forme</strong>
              <span>${formatFactor(candidate.formFactor)}</span>
            </div>
            <div class="metric-card">
              <strong>FCC reel</strong>
              <span>${formatFactor(candidate.coverageFactor)}</span>
            </div>
          </div>

          <div class="detail-list">
            <div class="detail-item">
              <strong>Montage</strong>
              <span>${candidate.mountMode.label}</span>
            </div>
            <div class="detail-item">
              <strong>Hauteur sous pales</strong>
              <span class="detail-value-stack">
                <span>${formatMeters(candidate.bladeHeight)}</span>
                <span class="detail-subtext">Plafond-pales: ${formatMeters(candidate.mountDistance)}</span>
              </span>
            </div>
            <div class="detail-item">
              <strong>Murs</strong>
              <span>Centre vers mur le plus proche: ${formatMeters(candidate.wallClearance)} &gt; ${formatMeters(candidate.diameter)}</span>
            </div>
            <div class="detail-item">
              <strong>Entre brasseurs</strong>
              <span>${candidate.interFanSpacing ? `${formatMeters(candidate.interFanSpacing)} &gt; ${formatNumber(2.5, 1)} × D` : "Non applicable (un seul brasseur)"}</span>
            </div>
            <div class="detail-item detail-item-stack">
              <strong>Diametres BRASSE II admissibles (FCC &gt;= 0,2)</strong>
              <span>${candidate.compatibleRealDiameters.map((option) => formatDiameterCm(option.diameter)).join(", ")}</span>
            </div>
            ${renderCeilingDetailItems(candidate)}
          </div>
        </div>
      </div>

      ${warnings.length > 0 ? `
        <div class="notice warning">
          <strong>Point d'attention.</strong>
          ${warnings.join(" ")}
        </div>
      ` : ""}

      ${renderCeilingNotice(candidate)}

      ${renderBrasse2Section(candidate, brasse2Models, realDiameters)}

    </article>
  `;
}

function renderSummary(dom, room, candidates, brasse2Models) {
  const roomArea = room.length * room.width;
  const displayedCandidates = candidates.slice(0, 5);
  const compatibleModels = getDistinctCompatibleModels(displayedCandidates, brasse2Models);
  const diameterSummary = getMaxDiameterSummary(displayedCandidates);
  const mountSummary = getNeutralMountSummary(displayedCandidates);

  dom.summaryGrid.innerHTML = [
    createSummaryCard(
      "Piece",
      `${formatMeters(room.length)} × ${formatMeters(room.width)}`,
      `${formatSquareMeters(roomArea)} - HSP ${formatMeters(room.height)}`
    ),
    createSummaryCard(
      "Options valides",
      `${candidates.length}`,
      `Montages visibles : ${mountSummary}`
    ),
    createSummaryCard(
      "Diametre max",
      diameterSummary.value,
      diameterSummary.detail
    ),
    createSummaryCard(
      "Base BRASSE II",
      compatibleModels.length > 0
        ? `${compatibleModels.length} modeles`
        : "Aucun modele compatible",
      "Compatibles avec les options affichees"
    )
  ].join("");
  dom.highlights.innerHTML = "";
}

function renderStatusNote(
  dom,
  room,
  candidates,
  fallbackFlush,
  modes,
  realDiameters
) {
  const notes = [];
  const heightRequirementMessage = buildHeightDiameterRequirementMessage(room, modes, realDiameters);

  if (candidates.length > 0) {
    const best = candidates[0];
    if (best.mountMode.id === "low-profile") {
      notes.push(`
        <div class="notice warning">
          <strong>Le meilleur cas passe en low-profile.</strong>
          Le guide indique alors une baisse de vitesse d'air d'environ 15 %. Une variante standard est egalement
          affichee plus bas si elle existe. Le diametre retenu est choisi ici parmi les diametres reels disponibles.
        </div>
      `);
    }
  }

  if (fallbackFlush && candidates.length === 0) {
    notes.push(`
      <div class="notice danger">
        <strong>Aucune solution standard ou low-profile n'a ete trouvee.</strong>
        Un montage flush pourrait rouvrir une piste jusqu'a ${formatMeters(fallbackFlush.diameter)}, mais le guide
        annonce alors une perte de performance superieure a 40 % et demande de l'eviter au maximum.
        ${heightRequirementMessage}
      </div>
    `);
  }

  if (!fallbackFlush && candidates.length === 0) {
    notes.push(`
      <div class="notice danger">
        <strong>Aucun diametre réel de la liste testée ne passe dans ce local.</strong>
        L'outil a teste ${formatDiameterList(realDiameters)} avec les regles BRASSE
        de FCC, de distances et de hauteur.
        ${heightRequirementMessage}
      </div>
    `);
  }

  dom.statusNote.innerHTML = notes.join("");
}

function renderResults(dom, candidates, brasse2Models, realDiameters, selectedOptionKeys = []) {
  resetResultsModelSections();
  const selectedOptionKeySet = new Set(selectedOptionKeys);
  dom.resultsList.innerHTML = candidates
    .slice(0, 5)
    .map((candidate, index) =>
      candidateCard(candidate, index + 1, brasse2Models, realDiameters, selectedOptionKeySet)
    )
    .join("");
}

// src/report/pdf.js
function getAllReportOptions(state) {
  if (!state) {
    return [];
  }

  if (state.kind === "uniformity-ok") {
    return state.candidates || [];
  }

  return [];
}

function getReportOptionNumber(state, option) {
  const index = getAllReportOptions(state).findIndex((item) => item.key === option.key);
  return index >= 0 ? index + 1 : null;
}

function getDistinctCompatibleModels(options, brasse2Models) {
  const modelsById = new Map();

  options.forEach((option) => {
    getBrasse2ModelsForCandidate(option, brasse2Models).forEach((model) => {
      modelsById.set(model.id, model);
    });
  });

  return [...modelsById.values()];
}

function getMaxDiameterSummary(options) {
  const diameters = options.map((option) => option.diameter).filter(Number.isFinite);

  if (diameters.length === 0) {
    return {
      value: "Aucun",
      detail: "Aucun diametre reel compatible"
    };
  }

  const maxDiameter = Math.max(...diameters);
  return {
    value: `${formatDiameterCm(maxDiameter)} cm`,
    detail: "Plus grand diametre reel des options exportees"
  };
}

function renderReportWarningList(items) {
  if (!items || items.length === 0) {
    return "";
  }

  return `
    <div class="report-note report-note-warning">
      <strong>Points d'attention</strong>
      <ul>
        ${items.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
      </ul>
    </div>
  `;
}

function renderReportSummaryCards(items) {
  return `
    <div class="report-summary-grid">
      ${items
        .map(
          ([label, value, detail]) => `
            <article class="report-summary-card">
              <strong>${escapeHtml(label)}</strong>
              <span>${escapeHtml(value)}</span>
              <small>${escapeHtml(detail)}</small>
            </article>
          `
        )
        .join("")}
    </div>
  `;
}

function renderReportMetricGrid(items) {
  return `
    <div class="report-metrics">
      ${items
        .map(
          ([label, value]) => `
            <article class="report-metric">
              <strong>${escapeHtml(label)}</strong>
              <span>${escapeHtml(value)}</span>
            </article>
          `
        )
        .join("")}
    </div>
  `;
}

function renderReportTable(headers, rows, compact = false) {
  return `
    <table class="report-table${compact ? " compact" : ""}">
      <thead>
        <tr>
          ${headers.map((header) => `<th>${escapeHtml(header)}</th>`).join("")}
        </tr>
      </thead>
      <tbody>
        ${rows
          .map(
            (row) => `
              <tr>
                ${row.map((cell) => `<td>${escapeHtml(cell)}</td>`).join("")}
              </tr>
            `
          )
          .join("")}
      </tbody>
    </table>
  `;
}

function renderModelHighlights(option, brasse2Models) {
  const highlights = getReportModelHighlights(option, brasse2Models);
  if (highlights.length === 0) {
    return `
      <div class="report-note">
        <strong>Modeles pre-selectionnes</strong>
        <p>Aucun modele compatible dans la base BRASSE II embarquee sur les diametres admissibles de cette option.</p>
      </div>
    `;
  }

  const rows = highlights.map((entry) => [
    entry.title,
    `${entry.model.brand} ${entry.model.model}`,
    `${entry.model.diameterCm} cm`,
    Number.isFinite(entry.model.compatibleOption.coverageFactor)
      ? formatFactor(entry.model.compatibleOption.coverageFactor)
      : "—",
    formatTemp(entry.model.ceDirDeboutMax),
    `${formatNumber(entry.model.cfeDirDeboutMax, 4)} °C/W`,
    formatDb(entry.model.lwaMaxDbA)
  ]);

  return `
    <section class="report-section">
      <h3>Modeles pre-selectionnes</h3>
      ${renderReportTable(
        ["Lecture", "Marque / modele", "Diam.", "FCC", "CE dir.", "CFE dir.", "LwA"],
        rows,
        true
      )}
    </section>
  `;
}

function renderStudySummarySection(state, selectedOptions, brasse2Models) {
  const roomArea = state.room.length * state.room.width;
  const compatibleModels = getDistinctCompatibleModels(selectedOptions, brasse2Models);
  const maxDiameterSummary = getMaxDiameterSummary(selectedOptions);
  const baseCards = [
    [
      "Piece",
      `${formatMeters(state.room.length)} × ${formatMeters(state.room.width)}`,
      `${formatSquareMeters(roomArea)} - HSP ${formatMeters(state.room.height)}`
    ],
    [
      "Diametre max",
      maxDiameterSummary.value,
      maxDiameterSummary.detail
    ],
    [
      "Base BRASSE II",
      compatibleModels.length > 0 ? `${compatibleModels.length} modeles` : "Aucun modele",
      "Compatibles avec les options exportees"
    ]
  ];

  return `
    <section class="report-block">
      <h2>Synthese de la piece etudiee</h2>
      ${renderReportSummaryCards(baseCards)}
    </section>
  `;
}

function renderSelectedOptionsOverview(state, selectedOptions) {
  if (selectedOptions.length === 0) {
    return `
      <section class="report-block">
        <h2>Synthese de ou des option(s) retenue(s) :</h2>
        <div class="report-note report-note-warning">
          <strong>Aucune option selectionnee</strong>
          <p>Cochez au moins une option dans l'outil pour l'inclure dans le rapport PDF.</p>
        </div>
      </section>
    `;
  }

  if (state.kind === "uniformity-ok") {
    const rows = selectedOptions.map((option) => [
      `Option ${getReportOptionNumber(state, option)}`,
      `${option.nx} × ${option.ny}`,
      formatMeters(option.diameter),
      option.mountMode.label,
      formatFactor(option.coverageFactor)
    ]);

    return `
      <section class="report-block">
        <h2>Synthese de ou des option(s) retenue(s) :</h2>
        ${renderReportTable(
          ["Option", "Trame", "Diametre reel", "Montage", "FCC reel"],
          rows,
          true
        )}
      </section>
    `;
  }
}

function renderUniformityOptionPage(state, option, brasse2Models) {
  const optionNumber = getReportOptionNumber(state, option);

  return `
    <section class="report-page report-option-page">
      <div class="report-section-head">
        <p class="report-section-kicker">Option ${optionNumber}</p>
        <h2>${escapeHtml(`${option.nx} × ${option.ny} cellules`)}</h2>
        <p>${escapeHtml(`${option.fanCount} brasseur${option.fanCount > 1 ? "s" : ""} • ${option.mountMode.label}`)}</p>
      </div>

      ${renderReportMetricGrid([
        ["Diametre reel retenu", formatMeters(option.diameter)],
        ["Diametre theorique max", formatMeters(option.theoreticalMaxDiameter)],
        ["Facteur de forme", formatFactor(option.formFactor)],
        ["FCC reel", formatFactor(option.coverageFactor)],
        ["Hauteur sous pales", formatMeters(option.bladeHeight)],
        ["Plafond-pales", formatMeters(option.mountDistance)]
      ])}

      <div class="report-plan-block">
        <div class="report-plan">${svgForCandidate(option)}</div>
        <div class="report-side">
          ${renderReportTable(
            ["Lecture", "Valeur"],
            [
              ["Montage", option.mountMode.label],
              ["Cellule", `${formatMeters(option.cellLength)} × ${formatMeters(option.cellWidth)}`],
              ["Mur limitant", `${formatMeters(option.wallClearance)} > ${formatMeters(option.diameter)}`],
              [
                "Entraxe mini",
                option.interFanSpacing
                  ? `${formatMeters(option.interFanSpacing)} > 2,5 × D`
                  : "Non applicable"
              ],
              ["Diametres admissibles", formatDiameterCmList(option.compatibleRealDiameters)]
            ],
            true
          )}
          ${renderReportWarningList(getCandidateWarnings(option))}
        </div>
      </div>

      ${renderModelHighlights(option, brasse2Models)}
    </section>
  `;
}

function renderReportFirstPage(state, selectedOptions, brasse2Models) {
  return `
    <section class="report-page report-first-page">
      <header class="report-cover">
        <p class="report-cover-kicker">Rapport de simulation • le ${escapeHtml(
          formatDateTime(state.generatedAt)
        )}</p>
        <h1>${escapeHtml(state.simulationName)}</h1>
        <p class="report-cover-lead">
          Synthese de la piece etudiee et des options selectionnees pour l'export PDF.
        </p>
      </header>

      ${renderStudySummarySection(state, selectedOptions, brasse2Models)}
      ${renderSelectedOptionsOverview(state, selectedOptions)}
    </section>
  `;
}

function renderEmptyOrInvalidReport(state) {
  const roomBlock =
    state.room && Number.isFinite(state.room.length)
      ? renderReportSummaryCards([
          [
            "Piece",
            `${formatMeters(state.room.length)} × ${formatMeters(state.room.width)}`,
            `${formatSquareMeters(state.room.length * state.room.width)} - HSP ${formatMeters(
              state.room.height
            )}`
          ]
        ])
      : "";

  let title = "Calcul impossible";
  let text =
    "Les donnees saisies ne permettent pas de produire un rapport de calepinage exploitable.";

  if (state.kind === "uniformity-empty") {
    title = "Aucune solution compatible";
    text =
      "Aucun cas standard ou low-profile n'a pu etre valide avec les regles BRASSE de FCC, de distances et de hauteur.";
  }

  return `
    <section class="report-page report-first-page">
      <header class="report-cover">
        <p class="report-cover-kicker">Rapport de simulation • le ${escapeHtml(
          formatDateTime(state.generatedAt)
        )}</p>
        <h1>${escapeHtml(state.simulationName)}</h1>
      </header>
      ${roomBlock}
      <section class="report-block">
        <h2>Conclusion</h2>
        <div class="report-note report-note-warning">
          <strong>${escapeHtml(title)}</strong>
          <p>${escapeHtml(text)}</p>
        </div>
        ${
          state.issues?.length
            ? `
              <div class="report-note" style="margin-top: 10px;">
                <strong>Points releves</strong>
                <ul>
                  ${state.issues.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
                </ul>
              </div>
            `
            : ""
        }
      </section>
    </section>
  `;
}

function buildPdfReportStyles() {
  return `
    @page { size: A4; margin: 12mm; }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "Segoe UI", Arial, sans-serif;
      color: #17212b;
      background: #ffffff;
      line-height: 1.45;
    }
    .report-root {
      max-width: 186mm;
      margin: 0 auto;
    }
    .report-page + .report-page {
      break-before: page;
      page-break-before: always;
    }
    .report-cover {
      display: grid;
      gap: 8px;
      padding-bottom: 12px;
      border-bottom: 1px solid #d6d9df;
      margin-bottom: 16px;
    }
    .report-cover-kicker,
    .report-section-kicker {
      margin: 0;
      color: #6b7280;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }
    .report-cover h1,
    .report-section-head h2 {
      margin: 0;
      font-size: 22px;
      line-height: 1.15;
    }
    .report-cover-lead {
      margin: 0;
      color: #4b5563;
      font-size: 12px;
    }
    .report-section-head p {
      margin: 4px 0 0;
      color: #4b5563;
      font-size: 12px;
    }
    .report-summary-grid,
    .report-metrics {
      display: grid;
      gap: 10px;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .report-summary-card,
    .report-metric {
      border: 1px solid #d6d9df;
      border-radius: 10px;
      padding: 10px 12px;
      background: #f8fafc;
    }
    .report-summary-card strong,
    .report-metric strong {
      display: block;
      margin-bottom: 4px;
      color: #6b7280;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }
    .report-summary-card span,
    .report-metric span {
      display: block;
      font-size: 13px;
      font-weight: 700;
      color: #111827;
    }
    .report-summary-card small {
      display: block;
      margin-top: 4px;
      color: #4b5563;
      font-size: 11px;
      line-height: 1.4;
    }
    .report-block,
    .report-section,
    .report-note,
    .report-plan-block {
      break-inside: avoid;
      page-break-inside: avoid;
    }
    .report-block + .report-block,
    .report-section + .report-section {
      margin-top: 16px;
    }
    .report-block h2,
    .report-section h3 {
      margin: 0 0 10px;
      font-size: 15px;
      line-height: 1.25;
    }
    .report-section-head {
      display: grid;
      gap: 2px;
      margin-bottom: 12px;
      padding-bottom: 12px;
      border-bottom: 1px solid #d6d9df;
    }
    .report-plan-block {
      display: grid;
      gap: 14px;
      grid-template-columns: minmax(0, 1.25fr) minmax(0, 0.95fr);
      align-items: start;
      margin-top: 12px;
    }
    .report-plan {
      border: 1px solid #d6d9df;
      border-radius: 10px;
      padding: 10px;
      background: #f8fafc;
    }
    .report-plan svg {
      width: 100%;
      height: auto;
      display: block;
    }
    .report-side {
      display: grid;
      gap: 10px;
    }
    .report-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 11px;
    }
    .report-table th,
    .report-table td {
      padding: 8px 9px;
      border: 1px solid #d6d9df;
      vertical-align: top;
      text-align: left;
    }
    .report-table th {
      background: #f8fafc;
      color: #374151;
      font-weight: 700;
    }
    .report-table.compact th,
    .report-table.compact td {
      padding: 7px 8px;
      font-size: 10.5px;
    }
    .report-note {
      border: 1px solid #d6d9df;
      border-radius: 10px;
      padding: 10px 12px;
      background: #f8fafc;
    }
    .report-note-warning {
      background: #fff6e5;
      border-color: rgba(184, 109, 33, 0.24);
    }
    .report-note strong {
      display: block;
      margin-bottom: 6px;
      font-size: 11px;
    }
    .report-note p,
    .report-note ul {
      margin: 0;
      color: #4b5563;
      font-size: 11px;
    }
    .report-note ul {
      padding-left: 16px;
    }
    .report-footer {
      margin-top: 18px;
      padding-top: 10px;
      border-top: 1px solid #d6d9df;
      color: #4b5563;
      font-size: 10.5px;
    }
    .report-footer a {
      color: #1f4f82;
      text-decoration: none;
    }
    @media print {
      .report-root {
        max-width: none;
      }
    }
  `;
}

function buildPdfFilename(simulationName) {
  const base = simulationName
    .normalize("NFD")
    .replace(/[\u0300-\u036f]/g, "")
    .replace(/[^a-zA-Z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "")
    .toLowerCase();
  const stamp = new Intl.DateTimeFormat("sv-SE", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  }).format(new Date());
  return `${base || "simulation"}-${stamp}`;
}

function buildPdfReportDocument(state, brasse2Models) {
  if (!state) {
    return "";
  }

  const selectedOptions = getSelectedReportOptions(state);
  let bodyContent = "";

  if (state.kind === "uniformity-ok") {
    bodyContent = `
      ${renderReportFirstPage(state, selectedOptions, brasse2Models)}
      ${selectedOptions
        .map((option) => renderUniformityOptionPage(state, option, brasse2Models))
        .join("")}
    `;
  } else {
    bodyContent = renderEmptyOrInvalidReport(state);
  }

  return `
    <!DOCTYPE html>
    <html lang="fr">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>${escapeHtml(buildPdfFilename(state.simulationName))}</title>
      <style>${buildPdfReportStyles()}</style>
    </head>
    <body>
      <main class="report-root">
        ${bodyContent}
        <footer class="report-footer">
          Fait par Nathan Château, <a href="mailto:chateaunathan@proton.me">chateaunathan@proton.me</a>
        </footer>
      </main>
    </body>
    </html>
  `;
}

function exportPdfReport(latestReportState, brasse2Models) {
  if (!latestReportState) {
    return;
  }

  const reportHtml = buildPdfReportDocument(latestReportState, brasse2Models);
  if (typeof window.open === "function") {
    const popup = window.open("", "_blank");
    if (popup && popup.document) {
      const triggerPrint = () => {
        try {
          popup.focus();
          popup.print();
        } catch (error) {
          console.error("Impossible de lancer l'impression du rapport.", error);
        }
      };

      popup.onload = () => {
        window.setTimeout(triggerPrint, 150);
      };
      popup.document.open();
      popup.document.write(reportHtml);
      popup.document.close();
      window.setTimeout(triggerPrint, 600);
      return;
    }
  }

  if (typeof window.print === "function") {
    window.print();
  }
}

// src/main.js
const dom = getDomRefs();
const state = createAppState();
const brasse2Models = BRASSE2_MODELS;
const realDiameters = [
  ...new Set(
    brasse2Models
      .map((model) => Number(model.diameterCm) / 100)
      .filter((value) => Number.isFinite(value) && value > 0)
  )
].sort((a, b) => a - b);

function getSimulationName() {
  const value = String(dom.simulationNameInput.value || "").trim();
  return value || "Simulation sans nom";
}

function getSelectedModes() {
  const modes = [];
  if (dom.allowStandardInput.checked) {
    modes.push(MOUNT_MODES[0]);
  }
  if (dom.allowLowInput.checked) {
    modes.push(MOUNT_MODES[1]);
  }
  return modes;
}

function getSelectedModesLabel(modes = getSelectedModes()) {
  const labels = modes.map((mode) => mode.label);
  return labels.length > 0 ? labels.join(" + ") : "Aucun";
}

function getRoomInputs() {
  return {
    length: parseNumber(dom.lengthInput),
    width: parseNumber(dom.widthInput),
    height: parseNumber(dom.heightInput)
  };
}

function renderCeilingPanel() {
  dom.ceilingEnabledInput.checked = state.ceilingLayout.enabled;
  return renderCeilingEditor(dom, state, getRoomInputs());
}

function getActiveCeilingGrid(room) {
  if (!state.ceilingLayout.enabled) {
    return null;
  }

  return buildCeilingGrid(room, state.ceilingLayout);
}

function attachCeilingAssessments(items, room) {
  const ceilingGrid = getActiveCeilingGrid(room);

  if (!ceilingGrid) {
    return items;
  }

  return items.map((item) => ({
    ...item,
    ceilingGrid,
    ceilingAssessment: assessOptionCeilingCompatibility(item, ceilingGrid)
  }));
}

function updateHeader({ room, recommendation = "", generatedAt = new Date() }) {
  refreshReportHeader(dom, {
    simulationName: getSimulationName(),
    modesLabel: getSelectedModesLabel(),
    recommendation,
    generatedAt,
    room
  });
}

function validateInputs(values) {
  const issues = [];
  if (!(values.length > 0)) {
    issues.push("La longueur doit etre strictement positive.");
  }
  if (!(values.width > 0)) {
    issues.push("La largeur doit etre strictement positive.");
  }
  if (!(values.height > 0)) {
    issues.push("La hauteur sous plafond doit etre strictement positive.");
  }
  if (!dom.allowStandardInput.checked && !dom.allowLowInput.checked) {
    issues.push("Selectionnez au moins un type de montage.");
  }
  return issues;
}

function updateExportControls() {
  const latestReportState = state.latestReportState;
  if (!latestReportState) {
    setExportAvailability(dom, false);
    setExportSelectionSummary(dom, "");
    return;
  }

  const selectedOptions = getSelectedReportOptions(latestReportState);
  if (selectedOptions.length === 0) {
    setExportAvailability(dom, latestReportState.kind !== "uniformity-ok");
    setExportSelectionSummary(
      dom,
      latestReportState.kind === "uniformity-ok" ? "Aucune option selectionnee pour le PDF" : ""
    );
    return;
  }

  const selectableCount =
    latestReportState.kind === "uniformity-ok" ? latestReportState.candidates.length : 0;

  setExportAvailability(dom, true);
  setExportSelectionSummary(
    dom,
    selectableCount > 0
      ? `${selectedOptions.length} option${selectedOptions.length > 1 ? "s" : ""} selectionnee${selectedOptions.length > 1 ? "s" : ""} sur ${selectableCount}`
      : ""
  );
}

function resetResultsVisibility() {
  dom.emptyState.classList.add("hidden");
  dom.resultsContent.classList.add("hidden");
  setExportAvailability(dom, false);
  setExportSelectionSummary(dom, "");
  setLatestReportState(state, null);
  resetResultsModelSections();
}

function renderInvalidState(rawValues, issues, generatedAt) {
  updateHeader({
    room: rawValues,
    recommendation: "Calcul impossible",
    generatedAt
  });
  setLatestReportState(state, {
    kind: "invalid",
    simulationName: getSimulationName(),
    room: rawValues,
    modesLabel: getSelectedModesLabel(),
    generatedAt,
    context: "Calcul impossible",
    issues
  });
  dom.statusNote.innerHTML = `
    <div class="notice danger">
      <strong>Calcul impossible.</strong><br>
      ${issues.join(" ")}
    </div>
  `;
  dom.summaryGrid.innerHTML = "";
  dom.highlights.innerHTML = "";
  dom.resultsList.innerHTML = "";
  dom.resultsContent.classList.remove("hidden");
}

function renderUniformityEmpty(room, fallbackFlush, generatedAt) {
  updateHeader({
    room,
    recommendation: "Aucune solution compatible",
    generatedAt
  });
  const modes = getSelectedModes();
  const heightRequirementMessage = buildHeightDiameterRequirementMessage(room, modes, realDiameters);
  setLatestReportState(state, {
    kind: "uniformity-empty",
    simulationName: getSimulationName(),
    room,
    modesLabel: getSelectedModesLabel(modes),
    generatedAt,
    context: "Aucune solution compatible",
    fallbackFlush,
    issues: [
      "Aucun cas standard ou low-profile n'a respecte les regles BRASSE de FCC, de distances et de hauteur.",
      ...(heightRequirementMessage ? [heightRequirementMessage] : [])
    ]
  });
  dom.summaryGrid.innerHTML = [
    createSummaryCard("Local", `${formatMeters(room.length)} × ${formatMeters(room.width)}`, `HSP ${formatMeters(room.height)}`),
    createSummaryCard("Resultat", "Aucune solution", "Aucun cas standard ou low-profile compatible"),
    createSummaryCard("Base BRASSE II", `${brasse2Models.length} modeles integres`, "Sans effet ici tant que la geometrie bloque")
  ].join("");
  dom.highlights.innerHTML = "";
  renderStatusNote(dom, room, [], fallbackFlush, modes, realDiameters);
  dom.resultsList.innerHTML = "";
  setExportAvailability(dom, true);
  dom.resultsContent.classList.remove("hidden");
}

function render() {
  renderCeilingPanel();

  const rawValues = getRoomInputs();
  const issues = validateInputs(rawValues);
  const generatedAt = new Date();

  resetResultsVisibility();

  if (issues.length > 0) {
    renderInvalidState(rawValues, issues, generatedAt);
    return;
  }

  const room = {
    length: rawValues.length,
    width: rawValues.width,
    height: rawValues.height
  };
  const modes = getSelectedModes();
  const candidates = attachCeilingAssessments(
    enumerateCandidates(room, MAX_GRID_FANS, modes, realDiameters),
    room
  );
  const fallbackFlush =
    candidates.length === 0 ? getFallbackFlushCandidate(room, MAX_GRID_FANS, realDiameters) : null;

  if (candidates.length === 0) {
    renderUniformityEmpty(room, fallbackFlush, generatedAt);
    return;
  }

  updateHeader({
    room,
    recommendation: `Option recommandee : ${candidates[0].nx} × ${candidates[0].ny} - ${candidates[0].fanCount} brasseur${candidates[0].fanCount > 1 ? "s" : ""}`,
    generatedAt
  });
  setLatestReportState(state, {
    kind: "uniformity-ok",
    simulationName: getSimulationName(),
    room,
    candidates: candidates.slice(0, 5),
    selectedOptionKeys: createSelectedReportOptionKeys(candidates.slice(0, 5)),
    modesLabel: getSelectedModesLabel(modes),
    generatedAt,
    context: `Recherche d'uniformite • option recommandee : ${candidates[0].nx} × ${candidates[0].ny}`
  });
  renderSummary(dom, room, candidates, brasse2Models);
  renderStatusNote(dom, room, candidates, fallbackFlush, modes, realDiameters);
  renderResults(
    dom,
    candidates,
    brasse2Models,
    realDiameters,
    state.latestReportState?.selectedOptionKeys || []
  );
  updateExportControls();
  dom.resultsContent.classList.remove("hidden");
}

dom.form.addEventListener("submit", (event) => {
  event.preventDefault();
  render();
});

dom.resetButton.addEventListener("click", () => {
  dom.lengthInput.value = "9";
  dom.widthInput.value = "5";
  dom.heightInput.value = "2.5";
  dom.simulationNameInput.value = "";
  dom.allowStandardInput.checked = true;
  dom.allowLowInput.checked = true;
  resetState(state);
  render();
});

dom.simulationNameInput.addEventListener("input", () => {
  updateHeader({
    room: getRoomInputs()
  });
});

dom.exportPdfButton.addEventListener("click", () => {
  exportPdfReport(state.latestReportState, brasse2Models);
});

dom.catalogSearchInput.addEventListener("input", () => {
  renderCatalog(dom, brasse2Models);
});

[
  dom.catalogBrandInput,
  dom.catalogDiameterInput,
  dom.catalogMotorInput,
  dom.catalogFixationInput,
  dom.catalogSortInput
].forEach((input) => {
  input.addEventListener("change", () => {
    renderCatalog(dom, brasse2Models);
  });
});

dom.catalogResetButton.addEventListener("click", () => {
  resetCatalogFilters(dom);
  renderCatalog(dom, brasse2Models);
});

dom.resultsList.addEventListener("change", (event) => {
  const target = event.target;
  if (!target?.matches?.("[data-export-option-key]")) {
    return;
  }

  toggleLatestReportOptionSelection(state, target.dataset.exportOptionKey, target.checked);
  updateExportControls();
});

dom.ceilingEnabledInput.addEventListener("change", () => {
  updateCeilingLayout(state, {
    enabled: dom.ceilingEnabledInput.checked
  });
  render();
});

[dom.ceilingAnchorXInput, dom.ceilingAnchorYInput].forEach((input) => {
  input.addEventListener("change", () => {
    updateCeilingLayout(state, {
      xAnchorMode: dom.ceilingAnchorXInput.value,
      yAnchorMode: dom.ceilingAnchorYInput.value
    });
    render();
  });
});

dom.ceilingEditorCanvas.addEventListener("click", (event) => {
  const tileElement =
    event.target && typeof event.target.closest === "function"
      ? event.target.closest("[data-luminaire-tile-key]")
      : null;

  if (!tileElement) {
    return;
  }

  toggleCeilingLuminaireTile(state, tileElement.dataset.luminaireTileKey);
  render();
});

dom.clearCeilingLuminairesButton.addEventListener("click", () => {
  clearCeilingLuminaires(state);
  render();
});

[dom.lengthInput, dom.widthInput, dom.heightInput, dom.allowStandardInput, dom.allowLowInput].forEach(
  (input) => {
    input.addEventListener("change", () => {
      render();
    });
  }
);

updateHeader({
  room: getRoomInputs()
});
setExportAvailability(dom, false);
setExportSelectionSummary(dom, "");
bindResultsInteractions(dom);
initializeCatalogFilters(dom, brasse2Models);
renderCatalog(dom, brasse2Models);
render();
})();
